# VS_LIKE — 수치 · 체감 테스트 (Tuning)

> **최종 갱신:** 2026-09-04 (C41 — **§4 플레이 기록 개시**: `D58` 계측으로 6개 닫힘.
> 9층 294마리 **111 fps** · 탄 구분 **색상각 170도 + 밝기차 0.383** · vSync 경고가 실제로 걸렸다.
> §K 적 등장·행동 계수 14개 · §J 보스 · §I 밀도 · §H-2 영구 강화도 이 문서에 있다)
> 2026-09-03 (C30 — **§J 보스** 신설 · §H-2 **영구 강화 7종** 신설 ·
> §I **적 밀도·`MaxAlive` 천장·구슬 회수** 신설 · §G 를 `D27` 실측 결과로 재작성)
> 완료 이력은 [`SETUP_STATUS.md`](SETUP_STATUS.md), 남은 작업은 [`TODO.md`](TODO.md),
> 수치를 **어떻게** 고치는지는 [`BALANCE.md`](BALANCE.md).

**방침 (2026-08-29 확정):** 수치는 지금 잡지 않는다. **기능을 먼저 완성**하고,
플레이하면서 조절한다. 이 문서는 그때 볼 목록이다.

---

## 0. 이 문서의 범위

다른 문서와 겹치지 않게 경계를 그어 둔다.

| 문서 | 담는 질문 |
|---|---|
| [`TODO.md`](TODO.md) §1 | **"동작하나?"** — 고쳤는데 안 돌려본 것. 버그 여부. 예/아니오로 답이 난다 |
| **이 문서** | **"느낌이 맞나?"** — 눈·귀·손으로만 판정되는 것. 답이 숫자다 |
| [`BALANCE.md`](BALANCE.md) | **"어떻게 고치나?"** — CSV 파이프라인 사용법과 현재 수치의 근거 |

즉 여기 있는 항목은 **전부 사람이 직접 플레이해야만** 판정된다.
로그·캡처로 대신할 수 없어서 지금까지 미뤄진 것들이다.

---

## 1. 수치가 실제로 있는 곳

고치는 비용이 다르다. **튜닝 루프의 속도가 여기서 갈린다.**

| 대상 | 위치 | 반영 방법 | 비용 |
|---|---|---|---|
| 플레이어 스탯 · 무적 · 넉백 · 상점 · XP 곡선 | `Assets/Game/Balance/Economy.csv` | `Game/Balance/Import CSV -> ScriptableObjects` | 즉시 (재컴파일 없음) |
| 적 스탯 · AI 파라미터 | `Enemies.csv` | 〃 | 즉시 |
| 무기 · 패시브 · 아이템 · 진화 | `Weapons.csv` `Passives.csv` `Items.csv` `Evolutions.csv` | 〃 | 즉시 |
| 직업 · 승급 · 소지 상한 | `Classes.csv` `ClassEvolutions.csv` | 〃 | 즉시 |
| 웨이브 구성 · `MaxAlive` | `Waves.csv` | 〃 | 즉시 — 🔴 **단 `MaxAlive` 는 400 이 천장이다** (§I-1) |
| **보스 페이즈·내려찍기·소환** | `Bosses.csv` **(D31 신설)** | 〃 | 즉시 — 🔴 **`SlamWindup`·`SlamRadius` 는 직업 이속과 묶여 있다** (§J-1) |
| 🔴 **무리 분리 3값**(`SeparationRadius` `Weight` `EveryNSteps`) · 이웃 버퍼(`NeighborBufSize`) | `Assets/Scripts/Enemy/EnemyBase.cs:207-209, 235` **(C# `const` — DEV 소유)** | `REQ/DEV.md` 요청 | **비싸다** — 재컴파일 + 도메인 리로드. §I-2 |
| 🔴 **구슬 자동 흡수 반경**(`magnetActivationRange` 8) | `Assets/Scripts/Experience/ExpDrop.cs:26` **(프리팹 `[SerializeField]` — DEV 소유)** | 인스펙터 저장 | 즉시 **(DEV 만)** · §I-3 |
| **스테이지 맵 층 수 · 노드 가중치**(상점 빈도) | `Economy.csv` 의 `StageMapManager` 6줄 **(C27 에서 옮겨 왔다)** | 〃 | 즉시 |
| **영구 강화 7종**(메타 화면) | `Upgrades.csv` **(D30 신설)** | 〃 | 즉시 — 🔴 **항목을 넣고 빼면 씬 배열도 같이 고쳐야 한다** (§H-2) |
| 건물 산출 | `Buildings.csv` | 〃 | 즉시 |
| 드랍 확률 · 애셋 배열 | `SceneWiring.csv` | 〃 | 즉시 |
| 🔴 **안내 문구 외곽선·그림자** | `Assets/Fonts/Pretendard SDF - Prompt.mat` **(D36 신설 · 내 경로)** | 인스펙터 저장 | 즉시 — ⚠️ **공유 머티리얼(`Pretendard SDF`)을 고치면 텍스트 75개가 같이 바뀐다.** 이건 그 파생본이고 **2곳에만** 물려 있다 (§D) |
| 오디오 클립별 볼륨 · 피치 | `Assets/Game/Audio/AudioLibrary.asset` (인스펙터) | 저장만 | 즉시 |
| BGM/SFX 마스터 볼륨 | 씬의 `AudioManager` 인스펙터 | 저장만 | 즉시 |
| 🟡 **적 넉백 · 사망 팝 · 히트스톱 · 처치 흔들림** | `Economy.csv` 의 `CombatFeel` 12줄 **(표는 넣었고 컴포넌트는 DEV 대기 — C2)** | 〃 | 즉시 **(배선 후)** |

> ⚠️ **Import 는 플레이 모드에서 못 돈다** (`MarkSceneDirty` 제약). 일시정지도 플레이 모드다.
> 플레이를 멈추고 → Import → 다시 플레이가 한 사이클이다.

### 🟡 적 타격 반응을 CSV 로 뺀다 — `CombatFeel` (I-59 에서 발견 → C2 에서 처리 중)

I-44 로 넣은 **적** 쪽 타격 반응이 `EnemyBase.cs` 에 `const` 로 박혀 있었다.
**플레이어** 쪽(`knockbackForce` 9 · `shakeMagnitude` 0.18 등)은 `Economy.csv` 에 있는데
적 쪽만 빠졌다. **하필 이 값들이 §2-A 에서 가장 많이 만지게 될 것들이다** —
한 번 고칠 때마다 도메인 리로드를 기다려야 해서 튜닝 루프가 몹시 느려진다.

#### 왜 `EnemyBase,knockbackForce` 로 못 넣나

두 번 막혔다. 둘 다 확인하고 기각했다.

| 시도 | 왜 안 되나 |
|---|---|
| `Economy.csv` 에 `EnemyBase,...` 로 추가 | 임포터(`BalanceImporter.FindSceneComponent`)가 **씬만 훑는다**(`FindObjectsByType`). 적은 **프리팹 1개를 공유**하므로 씬 컴포넌트가 아니다 → `! 씬에 EnemyBase 없음` |
| `Enemies.csv` 에 열로 추가 | 흔들림·히트스톱은 **엘리트/보스 여부**로 갈리는데, 등급은 `EnemyData` 가 아니라 **소환 시점 인자**다(`Initialize(data, isElite, isBoss)`). 게다가 같은 숫자를 **6줄에 복붙**하게 된다 |

#### 확정 — `CombatFeel` 씬 컴포넌트 (사용자 결정, 2026-08-30)

GameManager 오브젝트에 `CombatFeel` 을 하나 붙이고, 적이 **쓰는 시점에** 읽어 간다.
임포터는 **고칠 게 없다** — 씬 컴포넌트라 기존 3열 규칙에 그대로 걸린다.

| 항목 | 값 |
|---|---|
| CSV | `Economy.csv` §적 타격감 — **12줄. 이미 넣었다 (C2)** |
| 코드 | `CombatFeel.cs` 신설 + `EnemyBase` 가 `const` 대신 읽기 — **DEV 대기** |
| 기준선 | 12줄 전부 **지금 `const` 값 그대로**다. 첫 Import 는 **화면이 안 바뀌는 게 정상** |

| 값 | 현재 | 원래 위치 |
|---|---:|---|
| `enemyKnockbackForce` | 6.0 | `EnemyBase.cs:354` |
| `enemyKnockbackTime` | 0.10 | `EnemyBase.cs:355` |
| `eliteKnockbackResist` · `bossKnockbackResist` | 0.4 · 0 | `EnemyBase.cs:384` |
| `deathPopTime` · `deathPopScale` | 0.14 · 1.25 | `EnemyBase.cs:435` `:450` |
| 엘리트 흔들림 · 히트스톱 | `0.20 / 0.25s` · `0.05s` | `EnemyBase.cs:469` `:471` |
| 보스 흔들림 · 히트스톱 | `0.45 / 0.5s` · `0.09s` | 〃 |

> ℹ️ **넉백 저항 2줄은 원래 표에 없던 것을 새로 뺀 것이다.** `EnemyBase.cs:384` 의
> `IsBoss ? 0f : IsElite ? 0.4f : 1f` 도 똑같이 감각으로 정한 숫자인데 상수로 박혀 있었다.
> **"보스는 아예 안 밀린다"가 옳은지**는 플레이해 봐야 아는 값이다.

---

## 1-9. 🔴 한 판만 한다면 이 셋부터 — **판정이 20개 넘게 밀려 있다**

*(2026-09-04 `C32` · 사용자 지시로 **요청은 안 넣고 문서로만** 남긴다)*

§2 의 판정 항목이 §G·§H·§I·§J 만 해도 **20개가 넘는데 §4 플레이 세션 기록은 아직 한 줄도 없다.**
전부 하려 들면 아무것도 안 된다. **아래 셋은 한 판으로 동시에 답이 나온다.**

| # | 무엇을 보나 | 어디서 | 못 맞으면 |
|---|---|---|---|
| ① | 🔴 **종료 로그의 `남은 런 골드 M`** | 콘솔 한 줄 | `M ≤ 300` 이면 가격이 맞다 · `M ≥ 600` 이면 배수구가 모자란다 → **§H** |
| ② | 🔴 **폭발광으로 보스 내려찍기를 피할 수 있나** | 보스전 | 못 피하면 `SlamWindup` **1.15 → 1.35** (반경이 아니다) → **§J-1** |
| ③ | 🔴 **첫 판이 끝났을 때 영구 강화를 하나라도 샀나** | 메타 화면 | 못 샀으면 가장 싼 칸 **60 → 45** → **§H-2** |

> 🔴 **`D58` 이 이 셋을 자동으로 못 닫는 이유를 찾았다** — 런은 **레벨업마다 `timeScale 0` 으로 멈춘다.**
> 그 카드 선택이 빌드를, 빌드가 처치 속도를, 처치 속도가 골드를 정하므로
> **자동으로 고르면 `M` 이 아니라 "아무 M" 이 나온다.**
> ⇒ **이 셋은 구조적으로 사람만 답할 수 있다.** 계측으로 대신할 방법이 없다.

**왜 이 셋인가**

- 셋 다 **한 판 완주(또는 사망) 한 번**이면 답이 나온다. 반복이 필요 없다
- 셋 다 **틀렸을 때 고칠 곳이 CSV 한 칸**이다 — 코드도 재컴파일도 필요 없다
- 셋 다 **최근에 값을 크게 바꾼 자리**다 (`C27` 가격 10배 · `C29` 강화 신설 · `C30` 보스).
  값을 바꿔 놓고 한 번도 안 본 상태가 가장 위험하다

> ⚠️ **`vSync` 를 먼저 끄고 볼 것.** 품질 5(Ultra)라 켜져 있고, 프레임이 6.94 ms 에 붙으면
> 성능이 아니라 vSync 를 보는 것이다 (§G). 프레임을 같이 볼 생각이면 그것부터.

---

## 2. 체감 판정 체크리스트

각 항목은 **[ ] 무엇을 보나 → 이상하면 어디를 얼마나** 형식이다.
판정이 끝나면 체크하고 §4 에 결과를 남긴다.

### A. 플레이어 · 타격감

- [ ] **맞았을 때 맞은 걸 아는가** — 넉백 `9 / 0.15s` + 붉은 플래시 `0.12s` + 화면 흔들림 `0.18`
      + `PlayerHit` 사운드(0.70). 넷이 동시에 터진다.
      → 과하면 `shakeMagnitude` 부터 낮춘다 (`Economy.csv`). 흔들림이 가장 먼저 피로해진다
- [ ] 🔴 **`invincibleTime` 0.6 초** — **난이도에 가장 크게 작용하는 단일 수치다.**
      적 하나가 붙어 있을 때 초당 피해 = `ContactDamage ÷ 0.6`.
      Zombie(12)면 초당 20, Ogre(25)면 초당 42 — 체력 100 이면 **2.4초에 죽는다**
      → 너무 빨리 죽으면 0.8~1.0 으로. 너무 안 죽으면 0.4 로
- [ ] **`MoveSpeed` 4 가 답답하지 않은가** — 가장 빠른 적 **Wolf(4.2)보다 느리다.**
      늑대에게서 도망칠 수 없다는 뜻이다. 의도라면 두고, 아니면 5 로
- [ ] **적 넉백 6.0** 이 대열을 너무 흩뜨리지 않는가 — 뱀서라이크는 적이 **뭉쳐 있어야**
      쓸어 담는 맛이 난다. 흩어지면 3~4 로 (`CombatFeel,enemyKnockbackForce`)
- [ ] **사망 팝 0.14초** 가 시체를 쌓아 보이게 하지 않는가 (`CombatFeel,deathPopTime`)
- [ ] 🔴 **엘리트/보스 처치 히트스톱이 렉처럼 느껴지지 않는가** — 일반 0.05s · 보스 0.09s.
      **노말 웨이브에서는 검증할 수 없다**(엘리트가 안 나옴). Elite/Boss 노드로 가야 한다
      (`CombatFeel,eliteHitstop` · `bossHitstop`)
- [ ] **보스가 안 밀리는 게 위압감으로 읽히는가 아니면 그냥 안 맞는 것처럼 보이는가** —
      지금 보스는 넉백 저항 **0**(전혀 안 밀림), 엘리트는 0.4 다.
      때리는 반응이 아예 없어 보이면 보스를 0.1~0.15 로 (`CombatFeel,bossKnockbackResist`).
      ⚠️ 올리면 **벽 없는 아레나에서 보스가 밀려 도망갈 수 있다** — 그게 원래 0 인 이유다
- [ ] **플레이어 1.0 유닛 / 일반 적 0.5 유닛** 비율이 실전투에서 맞는가 —
      주인공이 일반 몹의 2배다. 엘리트(×1.3 → 0.65)·보스(×2 → 1.0)와 나란히 놓였을 때
      **위압감이 사는지**
- [ ] **플레이어 걷기** — 프레임 속도 `framesPerSecond` 12 에서 발이 겉돌지 않는지 ·
      기울기 `leanAmount` 0.10 이 과하지 않은지 (피벗이 Center 라 **발도 반대로 살짝 밀린다**)

### B. 적

- [ ] **`MaxAlive` 60~130 이 화면을 얼마나 채우는가** — 60마리가 이미 벅차면 전부 낮춘다
      (`Waves.csv`). 반대로 허전하면 올린다. **뱀서라이크의 인상을 좌우하는 수치다**
      → ✅ **성능 천장을 이제 안다: 400 까지 공짜다.** 상세는 **§I-1** 로 옮겼다
- [ ] **재배치된 적이 억울하게 느껴지지 않는가** — 소환 반경 20 에 다시 놓으므로 화면 밖이지만,
      카메라 룩어헤드가 크면 시야에 들어올 수 있다
- [ ] 🔴 **Demon 의 `PreferredRange` 7 이 무기 사거리(3.5~6) 밖이다** —
      근접 직업(Warrior)이 Demon 을 **아예 못 잡는** 상황이 되는지 확인.
      되면 5~6 으로 낮춘다 (`Enemies.csv`)
- [ ] **Wolf 돌진 예고 0.45초가 난전에서 보이는가** — 지금 예고는 깜빡임뿐이다.
      안 보이면 피할 수 없는 공격이 된다 → 전용 연출(지면 화살표 등) 필요 = **새 애셋**
- [ ] **무리 분리(0.9)가 추격을 방해하지 않는가** — 적이 플레이어 주위를 맴돌기만 하면 낮춘다
      → 🔴 **반대 방향의 문제가 실측으로 나왔다 — 지금은 너무 겹친다**(800에서 85 %가 0.5유닛 안).
      낮추기 전에 **§I-2** 를 먼저 읽을 것
- [ ] **적탄(붉은색 0.55 스케일)이 난전에서 보이는가**
- [ ] **걷기 애니메이션 속도** *(I-58)* — `EnemyVisual.framesPerSecond` 10 기준에
      자기 `MoveSpeed` 배율(0.5~2.5배)이 곱해진다. Ogre(1.2)가 제자리걸음처럼 보이거나
      Wolf 돌진 중 발이 헛돌면 배율 상한/하한을 조인다 (`EnemyVisual.cs`)
- [ ] **6종이 실제로 섞여 나오는가** — 로그로는 확인했다. 눈으로 종류가 구분되는지
- [ ] **I-26 연출 수치** — 값은 검증했지만 전투 화면에서 본 적이 없다
      - 걷기 바운스 `EnemyVisual.bounceSpeed` 9 (이동속도 비례) 가 너무 빠르거나 느리지 않은지
      - 좌우 `flipX` 가 방향 전환 경계에서 떨지 않는지 (`flipDeadzone` 0.15)
      - 피격 플래시가 보이는지 (`flashDuration` 0.12s)
      - 외곽선 굵기 → **바로 아래 전용 항목**으로 뺐다
- [ ] 🟡 **Demolitionist 가 걸을 때 위아래로 들썩이는가** *(C28 실측)* — 걷기 시트의 발끝이
      프레임마다 **9px**(=1유닛의 3.5 %) 움직인다. 기존 7장은 **2~4px** 이라 2배 넘는다
      - 무거운 폭발광이라 **의도로 읽힐 수도** 있어 반려하지 않았다. 붕 떠 보이면 그 시트만 다시 뽑는다
      - Summoner 의 중심 흔들림(19px, 기존 10)은 **지팡이 각도** 때문이고 몸통은 안 흔들린다 — 볼 필요 없다
- [ ] **적 콜라이더 0.34×0.36** *(I-31)* 로 줄인 뒤 접촉 피해가 너무 안 들어오지 않는지

#### ✅ 엘리트 / 보스 외곽선 굵기 — **판정 끝. 확정값이다** (B1 뒤처리 · C21 → D17)

**고치는 곳:** `Assets/Scripts/Enemy/EnemyBase.cs:141` (`ApplyRankOutline()`) — CSV 가 아니라 **코드**다.

```cs
_mpb.SetFloat(OutlineWidthId, IsBoss ? 9f : IsElite ? 10.5f : 0f);
```

판정 문장은 이거였다 — **"난전 한복판에서 엘리트를 0.2초 안에 골라낼 수 있는가."**
✅ **2026-08-31 사용자가 직접 플레이하고 "외곽선 적절해" — 닫혔다.**
D17 자동 플레이 68.8초에서는 엘리트가 한 번도 안 나와 못 쳤던 항목이다.

🔴 **이제 이 값을 되돌리지 않는다.** 아래는 **왜 10.5 인가의 기록**이지 조절 사다리가 아니다.
`14`/`12`(B1 직후 값)나 `7`/`6`(낱장 복원값)으로 가는 길은 **닫혔다** —
`7` 은 Wolf 1.36px 로 점선이 되고, `14` 는 슬라임이 보라 덩어리가 된다. 둘 다 아래에서 계산으로 탈락했고
이제 눈으로도 확인됐다. 다시 만지고 싶어지면 **먼저 아래 자 두 개를 다시 대 볼 것.**

##### 왜 14 도 7 도 아닌가 — 자를 두 개 대야 한다

`ortho 6` · 1080p 기준 **1 유닛 = 90 화면 px**. 굵기 = `W ÷ 512 × SizeScale × 등급배율`.

| | 엘리트 `10.5` | | 보스 `9` | |
|---|---:|---:|---:|---:|
| 적 (`SizeScale`) | 화면 px | 키 대비 | 화면 px | 키 대비 |
| Wolf (0.85) | **2.04** | 5.9 % | 2.69 | 5.0 % |
| Slime (0.90) | 2.16 | **9.2 %** | 2.85 | 7.9 % |
| Goblin (1.00) | 2.40 | 8.0 % | 3.16 | 6.8 % |
| Zombie (1.05) | 2.52 | 5.3 % | 3.32 | 4.5 % |
| Demon (1.25) | 3.00 | 5.0 % | 3.96 | 4.3 % |
| Ogre (1.60) | 3.84 | 5.2 % | 5.06 | 4.4 % |

- **아래 하한 = 2 화면 px.** 시트가 `filterMode 0`(Point)이라 그 밑은 선이 **끊겨 점선처럼** 보인다.
  `7` 로 내리면 Wolf **1.36px** · Slime **1.44px** — 가장 흔한 잡몹 넷이 하한 밑이다.
- **위 상한 ≈ 키의 9 %.** 슬라임은 화면에서 **23px** 밖에 안 된다(엘리트 기준).
  `14` 면 외곽선이 **12.3 %**, 양쪽 합쳐 보이는 덩어리의 20 % 가 테두리 색이 된다.

##### 종별 보정은 **하지 않는다** — 두 자가 정반대로 간다

Goblin 을 10.5 로 두고 나머지를 맞추면:

| 적 | 화면 px 를 균일하게 | 키 대비를 균일하게 |
|---|---:|---:|
| Wolf | 12.4 | 14.2 |
| Slime | 11.7 | 9.1 |
| Zombie | 10.0 | 15.8 |
| **Ogre** | **6.6** | **16.1** |

**오우거 하나에서 6.6 과 16.1 로 갈린다.** 한쪽을 맞추면 다른 쪽이 그만큼 더 틀어지므로
**보정할수록 나빠진다.** 한 숫자로 두면 두 오차를 반씩 나눠 갖는다
(화면 px 2.0~3.8 · 키 대비 5.0~9.2 %). 그게 지금 할 수 있는 최선이다.

> 🟡 **실플레이로 뒤집히면 그때 종별로 간다.** 다만 그때도 기준을 먼저 정할 것 —
> "슬라임이 굵다"면 **키 대비** 자를, "오우거가 안 보인다"면 **화면 px** 자를 쓰는 것이다.

### C. 오디오 — **귀로만 판정된다**

고치는 곳은 전부 `AudioLibrary.asset`(클립별 `Volume`/`PitchJitter`) 또는
`AudioManager` 의 `BgmVolume`(0.8) / `SfxVolume`(1.0).

- [ ] 🔴 **`EnemyHit`(0.28) 이 묻히거나 시끄럽지 않은가** — 초당 수십 번 울린다.
      **이 값 하나가 게임 전체의 인상을 좌우한다**
- [ ] **`XpPickup`(0.22) 을 자석으로 먹었을 때** — 중복 컷이 20개를 1개로 줄이는 건 확인했지만,
      0.04초 간격으로 연달아 울리면 여전히 기관총처럼 들릴 수 있다
- [ ] **`PlayerHit`(0.70) 이 알아챌 만큼 튀는가** — 피격은 놓치면 안 되는 정보다
- [ ] **BGM 이 SFX 를 덮지 않는가** — 덮으면 그때가 **`AudioMixer` 도입 시점**이다
      (덕킹이 필요해졌다는 뜻. 지금 없는 이유는 [`TODO.md`](TODO.md) §3)
- [ ] **곡 4개가 화면 성격과 맞는가** — 특히 `BGM_Shop` 은 스테이지맵·상점·이벤트·승리를 **겸한다**
- [ ] **웨이브 → 레벨업 → 웨이브에서 곡이 안 끊기는가** (의도된 동작. 끊기면 버그 → TODO 로)

#### 신규 6종 — **배선 끝났다. 지금 들어 볼 것** *(C3 설계 → D8 배선, 2026-08-30)*

🔴 **이 6개는 아직 아무도 들어 보지 않았다.** 아래 숫자는 C3 이 규칙만 보고 정한 값을
D8 이 **그대로** 넣은 것이다. 근거가 "규칙" 뿐이라 실제로 맞는지는 전혀 모른다.

| 키 | 클립 | 길이 | Volume | PitchJitter |
|---|---|---:|---:|---:|
| `BuildingFire` | `SFX_BuildingFire.wav` | 0.52s | 0.22 | 0.14 |
| `EnemyShoot` | `SFX_EnemyShoot.wav` | 0.63s | 0.30 | 0.12 |
| `UiCancel` | `SFX_UiCancel.wav` | 0.52s | 0.45 | 0.03 |
| `Heal` | `SFX_Heal.wav` | 0.91s | 0.50 | 0.05 |
| `EnemyDieElite` | `SFX_EnemyDieElite.wav` | 1.23s | 0.75 | 0.04 |
| `BossAppear` | `SFX_BossAppear.wav` | 2.51s | 0.95 | 0.00 |

시작값의 근거는 **"자주 울릴수록 작고 피치를 더 흔든다"** 는 기존 표의 규칙이다.
그래서 `BuildingFire`(초당 여러 번)가 가장 작고 지터가 가장 크며,
`BossAppear`(한 웨이브에 한 번)는 가장 크고 지터가 **0** 이다 — 예고음이 매번 다른 음정이면
"이 소리 = 보스"라는 학습이 안 된다.

> 🔴 **먼저 이것부터: 이 머신은 지금 게임이 무음이다.**
> 저장된 설정이 `bgm_vol=0.00` · `sfx_vol=0.00` 이다 (버그 아님 — 저장된 사용자 설정).
> `AudioManager.PlaySfx()` 가 `SfxVolume <= 0f` 이면 **즉시 return** 하므로 아무 소리도 안 난다.
> **옵션에서 볼륨을 올리지 않으면 아래 항목을 하나도 판정할 수 없다.**

> ⚠️ **고치는 곳은 `AudioLibrary.asset` 이고 그건 DEV 소유다.**
> 들어 보고 숫자를 정했으면 [`Parallel/REQ/DEV.md`](Parallel/REQ/DEV.md) 로 요청한다.
> CONTENT 가 직접 못 고친다.

- [ ] 🔴 **`BuildingFire`(0.22) 가 플레이어 발사음을 잡아먹지 않는가** — **터렛 3채 이상**
      세우고 판정한다. 터렛은 최대 7채가 쉬지 않고 쏘므로 **이 게임에서 가장 반복적인 소리**가 된다.
      내 무기가 나갔는지 모르겠으면 0.15 로 먼저 내린다
      (⚠️ 중복 컷이 0.04초라 같은 키를 쓰면 서로를 지운다. 그래서 `WeaponFire` 와 분리했다)
- [ ] 🔴 **`EnemyDieElite`(0.75) 가 `EnemyDie`(0.40) 와 확실히 구분되는가** —
      **난전 중에** 구분돼야 의미가 있다. 조용할 때 구분되는 건 판정이 아니다.
      **Elite/Boss 노드로 가야 들을 수 있다**
- [ ] **`BossAppear`(0.95) 가 과한가** — 게임에서 가장 큰 순간이라 `PlayerDie`(0.90) 위에 뒀다.
      깜짝 놀랄 정도면 0.8 로
- [ ] **`EnemyShoot`(0.30) 이 난전에서 들리는가** — 안 들리면 **적탄을 피할 단서가 없다.**
      §2-B 의 "적탄이 보이는가"와 **같이** 판정할 것 (눈과 귀 중 하나는 알려 줘야 한다)
- [ ] **`UiCancel`(0.45) 이 `UiSelect`(0.55) 와 구분되면서 더 작게 들리는가** —
      취소가 확정보다 크면 안 된다
- [ ] **`Heal`(0.50) 이 회복된 걸 알려 주는가** — 식당은 지금 소리 없이 일한다

**아직 못 듣는 1종** — `Crit`(0.45) 은 위 6종과 달리 **배선되지 않았다.**

- [ ] **`Crit`(0.45) 이 `EnemyHit`(0.28) 를 뚫고 올라오는가** — 못 뚫으면 정보가 아니다
      (⚠️ **배관이 먼저다.** `CalculateDamage` 가 치명타 결과를 버려서 울릴 자리가 없다)

#### C-2. 무기 전용 SFX 4종 — **4종 전부 배선 끝** *(C17·C18 → D14 / C20 → D16)*

D12 가 부탁한 근접 SFX(검)와, 그때 같이 발견한 장판 SFX다.
**빌려 쓰던 소리는 이제 안 난다** — D14 프로브가 옛 소리를 **대조군으로 같이** 셌다:
`Swing=7 Toxin=1 | 대조 Fire=0 Cast=0`. 🔑 `Fire=0 Cast=0` 이 핵심이다 —
"새 소리가 난다"만으로는 **옛 소리가 같이 나는지**를 알 수 없다.

| 키 | 클립 | 길이 | Volume | Jitter | **실효** | 호출부 | 상태 |
|---|---|---:|---:|---:|---:|---|---|
| `WeaponSwing`(4) | `SFX_WeaponSwing.wav` | 0.280s | **0.30 유지** | 0.12 → 🔴 **0.14** | 0.103 | `MeleeWeapon.cs:52` | 배선됨 |
| `ToxinSpill`(5) | `SFX_ToxinSpill.wav` | 0.700s | 0.38 → 🔴 **0.32** | 0.10 | 0.145 → **0.123** | `FieldWeapon.cs:32` | 배선됨 |
| `TentacleLash`(6) | `SFX_TentacleLash.wav` | 0.280s | **0.30** | **0.13** | **0.097** | `SummonWeapon.cs:174` | 배선됨 (D16) |
| `DragonSpit`(7) | `SFX_DragonSpit.wav` | 0.220s | **0.32** | **0.14** | **0.094** | `SummonWeapon.cs:159` | 배선됨 (D16) |

> 🔴 **여기 적혀 있던 계산이 틀렸다 (C20 에서 바로잡음).**
> 전에는 `실제 출력 = Volume × sfx_vol(0.31)` 로 쟀다. 두 군데가 틀렸다 —
> ① `AudioManager.SfxVolume` 의 기본값은 **1.0** 이다 (`PlayerPrefs "sfx_vol"` 기본도 1.0).
> ② 더 중요한 건, **클립마다 피크가 다른데 그걸 통째로 빼먹었다.**
> `EnemyHit` 은 피크 **0.962**, `WeaponCast` 는 **0.233** — **4배** 차이다.
> 같은 `Volume` 이라도 실제로 들리는 크기가 4배 벌어진다는 뜻이라 **사다리 순서 자체가 바뀐다.**
>
> **실효 = `Volume` × 클립 모노 피크.** 프로젝트가 `forceToMono: 1` 이라 모노 피크가 실제 값이다.
> 이 자로 다시 세운 사다리 (`Tools/Audio/compare_sfx.py` 가 찍어 준다):

| | Volume | 클립 피크 | **실효** |
|---|---:|---:|---:|
| `Explosion`(3) | 0.55 | 1.000 | **0.550** |
| `EnemyHit`(10) | 0.28 | **0.962** | **0.269** |
| `WeaponFire`(1) | 0.35 | 0.472 | 0.165 |
| `ToxinSpill`(5) | 0.32 | 0.383 | 0.123 |
| `WeaponCast`(2) | 0.45 | **0.233** | 0.105 |
| `WeaponSwing`(4) | 0.30 | 0.344 | 0.103 |
| `TentacleLash`(6) | 0.30 | 0.324 | 0.097 |
| `DragonSpit`(7) | 0.32 | 0.295 | 0.094 |

> 🔑 `WeaponCast` 는 `Volume` 이 0.45 로 **사다리 2위**인데 실효는 **밑에서 4번째**다.
> 클립이 그만큼 작게 구워져 있기 때문이다. **`Volume` 만 보고 판단하지 말 것.**

##### 🔴 검을 올리자던 판단을 **철회한다** — 근거 두 개가 다 틀렸다

C19(요청-13 ③)에서 `WeaponSwing` 을 0.30 → 0.35 로 올려 달라고 했다.
**DEV 가 아직 적용하지 않았고, 적용하지 말 것.** 근거로 댔던 두 가지가 코드와 맞지 않는다.

**틀린 근거 ① — "서로 다른 적의 타격음은 중복 컷에 안 걸린다"**

```csharp
// AudioManager.cs:162  — 키가 "적"이 아니라 "클립"이다
if (_lastPlayedAt.TryGetValue(clip, out float last) && now - last < DedupeWindow) return;
```

적이 몇이든 **`SFX_EnemyHit` 은 한 클립**이다. `Strike()` 는 부채꼴 안의 적을 **같은 프레임에**
전부 때리므로 전부 0.04초 창 안에 들어온다 → **타격음은 1회로 합쳐진다.**
"휘두름 1회에 타격음이 여러 번 겹친다"는 **일어나지 않는 일**이었다.

**틀린 근거 ② — 실효값을 잘못 쟀다.** 위 표 참조. `EnemyHit` 은 0.087 이 아니라 **0.269** 다.
0.35 로 올려도 실효 0.120 이라 **여전히 타격음의 절반도 안 된다.** 올려서 해결될 문제가 아니었다.

**대신 진짜 문제가 하나 나왔다 — 검은 Lv5 에서 소리가 끊기지 않는다.**

| | 값 | 출처 |
|---|---|---|
| 연타 간격 | **0.249s** | `hitDelay 0.069` + `comboInterval 0.18` (D14 실측과 일치) |
| 🔴 연타 간격은 **공속과 무관** | 고정값 | `MeleeWeapon.cs:58` 이 상수를 그대로 기다린다 |
| Lv5 쿨 | 0.7s | `Weapons.csv` |
| `AttackSpeed` Lv5 | **×0.70** | `WeaponBase.cs:54` `Cooldown × Final.AttackSpeed` |
| 실제 쿨 | **0.49s** | |
| 3연타가 걸리는 시간 | **0.498s** | 0.249 × 2 |

**쿨(0.49s)이 연타가 끝나는 시간(0.498s)보다 짧다.** 즉 검 Lv5 + 공속 Lv5 는
**한 연타가 끝나기도 전에 다음 연타가 시작된다.** 0.249초마다 0.280초짜리 클립이 나가므로
점유율 **113%** — 침묵이 아예 없다.

⇒ §C 규칙("자주 울릴수록 작게")대로면 검은 **올릴 게 아니라 사다리 맨 아래**가 맞다.
**0.30 을 유지하고, 대신 `PitchJitter` 를 0.12 → 0.14 로 올린다.**
끊기지 않는 소리가 같은 음정이면 **드론(웅- 하는 지속음)처럼 들리기** 때문이다.

**② 장판 0.38 → 0.32 (내린다) — 이건 유효하다. 오히려 근거가 세졌다.**

점유율을 다시 쟀다. 🔴 **전에는 `AttackSpeed` 패시브를 빼먹고 계산했다.**

| | 클립 | 최소 쿨 (Lv5 × 공속 Lv5) | **점유율** |
|---|---:|---:|---:|
| 검(`WeaponSwing`) | 0.280s | **0.249s** (연타 간격) | 🔴 **113%** |
| 독 장판 | 0.700s | 1.8 × 0.7 = **1.26s** | **56%** (전엔 39% 로 적었다) |
| 촉수 | 0.280s | 0.8 × 0.7 = **0.56s** | **50%** |
| 드래곤 | 0.220s | 0.8 × 0.7 = **0.56s** | **39%** |
| 총·활 | ~0.15s | 0.22 × 0.7 = 0.15s | ~100% |

0.32 는 실효 **0.123** 으로 `WeaponFire`(0.165) 아래·`WeaponCast`(0.105) 위에 앉는다.

> 🟡 **여전히 예측이지 판정이 아니다.** 점유율은 사실이지만 **얼마나 거슬리는지는 귀로만 안다.**
> D14 판정 ②가 못 한 것도 이것이다 — 그 판은 짧아 장판이 **1회**만 울렸다.
> 30회 = **90초 연속 플레이**가 필요하다.

시작값의 근거는 §C 의 규칙 그대로 — **자주 울릴수록 작고 피치를 더 흔든다.**
기존 사다리에 끼워 넣으면 이렇다:

`BuildingFire` 0.22/0.14 · `EnemyHit` 0.28/0.12 · **`WeaponSwing` 0.30/0.12** ·
`WeaponFire` 0.35/0.10 · **`ToxinSpill` 0.38/0.10** · `WeaponCast` 0.45/0.08

- **검**이 `WeaponFire` 아래인 이유 — Lv5 에서 0.70초 쿨에 **3연타**라 실제로는 3배 자주 울린다.
  지터 0.12 도 같은 이유다. 3연타가 **같은 음정으로 세 번 나면 기계음처럼 들린다**
- **장판**이 `WeaponCast`(빌려 쓰던 자리) 바로 아래인 이유 — 쿨 3.0→1.8초로 **한 판 내내 깔린다.**
  한 번 들어서 괜찮아도 30번 들으면 거슬릴 수 있어 한 칸 낮췄다

**만든 방식** — `Tools/Audio/gen_sword_swish.py` · `gen_toxin_splash.py`.
`Tools/Art/` 의 그림과 같이 **절차 생성이라 파라미터를 고쳐 다시 구울 수 있다.**

| 설계 | 검 (`WeaponSwing`) | 독 장판 (`ToxinSpill`) |
|---|---|---|
| 밴드패스 스윕 | 700 → **5200** → 1500 Hz (**올라간다**) | 900 → **260** Hz (**내려간다**) |
| 왜 방향이 반대인가 | 칼이 귀 앞을 **지나간다** | 액체가 **주저앉는다**. 올라가면 튀는 소리다 |
| 어택 | 25% 구간 smoothstep | **8ms** |
| 왜 어택이 반대인가 | 🔴 **딱 하는 시작음이 곧 총소리다.** 일부러 없앴다 | 🔴 **철퍽은 순간이어야 한다.** 없으면 김 빠지는 소리다 |
| 층 구성 | 1층 (스윕 노이즈) | **3층** — 철퍽 + 치익(불규칙 진폭 변조) + **거품 7개** |
| 하이패스 | 300 Hz (3연타 저음 뭉침 방지) | 120 Hz |
| 피크 / RMS | 0.400 / 0.053 | 0.420 / 0.054 |

🔑 **거품 층이 핵심이다.** 고역 노이즈만 있으면 "치이익" 하는 **스프레이**로 들린다.
꼬리에 짧은 하강 사인 7개를 흩뿌려야 **액체**가 된다. 빼고 싶으면 `N_BUBBLE` 을 0 으로 두고 비교해 볼 것.

**미리 잰 것** (귀로 판정하기 전 참고용. 두 스크립트가 각각 출력한다)

| 잰 것 | 검 | 독 장판 |
|---|---|---|
| 겹침 | ✅ **D14 실측으로 확인.** 연타 간격 **0.249s·0.254s**(설계값 0.247 과 일치) > 클립 0.280s → 겹침 **0.031s**, 그 구간은 이미 페이드아웃. 3연타 합성 피크 0.400 = 1타와 동일 | ✅ **겹치지 않는다.** Lv5 쿨 **1.8s** > 클립 0.701s. 4장 연속 재생 피크도 0.420 그대로 |
| 0~200Hz | **0.0%** (총소리 저역 없음) | **1%** |
| 200~800Hz | **1%** | **11%** ← 🔑 **두 소리를 가르는 대역이다** (11배) |
| 시작 트랜지언트 | 첫 10ms 피크 0.006 (전체의 1/67) — **클릭 없음** | 앞 50ms 가 **전체 피크** — 철퍽이 가장 큰 순간이다 |
| 모노 합산 피크 | 0.344 | 0.383 — 좌우 상쇄 없음 |

> 🔑 **둘이 서로 안 헷갈리게 만든 게 설계 목표다.** 고역(3~9kHz)은 두 소리가 비슷할 수밖에 없는데
> (둘 다 노이즈 기반), **길이(0.28 vs 0.70)** · **스윕 방향(↑ vs ↓)** · **200~800Hz 대역(1% vs 11%)**
> 세 가지로 갈라 뒀다. 한쪽 파라미터를 고칠 때 **다른 쪽과 가까워지지 않는지** 같이 볼 것.

- [ ] **검이 총소리로 안 들리는가** — 이게 1번 판정 기준이다
- [ ] **장판이 마법 시전음으로 안 들리는가** — 같은 이유로 1번이다
- [ ] 🔴 **둘을 같이 들었을 때 구분되는가** — 검사+독 조합으로 한 판. 안 갈리면
      `gen_toxin_splash.py` 의 `SPLAT_GAIN`(2.10)을 올려 저중역을 더 벌린다
- [ ] **3연타가 시끄럽지 않은가** — 🔴 **Lv5 Sword 로 판정할 것.** Lv1 은 1연타라 문제가 안 보인다
- [ ] **장판이 한 판 내내 거슬리지 않는가** — 🔴 **판정은 30번쯤 들은 뒤에 한다.**
      한 번 듣고 괜찮은 건 판정이 아니다. 거슬리면 `Volume` 을 0.30 으로 먼저 내린다
- [ ] 🔴 **`EnemyHit`(실효 0.269) 에 묻히지 않는가** — 벤 소리(0.103)가 맞은 소리의 **2.6분의 1**이다.
      묻힌다면 **검을 올릴 게 아니라 `EnemyHit` 을 내리는 쪽이 맞다** — 그 클립은 피크가 0.962 로
      혼자 유난히 뜨겁게 구워져 있다. `Volume` 0.28 → 0.22 면 실효 0.212 가 된다.
      ⚠️ 다만 `EnemyHit` 은 **모든 무기가 같이 쓰는 소리**라 하나 내리면 전부 바뀐다. 검만 보고 정하지 말 것
- [ ] 🔴 **검 0.280초가 짧은가** — "탁"으로 들리면 늘리고 싶어지는데 **혼자 못 늘린다.**

  | | 값 |
  |---|---|
  | 지금 클립 | **0.280s** |
  | 연타 간격 (D14 실측) | **0.249s · 0.254s** |
  | **클립 길이 상한** | 🔴 **0.247s** — 이걸 넘는 만큼이 곧 겹침이다 |
  | 지금 겹침 | 0.031s (페이드아웃 구간이라 무해) |
  | `DUR` 0.34 로 올리면 | 겹침 **0.093s** — 페이드아웃을 넘어 **본체끼리 겹친다** |

  → **늘려야겠다면 클립만 바꾸지 말고 DEV 에게 알린다.** `MeleeWeapon.comboInterval`(0.18)을
  같이 올려야 하는데 그건 **연타의 손맛을 바꾸는 일**이라 CONTENT 혼자 정할 게 아니다.
  🔑 **줄이는 쪽(0.28 → 0.24 이하)은 겹침이 사라지므로 자유다.**
- [ ] **장판 0.701초가 긴가** — 꼬리가 늘어지면 `gen_toxin_splash.py` 의 `DUR` 을 0.55 로 내린다.
      점유율도 39% → 31% 로 같이 떨어진다
      (⚠️ 0.30 아래로 내리면 거품 7개가 들어갈 자리가 없어 **액체 느낌이 사라진다**)

> ℹ️ **스테레오로 구웠지만 프로젝트가 모노로 접는다** (`forceToMono: 1`, 기존 SFX 전부 동일).
> 미리 재 둔 **모노 합산 피크 0.344 / 0.383** 이 실제로 들리는 값이다. 좌우 상쇄 없음 — 맞았다.
> 앞으로 만들 SFX 도 **모노 합산으로 검산**해야 한다. 스테레오 피크만 보면 실제보다 크게 잡는다.

##### ✅ 소환수 2종 — **배선까지 끝났다** *(C20 → D16, 2026-08-30)*

> ✅ **D16 프로브가 옛 소리 대조까지 확인했다** — `WeaponFire=0` / `WeaponCast=0`.
> 클립 규격도 실측했다: 둘 다 `ch=1 44100 ADPCM forceToMono=1`, 길이 **0.280s** · **0.221s**
> (구운 값 0.280/0.220 과 일치). 발사 간격도 쿨다운 그대로 **1.2s** · **1.4s** 였다.
> 🔑 **이제 아래 체크박스를 판정해도 된다.** 배선 전에 판정했으면 총소리·시전음을 듣고
> "내가 구운 클립이 이상하다"고 결론냈을 것이다.

`Tools/Audio/gen_tentacle_lash.py` · `gen_dragon_spit.py`.
D14 가 남긴 조건 4개를 전부 자로 재서 맞췄다.

| D14 조건 | 어떻게 맞췄나 | 잰 값 |
|---|---|---|
| 점유율을 보고 볼륨을 정할 것 | 길이를 먼저 묶었다 — 촉수 0.28s · 드래곤 0.22s | 50% · 39% |
| 촉수 피크를 `lashHitDelay 0.1` 에 | 타격층을 `SLAP_LEAD 6.5ms` 만큼 **당겨서** 시작 | **오차 +0.3ms** |
| 화염구는 `Explosion` 저역을 비울 것 | `HPF 380Hz` | **0~200Hz 0.00%** |
| 촉수는 검과도 갈릴 것 | 아래 표 | **거리 1.5 초과** (근처도 아님) |

**🔴 촉수의 피크 위치는 취향이 아니라 코드가 정한다.**

```csharp
SpawnLash(range);                              // t = 0
AudioManager.Play(SfxId.WeaponFire);           // t = 0     <- 소리가 시작하는 곳
yield return new WaitForSeconds(lashHitDelay); // 0.1s
OverlapCircleAll(...)                          // t = 0.1   <- 피해가 들어가는 곳
```

소리는 후리기 **시작**에 재생되는데 맞는 건 **0.1초 뒤**다. 그래서 클립이
"때리는 소리"가 아니라 **"휘두르다 0.1초에 때리는 소리"** 여야 한다.
⇒ 접근음(0→0.1s) + 타격(0.1s) + 물기 꼬리. 그림의 **프레임 3**(30fps)과도 같은 순간이다.
⚠️ **DEV 가 `lashHitDelay` 를 바꾸면 `HIT_AT` 도 바꿔 다시 구워야 한다.**

**서로 갈리는지 — 자로 쟀다** (`python Tools/Audio/compare_sfx.py`)

| 클립 | 길이 | 어택 | 0–200 | 200–800 | 800–3k | 3k–9k |
|---|---:|---:|---:|---:|---:|---:|
| `WeaponSwing`(검) | 0.280s | 63ms | 0% | 0% | 11% | **83%** |
| `ToxinSpill`(장판) | 0.700s | 9ms | 0% | **34%** | 32% | 28% |
| `TentacleLash`(촉수) | 0.280s | **100ms** | 0% | 7% | **69%** | 22% |
| `DragonSpit`(드래곤) | 0.220s | 12ms | **0%** | 3% | **52%** | 40% |

- **촉수 vs 검** — 길이가 같아서 제일 걱정했는데 **어택이 갈랐다.** 63ms vs 100ms 이고
  무엇보다 검은 3k–9k 가 83%(마른 쇳소리), 촉수는 800–3k 가 69%(젖은 몸통소리)다
- **촉수 vs 장판** — 둘 다 "젖었다". 길이 0.28 vs 0.70 · 거품층 없음 ·
  장판은 깔리고 **남고** 촉수는 때리고 **끝난다**
- 🔴 **드래곤 vs `WeaponCast`** — 처음 구웠을 땐 거리 **1.10** 으로 **가장 가까운 짝**이었다.
  하필 지금 빌려 쓰는 그 소리다. 숨 스윕을 2800→2200Hz, 펄럭임을 1800–7000→1200–4800Hz 로
  내려 3k–9k 에서 800–3k 로 무게를 옮겼다 → **1.33**

> ℹ️ `compare_sfx.py` 가 내는 거리는 **참고 지표지 판정이 아니다.**
> 멀어도 들으면 비슷할 수 있다. **"가깝다"만 믿을 값이다.**
> 지금 가장 가까운 짝은 내가 만든 게 아니라 **`ToxinSpill` vs `EnemyHit`(0.98)** 이다 — 따로 들어 볼 것.

**소리의 구조**

| | 촉수 (`TentacleLash`) | 드래곤 (`DragonSpit`) |
|---|---|---|
| 층 | 접근(0→0.1s) + **젖은 타격** + 물기 꼬리 | 숨 + **펄럭임** + 크래클 3발 |
| 스윕 | 타격 후 1700 → 430Hz (급락) | 2200 → 780Hz (하강) |
| 🔑 핵심 층 | **물기** — 38Hz 불규칙 진폭 변조. 빼면 그냥 마른 채찍 | **펄럭임** — 85Hz 불규칙 변조. 빼면 에어브러시 |
| 접근음 크기 | 타격의 **0.39배** (크면 검처럼, 작으면 안 들림) | — |

- [ ] **촉수가 검처럼 안 들리는가** — 이게 1번 판정이다. 같은 "휘두름"이라 제일 위험한 짝
- [ ] 🔴 **촉수 소리의 피크가 적이 맞는 순간과 맞는가** — 자로는 +0.3ms 다.
      어긋나 들리면 `lashHitDelay` 가 바뀐 것이니 `gen_tentacle_lash.py` 의 `HIT_AT` 을 맞추고 다시 굽는다
- [ ] **드래곤 뱉는 소리가 자기 폭발을 안 가리는가** — 저역 0% 로 비워 뒀지만 확인할 것
- [ ] 🔴 **드래곤이 `WeaponCast`(파이어볼 시전)와 갈리는가** — **마법사+소환사 조합으로 한 판.**
      안 갈리면 `gen_dragon_spit.py` 의 `BR_F0` 를 더 내린다(2200 → 1800)
- [ ] **소환수 2종을 같이 들었을 때 시끄럽지 않은가** — 둘 다 들면 점유율 합 **89%** 다.
      거슬리면 `Volume` 을 0.26 / 0.28 로 같이 내린다
- [x] **문어가 사거리 밖이라 안 후를 때 조용한가** — ✅ **D16 에서 확인.** 발사 간격에
      1.2s 사이사이 **2.4s 공백**이 몇 번 잡혔다 = 적이 없으면 조용히 리턴한다(허공 안 후림)
- [ ] 🔴 **촉수가 "후려치는" 소리로 들리나** *(D16 → C22)* — 배선 전엔 **총소리**(`WeaponFire`)를
      빌려 썼다. 이제 자기 소리가 나는데, **"총소리가 아니다"와 "후려치는 소리다"는 다른 판정**이다.
      아니면 `gen_tentacle_lash.py` 를 다시 굽는다
- [ ] 🔴 **타격 싱크 — 소리는 t=0, 피해는 t=+0.1s** *(D16 → C22)* — 클립은 그 지점에 피크가 오게
      구웠고 자로는 오차 **+0.3ms** 다. 어긋나 **들리면** `gen_tentacle_lash.py` 의 `HIT_AT` 과
      `SummonWeapon.lashHitDelay` 를 **반드시 같이** 바꾼다 (한쪽만 바꾸면 더 어긋난다)
- [ ] 🔴 **마법사의 `WeaponCast` 는 아직 한 번도 안 들어 봤다** *(D16 → C22)* — D16 판은 Warrior 라
      낼 수가 없었다. 드래곤이 빌려 쓰던 소리가 **원래 주인에게서는 멀쩡한지** 확인해야 한다.
      **Mage 로 한 판.** 위 "드래곤이 `WeaponCast` 와 갈리는가"와 **같은 판에서 같이 친다**

##### ✅ 픽업 3종 — **배선 끝** *(C23 → D20, 2026-08-31)*

`Tools/Audio/gen_bomb_pickup.py` · `gen_buff_pickup.py` · `gen_gold_pickup.py`.
`SfxId` **25·26·27** 로 `AudioLibrary.asset` 에 들어갔다 — **아래 Volume·Jitter 를 그대로** 썼다.
남은 건 **귀로 듣는 것뿐**이다.

> ℹ️ **힐 픽업은 새로 만들지 않았다.** `SfxId.Heal = 24` 가 이미 있고 배선돼 있다(Volume 0.50).
> 요청-12 가 "이미 나는 소리가 있으면 그대로 쓴다"고 했으므로 그대로 쓴다.

| 키 | 클립 | 길이 | 모노 피크 | **제안 Volume** | Jitter | **실효** |
|---|---|---:|---:|---:|---:|---:|
| `BombPickup` (25) | `SFX_BombPickup.wav` | 0.850s | 0.649 | **0.90** | 0.03 | **0.584** |
| `BuffPickup` (26) | `SFX_BuffPickup.wav` | 0.500s | 0.392 | **0.75** | 0.06 | **0.294** |
| `GoldPickup` (27) | `SFX_GoldPickup.wav` | 0.200s | 0.329 | **0.65** | 0.10 | **0.214** |

사다리에서의 자리 — `XpPickup` 0.091 · `Heal` 0.227 · `EnemyHit` 0.269 ·
**`GoldPickup` 0.214** · **`BuffPickup` 0.294** · `Explosion` 0.550 ·
**`BombPickup` 0.584** · `Magnet` 0.700 · `LevelUp` 0.850.

- **폭탄이 `Explosion` 보다 살짝 위**인 건 의도다. 런에 4번뿐인 화면 청소다
- **골드가 `XpPickup` 의 2.3배**인 것도 의도다. 경험치는 계속 먹고 돈은 런에 13번이다
- ⚠️ **Jitter 를 종류마다 다르게 줬다.** 골드는 13번 나므로 0.10 으로 흔들어야 기계음이 안 된다.
  폭탄은 반대로 **매번 똑같아야** 사건으로 느껴진다 → 0.03

**서로 갈리는지 — 자로 쟀다** (`python Tools/Audio/compare_sfx.py`)

| 클립 | 길이 | 어택 | 0–200 | 200–800 | 800–3k | 3k–9k |
|---|---:|---:|---:|---:|---:|---:|
| `BombPickup` | 0.850s | **3.4ms** | **65%** | 14% | 10% | 9% |
| `BuffPickup` | 0.500s | **231ms** | 0% | 0% | **60%** | 39% |
| `GoldPickup` | 0.200s | **0.5ms** | 0% | 0% | 7% | **62%** |

🔴 **`compare_sfx.py` 의 대상 목록이 무기 소리만 보고 있었다 — 이걸 먼저 고쳤다.**
픽업 무리(`XpPickup`·`LevelUp`·`ChestOpen`·`Magnet`·`Heal`·`UiSelect`)가 표에 없었고,
게다가 `TentacleLash`/`DragonSpit` 경로가 **`_Incoming/` 인 채 방치**돼 있었다
(D16 이 `Assets/` 로 옮겼는데 목록이 안 따라갔다). 둘 다 "없음"으로 조용히 건너뛰고 있었다.
⇒ **새 소리를 만드는데 부딪힐 상대가 목록에서 사라지는 것**이 이 자의 가장 위험한 고장이다.

경로를 고치자마자 **1.0 아래 짝 두 개가 튀어나왔고, 둘 다 쿨마다 계속 우는 무기였다:**

| 짝 | 처음 | 무엇이 붙었나 | 어떻게 뗐나 | 지금 |
|---|---:|---|---|---:|
| `BuffPickup` vs **`TentacleLash`** | **0.58** | 길이 0.30↔0.28 · 어택 118↔100ms · 800~3k 91↔69% — **세 축이 전부** | 3음 0.30s → **4음 0.50s 로 부풀리기**. 정점을 231ms 로 끌었다 | **1.09** |
| `GoldPickup` vs **`DragonSpit`** | **0.86** | 길이 0.20↔0.22 · 800~3k 38↔52% | 기음을 2300~3200 → **3000~4200Hz** 로 올려 3k~9k 로 이사 | **1.15** |

- ⚠️ 골드는 위로 올린 대가로 **`UiSelect` 와 1.09** 가 됐다. 위아래로 낀 자리다 —
  더 올리면 UI 클릭이, 내리면 드래곤이 붙는다. 지금이 가장 먼 지점이다
- ℹ️ 남은 **1.0 미만 짝 두 개는 내가 만든 게 아니다** — `LevelUp` vs `Magnet` **0.85**,
  `ToxinSpill` vs `EnemyHit` **0.98**. 앞엣것은 둘 다 "길게 올라가는 소리"라 원래 닮았다.
  들어 보고 거슬리면 `Magnet` 쪽을 손대는 게 맞다(레벨업이 더 중요한 사건이다)

- [ ] 🔴 **버프 소리가 문어 촉수와 갈리는가** — **소환사로 문어를 들고 한 판.** 1번 판정이다.
      안 갈리면 `gen_buff_pickup.py` 의 `NOTES` 를 한 옥타브 올린다(C6→C7 줄)
- [ ] 🔴 **골드가 `XpPickup` 과 갈리는가** — 둘은 **1초 안에 연달아** 난다. 요청-12 의 조건이다.
      안 갈리면 `CLINK_AT` 을 (0, 0.045, 0.095)로 벌려 "또르륵"을 더 길게 만든다
- [ ] **골드가 13번 나도 안 질리는가** — 가장 자주 나는 새 소리다.
      거슬리면 **`Volume` 을 0.65 → 0.50** 으로 먼저 내린다(클립을 다시 굽지 말 것)
- [ ] **폭탄이 화면 청소만큼 크게 들리는가** — `Explosion` 과 같은 "폭발"이라 제일 위험한 짝이다.
      대역으로는 못 갈랐고 **어택(3.4ms vs 78ms)과 길이(0.85 vs 0.57)로만** 갈라 뒀다.
      약하면 `Volume` 을 0.90 → 1.00 (실효 0.649, `Magnet` 바로 아래)
- [ ] **폭탄이 노트북 스피커에서도 들리는가** — 저역 65% 라 작은 스피커에서 사라질 수 있다.
      2k~9k 를 11% 남겨 뒀지만 **이건 큰 스피커로는 판정이 안 된다.** 노트북으로 한 번 볼 것
- [ ] **무적과 공속이 같은 소리인 게 헷갈리지 않는가** — 요청-12 가 공용을 허용했다.
      헷갈리면 소리가 아니라 **화면 효과로 가르는 게 맞다**(DEV 쪽 일이다)

### D. 화면 · 가시성

- [ ] **상점 카드 250×440** 이 실제로 읽히는가 — **YAML 수치로만 검증했고 눈으로 안 봤다.**
      Screen Space Overlay 캔버스는 `Unity_Camera_Capture` 에 **안 찍힌다**
      - 3장이 나란히 놓였을 때 화면 밖으로 안 넘치는지 (패널 900×900)
      - 설명 `18pt` 가 읽히는지 · 이름 `38pt` 가 카드 폭을 넘어 잘리지 않는지
- [ ] 🔴 **`[Z] Build` · `[E] PROMOTE` 안내 문구가 난전에서 읽히는가** *(D36)*
      `D36` 이 원인을 잡았다 — **조건부 표시는 원래 되고 있었고 안 보이는 게 문제**였다
      (외곽선 0 · 그림자 0). 전용 머티리얼 `Assets/Fonts/Pretendard SDF - Prompt.mat` 을 만들어 물렸다
      - 지금 값: 외곽선 **0.22** · 그림자 오프셋 **(0.6, −0.6)** · `UnderlayDilate` 0.15 · `Softness` 0.25
      - 여전히 안 읽히면 외곽선을 **0.30** 까지. 반대로 **글자가 뭉개져 보이면 0.15** 로
      - 🔴 **원본 `Pretendard SDF` 머티리얼을 고치지 말 것** — 텍스트 **75개**가 공유한다.
        이 파생본은 **2곳에만** 물려 있다
      - ⚠️ TMP 머티리얼을 복제하면 `ShaderUtilities.UpdateShaderRatios` 를 불러야 한다.
        `D36` 이 그걸 빼먹어 **글자가 통짜 덩어리로 렌더**됐다 (DEV 쪽 함정이지만 증상이 여기 보인다)
- [ ] 🔴 **승급했을 때 겉모습이 바뀌는 게 보이는가** *(I-57)* — 원화 4장 + 걷기 4장을 넣었다.
      승급은 이 게임에서 가장 큰 보상인데 **화면에 변화가 없으면 보상으로 안 느껴진다**
- [ ] 🔴 **승급 연출이 "가장 큰 보상"만큼 큰가** *(C34)* — 금색 충격파 링 3겹 + 별이다
      - 🔑 **비중을 다른 연출과 대 보는 게 판정법이다.** 지금 화면 흔들림은
        보스 등장 `0.55 / 0.7` · 엘리트 처치 `0.20 / 0.25` 다. **승급이 그 사이 어디쯤인가?**
        보스 등장보다 커야 한다고 보면 흔들림을 `0.6 / 0.8` 로 (DEV 요청)
      - 약하면 프리팹의 링 겹 수·최대 스케일·지속을 올린다. 세면 반대로
- [ ] **승급 파동 크기·시간** *(D41 이 감으로 정했다 — 반경 **2.6** / **0.62초**)*
      - 흔들림은 **0.35 / 0.30** 이다. 보스 등장 `0.55/0.7` 과 엘리트 처치 `0.20/0.25` **사이**로 잡았다
      - ⚠️ **흔들림은 정지 화면에서 안 난다** — `CameraController.cs:84` 가 `Time.deltaTime <= 0` 이면
        조기 반환한다. **제단(`E`) 승급처럼 게임이 도는 경로에서만 보인다**
- [ ] **레벨업 파동 크기·시간** *(반경 **1.5** / **0.42초** · 흔들림 **0**)*
      - 🔑 흔들림을 0 으로 둔 건 의도다 — 레벨업은 한 판에 10~12번이라 매번 흔들면
        **승급의 흔들림이 특별하지 않게 된다**
      - ℹ️ **`D43` 이 띄우는 시점을 옮겼다** — 레벨업 패널이 **닫힐 때** 뜬다(A안, 사용자 결정).
        패널이 떠 있는 동안엔 `ScreenSpaceOverlay` 캔버스가 월드 스프라이트를 무조건 덮어서
        **원래 위치에서는 아무도 못 봤다**
- [ ] 🟡 **상점 주인 발밑의 자갈 바닥이 거슬리는가** *(C36 이 남기기로 결정)*
      프롬프트에 "바닥 없이"라고 썼는데 붙어 나왔다. **남긴 이유는 둘이다** —
      ① 직업 초상 10장은 바닥이 없어서 **NPC 가 그 덕에 구분된다**(내가 요청에 건 조건이다)
      ② `D42` 가 상점 패널 위에서 보고 *"좌판 옆에 선 것처럼 읽힌다"* 고 했다
      - 플레이해 보고 **UI 판 위에 흙만 떠 있는 것처럼 보이면** `EditImageWithPrompt` 로 걷어낸다 (DEV 요청)
- [ ] **레벨업 연출(청록)이 승급(금색)과 헷갈리지 않는가** *(C34)* — **같은 그림에 색·크기만 다르다.**
      한 판에 레벨업은 10~12번, 승급은 많아야 1~2번이다. 헷갈리면 **레벨업 쪽을 더 줄인다** —
      자주 나는 쪽이 조용해야 한다
- [ ] **진화 무기 3종 아이콘이 카드에서 원본과 구분되는가** *(I-57)*
- [ ] **상자를 밟았을 때 레벨업인지 상자 보상인지 구분되는가** — 같은 패널을 쓴다.
      구분이 필요하면 **패널 제목만 바꾸면 된다** (새 UI 불필요)
- [ ] **상자·자석이 경험치 오브와 헷갈리지 않는가** (`sortingOrder 1` 로 위에 그린다)
- [ ] 🟡 **깊은 층 바닥이 얼룩덜룩한가** *(C37 재틴트 후 · D53 실측)*
      층별 색 분리는 끝났다 — `dL 0.0069 → 0.0727`(**10.5배**) · 겹침 **0 %**.
      다만 **얼룩(밝기 표준편차)이 층0 0.0134 vs 층9 0.0211 로 층9가 1.6배**다
      - 🔑 **그대로 두기로 했다** — 대안(smoothstep)은 둘을 0.0179 로 맞추는 대신
        **층0 을 0.0138 → 0.0179 로 더럽힌다.** 층0 은 첫인상 구간이라 거기가 제일 깨끗해야 한다.
        그리고 **풀밭은 고르고 돌무더기는 얼룩진 게 맞다** — 얼룩이 있어야 할 자리에 있다
      - 그래도 거슬리면 `Tools/Art/retint_tiles.py` 의 `DEEP` 를 `SHALLOW` 쪽으로 당긴다.
        `dL` 여유가 0.0727 이라 **0.03 은 내줘도 통과선(0.04)을 넘는다**
- [ ] **바닥이 밋밋한가** *(I-28 여파)* — 타일 10종의 휘도를 한 목표색으로 끌어내렸다.
      밋밋하면 평준화를 0.85 로 완화(`lerp(1, 배율, 0.85)`) → **퀼트가 재발하는지 캡처로 확인**.
      ⚠️ `Tile.color` 는 `Color32` 라 **1을 넘는 배율이 잘린다.** 밝히는 방향으로는 조정이 안 된다

### E. 건물

- [ ] **건물 밀기 감각** — `mass 5 / linearDamping 10`. 무거우면 `mass`, 미끄러지면 `linearDamping`
- [ ] 🔴 **건물이 길을 막지 않는가** — `TryFindSpot` 이 플레이어 주위 3링을 채운다.
      좁은 곳에서 5개를 연달아 세우면 **플레이어가 갇힐 수 있다**
- [ ] **1.30 유닛(플레이어 1.00 보다 크다)이 시야를 가리지 않는가** ·
      `placeDistance` 1.8 간격이 답답하지 않은가
- [ ] **건물이 일하고 있다는 게 보이는가** — 마을 XP·농장 골드가 **소리 없이** 들어온다.
      안 보이면 `DamagePopupManager` 로 `+2 XP` / `+1 G` 를 띄운다 (별도 작업)

### F. 빌드 설계 — 상한 · 도달 시간

여기서 나오는 답이 [`TODO.md`](TODO.md) §2 의 **미결정 1건**(T2 개수 · T4 여부)을 결정한다.

- [ ] 🔴 **소지 상한이 "무겁다"인가 "답답하다"인가** *(I-55)* — 상한 자체는 로그로 검증했다.
      판정이 필요한 건 **재미** 쪽이다. 고치는 곳은 `Classes.csv` 의 `Max*Slots`
      - **Warrior/Mage 의 무기 3칸** — "선택이 무겁다"면 성공, "그냥 답답하다"면 4로
      - **Ranger 의 건물 2칸** — 건물은 **설치라는 별도 조작**이 붙어 있어 2칸이면 아예 안 쓰게 될 수 있다
      - 상한이 찬 뒤 **카드가 반복되지 않는지.** 신규가 빠지면 후보 풀이 얇아진다.
        특히 Ranger(패시브 4·건물 2)는 후반에 뽑을 게 거의 없어진다.
        심하면 `OwnedWeight`(2)를 낮추는 게 아니라 **상한을 올리는 쪽**이 맞다
      - **다 찍었는데 카드가 0장**이 되는 상황이 오는지 (패널이 빈 채로 뜬다. 크래시는 안 난다)
- [ ] 🔴 **승급까지 몇 분 걸리는가** *(I-56)* — 4종 모두 `무기 Lv5 + 건물/패시브 Lv3`.
      건물은 레벨업 카드로만 오르는데 **설치까지 해야 제단이 생긴다**
      - ⚠️ Sentinel 은 `Gun` Lv5 가 필요한데 **Gun 은 Warrior 의 시작 무기가 아니다.**
        무기 3칸 중 2칸을 쓰게 되고, 레벨업 카드가 Gun 을 뽑아 줘야 한다
      - ⚠️ Ranger(건물 2칸)로는 Warden(Village 필요)이 **사실상 불가능**할 수 있다
- [ ] **무기 진화 조건이 한 런에서 닿는가** — `Sword Lv5 + Damage Lv3`.
      제단 반경 `EvolutionManager.altarRadius` 2.2 가 좁아 계속 헛누르지 않는지
- [ ] **진화/승급 안내 문구가 난전에서 보이는가** — HUD 하단 `y=140`. 다른 요소와 겹치지 않는지

### G. 성능

> ✅ **이 절의 대부분은 `D27` 이 실제로 쟀다** → [`PERF.md`](PERF.md). 아래는 **측정 뒤에 남은 것**만이다.
> 밀도·`MaxAlive` 는 §I 로 옮겼다.

- [x] ~~프레임 타임을 볼 때 먼저 `vSync` 를 확인한다~~ — ✅ **`D58` 에서 실제로 걸렸다.**
      측정 직전 확인하니 `vSyncCount 1` · 모니터 144 Hz 였다. 껐다가 되돌렸다.
      🔴 **이 항목은 닫지 말고 계속 볼 것** — 품질 레벨마다 따로 저장되므로 레벨이 바뀌면 되살아난다
- [ ] 🔴 **프레임 타임을 볼 때 먼저 `vSync` 를 확인한다** *(D28 이 넘긴 것)*
      `D27` 이 품질 레벨을 `0`(Very Low) → **`5`(Ultra)** 로 올렸고 **사용자 판단으로 유지**한다
      (근거: Standalone 기본값이 5). 🔴 **대가가 있다 — Ultra 는 `vSyncCount: 1` 이다.**
      `vSyncCount` 는 품질 레벨마다 따로 저장되고, 강제로 꺼 주던 `PerfHarness` 는 `D27` 이 지웠다
      - ⇒ **에디터 플레이가 144 Hz 에 고정된다.** 프레임 타임이 **정확히 6.94 ms** 에 붙어 있으면
        성능이 좋아진 게 아니라 **vSync 를 보고 있는 것**이다
      - 실측하려면 `QualitySettings.vSyncCount = 0` 을 **먼저** 확인한다.
        이걸 모르면 "고쳤더니 6.94 ms 로 안정됐다"는 **가짜 성공**을 보고하게 된다
- [ ] 🔴 **에디터에서 잰 프레임을 게임 성능으로 읽지 않는다** *(D35 재측정)*
      `EditorLoop` 가 **프레임의 45 %** 를 먹는다. 빌드에는 없는 비용이다
      - ⇒ 에디터 5 ms 는 빌드 3 ms 일 수도, 에디터가 가려 준 문제가 빌드에서 드러날 수도 있다.
        **"좋아졌다/나빠졌다"는 같은 환경끼리만** 비교한다
      - ℹ️ **위 vSync 경고가 실제로 걸린 사례가 나왔다** — `D35` 가 측정 직전 확인하니
        품질 5 라 `vSync` 가 **정말로 켜져 있었다.** 껐다가 끝나고 되돌렸다
- [ ] 🔴 **무기를 켜면 프레임이 2.5배가 된다 — 원인이 아직 미확정이다** (`PERF.md` §0-1)
      - ⚠️ **`D35` 가 재현을 시도했지만 못 했다.** 적 815마리 시나리오 A 는 중앙값 **5.08 ms** 로 건강한데
        시나리오 B(무기 켜고 대량 처치)가 안 잡혔다. **"이제 괜찮아졌다"는 뜻이 아니라 아직 못 쟀다는 뜻이다**
      - 적만 800마리는 **121 fps** 인데, 무기를 켜면 무너진다. 주 구간은 `BehaviourUpdate` 인데
        **그 안의 71~81 %가 설명되지 않았다**
      - 🔴 **이건 "느낌이 숫자"가 아니라 "동작이 예/아니오"에 가깝다.** 내 소관이 아니다 —
        플레이 중 튀는 순간이 있으면 **무엇을 하고 있었는지**만 적어서 DEV 에 넘긴다
- [x] ~~적이 많을 때 프레임이 흔들리는가~~ — ✅ **쟀다. 적만으로는 안 흔들린다.**
      800마리에서 121 fps, 현재 상한 130 에서는 분리 비용이 프레임의 **1 %** 다
      - ⚠️ 그때 세운 가설 둘은 **측정이 죽였다**: `OverlapCircleAll` 할당은 프레임의 **1.3 %**
        (단가는 비싼데 초당 30회뿐이라 빈도가 낮았다), 드로우콜은 적 1마리당 **0.66개**로
        예상(1개)보다 낮았다. 🔑 **비용 = 단가 × 빈도** — 코드를 읽고 순위를 매기면 단가만 보게 된다

### H. 경제 — 골드가 어디로 가는가 *(C27)*

지갑이 둘로 갈라졌고(`D25`) 가격을 10배로 올렸다(`C27`). **여기부터는 숫자가 아니라 감각이다.**
근거·계산은 전부 [`BALANCE.md`](BALANCE.md) §3-1 에 있다.

> 🟡 **`D58` 이 예측을 붙였다 — `M ≈ 600` 으로 본다.** 아직 사람이 한 판을 안 했다.
> 골드가 `EnemyBase.Die → GrantGold(CurrencyDrop)` 로 **처치마다** 들어오는 것을 확인하고 셌다:
>
> | | |
> |---|---:|
> | 전투 8노드 + 보스 · 층 소환 배율 포함 **이론 최대** | **1,757 G** |
> | 배수구 (상점 1.2회 × 3칸 × 평균가 67G + 리롤) | **241~306 G** |
>
> **절반만 잡아도 수입이 배수구의 3배를 넘는다.** ⇒ 아래 배수구 가지가 열릴 가능성이 높다.
> 🔴 **확정은 사람 한 판이다** — 종료 로그 한 줄이면 끝난다.

- [ ] 🔴 **판이 끝날 때 런 골드가 얼마나 남는가** — 이게 이 절의 유일한 핵심 질문이다.
      런 골드는 **소멸한다.** 계산상 수입 ≈1,100G 인데 최대 지출은 ≈400G 라 **절반 넘게 버려진다**
      - 종료 로그를 보면 된다: `[GameManager] 런 정산 — 메타 골드 +N · 남은 런 골드 M 소멸`
      - **M 이 300 이하**면 지금 가격이 맞다. **M 이 600 이상**이면 배수구가 모자란 것이다
      - 배수구를 늘리는 순서: ① `Economy.csv` `StageMapManager,weightShop` 0.15 → 0.28
        (상점 방문 1.2 → 2.2회) ② `ShopManager,shopSlotCount` 3 → 4
      - ⚠️ **가격을 더 올려서 맞추려 하지 말 것.** 상점이 1.2번밖에 안 열리므로
        아이템 하나가 한 노드 수입을 통째로 먹는 값이 돼야 하고, 그러면 "한 개 사고 끝"이 된다
- [ ] **첫 상점에서 살 수 있는 게 있는가** — 상점이 1~2층에 뜨면 수입이 아직 **140~290G** 뿐이다.
      가장 싼 게 `PickupRadius` 40G. 아무것도 못 사고 나오면 노드를 밟은 게 손해가 된다
      - 그런 판이 나오면 낮추는 건 전 품목이 아니라 **패시브만** (40~60 → 30~45)
- [ ] **리롤 25G(+15) 이 "고민"인가 "안 씀"인가** — 예전 3G 은 항상 눌렀다.
      한 번도 안 누르게 됐으면 `baseRerollCost` 를 15 로, 여전히 늘 누르면 35 로
- [ ] **레벨업 리롤 10G 이 상점 예산을 갉아먹는 게 느껴지는가** — 한 판 10~12회 = 100~120G,
      **상점 아이템 한 개 값**이다. 의도한 트레이드지만 체감상 "리롤이 사치"로 읽혀야 한다
- [ ] 🔴 **상점·이벤트 노드를 밟는 게 아까운가** — 이 둘은 **전투도 클리어 보상도 없다**.
      10칸 중 2.4칸이 수입 0 이다. 이벤트가 평균 8.4G 밖에 안 줘서 "꽝"으로 느껴지면
      `Events.csv` 의 `CurrencyBonus`(0/10/12/20)를 새 물가에 맞춰 올린다 — **지금은 안 올렸다**
### H-2. 영구 강화 7종 — 총 3,910G ≈ 완주 30판 *(C29)*

값의 근거는 [`BALANCE.md`](BALANCE.md) §3-5 에 있다. **여기 있는 건 플레이해야만 답이 나오는 것들이다.**

- [ ] 🔴 **첫 판이 끝났을 때 뭔가 하나는 샀는가** — 이 절의 핵심 질문이다.
      가장 싼 칸을 **60G**(Magnetism)로 둔 게 그 때문이다. 완주 132G · 중도 사망 50~80G
      - 못 샀으면 60 → 45 로 내린다. **화면이 있는데 빈손으로 나가면 그 화면은 없는 것과 같다**
- [ ] **30판이 길게 느껴지는가** — 이건 취향이 아니라 **용도**의 문제다.
      이 프로젝트는 포트폴리오고 보는 사람은 1~3판 한다. 길면 `Costs` 를 **전 항목 균등하게** 내린다
      - ⚠️ 한 항목만 싸게 하지 말 것. 그 항목이 "정답"이 되어 나머지 6개가 죽는다
- [ ] **`Toughness +2` 가 체감되는가, 아니면 너무 센가** — 슬라임 6→4 · 고블린 8→6 이다.
      🔴 **올리지 말 것.** `+4` 면 좀비까지 바닥값 1 이 된다(`BALANCE.md` §3-5 표). 세면 `+1` 로 내린다
- [ ] **`Swiftness +0.2` 를 레인저 플레이어가 체감하는가** — 레인저(+0.6)의 1/3 이다.
      **레인저가 "이제 별로 안 빠른 것 같다"고 느끼면 그건 이 값이 너무 높다는 뜻**이다
- [ ] **`Precision`(치명 배수)이 눈에 보이는가** — 기본 치명 확률이 **5 %** 뿐이라
      대부분의 직업은 20번에 한 번 본다. 어쎄신(20 %)만 체감할 수도 있다
      - 아무도 체감 못 하면 이 칸은 `CritChance` 로 바꾸는 게 맞다. **어쎄신 정체성과 겹치는 게 문제라 피한 것**이라
        바꾸려면 어쎄신의 `+0.15` 를 같이 봐야 한다
- [ ] 🟡 **Greed 가 없는 게 허전한가** — 일부러 뺐다(`BALANCE.md` §3-5).
      되살리는 조건은 **위 §H 의 런 골드 배수구가 먼저 뚫리는 것**이다. 순서를 뒤집지 말 것

- [ ] **캐릭터 해금 150G 이 몇 판 만에 닿는가** — 완주 1.1판이지만 **죽으면 50~80G** 라
      실제로는 2~3판. 5판 넘게 걸리면 `characterUnlockCost` 를 100 으로
      - ✅ **더는 유일한 배수구가 아니다** — `D30`/`C29` 로 영구 강화 7종(총 3,910G)이 생겼다.
      해금 150G 와 **첫 강화 60G** 중 무엇을 먼저 사는지가 실제 선택지다 (§H-2)

### I. 적 밀도 — 얼마나 많고, 얼마나 겹치나 *(C28 · `D27` 측정 후)*

`D27` 이 성능을 처음 쟀다([`PERF.md`](PERF.md)). **여기 있는 것은 그 측정이 "숫자로는 답이 안 나온다"고
넘긴 것들**이다. 셋 다 답이 ms 가 아니라 **눈**이다.

#### I-1. 🟢 `MaxAlive` 를 올릴 것인가 — **성능은 이제 이유가 아니다**

- [ ] 🔴 **화면이 뱀서라이크처럼 보이는가** — 이 절에서 제일 큰 질문이다.
      `PERF.md` §0 이 *"화면에 적이 수백 마리 뜨는 것이 장르의 정의"* 라고 적어 놓고 시작했는데
      현재 상한은 **130** 이다
      - **130 에 성능 근거는 없었다.** 난이도 곡선으로 정한 값이고, 성능은 이번에 처음 쟀다
      - 실측: **130 에서 무리 분리가 프레임의 1.0~1.1 %.** 60 fps 예산에 3배가 남는다
      - ✅ **`D58` 실측: 9층 294마리에서 111.3 fps (8.99 ms)** — 통과선 60 을 크게 넘는다.
        `C38` 이 계산으로 잡은 `layerSpawnGrowth 0.14` 를 **안 내려도 된다**
      - 🟢 **400 까지는 그냥 올려도 된다** — 비용 차이가 측정 노이즈 안이다 (`PERF.md` §8-4)
      - 🔴 **400 을 넘기려면 `REQ/DEV.md` 에 요청**한다. `EnemyBase.NeighborBufSize`(64)의
        전제가 깨진다 → 고치는 법은 [`BALANCE.md`](BALANCE.md) `Waves.csv` §`MaxAlive`
      - 올릴 때 **여섯 웨이브를 같은 비율로** 올릴 것. 지금 곡선은 `60→80→110→110→130→90` 이고
        보스 웨이브(90)가 낮은 건 **보스에 집중시키려는 의도**라 비율을 유지해야 한다
      - ⚠️ 올리면 아래 I-2(겹침)가 **같이 나빠진다.** 둘을 한 번에 바꾸지 말 것

#### I-2. 🔴 적이 서로 너무 겹친다 — **값으로 고쳐질지 불확실하다**

측정에서 적 800마리일 때 **85 %가 0.5유닛 안에 다른 적을 두고 있었다.**
분리 반경이 `0.85` 인데 실제 간격이 그 **1/3** 이고, 최소 거리는 `0.005~0.03` — 완전히 겹친 쌍이 있다.

관련 값 3개는 전부 `EnemyBase.cs:207-209` 의 `const` 라 **DEV 소유다**(CSV 가 아니다):

| 상수 | 현재 | 올리면 | 내리면 |
|---|---:|---|---|
| `SeparationRadius` | `0.85f` | 더 멀리 밀어낸다 (질의도 비싸진다) | 더 붙는다 |
| `SeparationWeight` | `0.9f` | 밀어내는 힘이 세진다 (추격이 흐트러진다) | 추격 우선 |
| `SeparationEveryNSteps` | `4` | 덜 자주 계산 = 싸지만 더 겹친다 | 자주 = 비싸다 |

- [ ] 🔴 **적 무리가 "한 덩어리"로 보이는가, "여러 마리"로 보이는가** — 겹치면 개체 수가
      시각적으로 사라지고, 그러면 `MaxAlive` 를 올려도 화면이 안 채워진다. **I-1 과 묶인 질문이다**
- [ ] **덩어리를 뚫고 지나갈 때 억울한가** — 적 콜라이더가 트리거라 **물리 반발이 없다.**
      겹친 3마리를 스치면 접촉 피해가 3번 들어오는 것처럼 느껴질 수 있다
- [ ] 🟡 **값만 바꿔서 될 일이 아닐 수 있다** — 원인이 상수가 아니라 **설계**일 가능성이 있다
      (적끼리 물리 충돌이 없고 **조향으로만** 밀어낸다, `EnemyBase.cs:203-205`).
      그렇다면 CSV·상수가 아니라 DEV 의 설계 결정이다 → 그럴 땐 `REQ/DEV.md` 로 넘긴다

**🔑 판정에 쓸 실측값이 있다** (적 800 기준, `PERF.md` §8-6). 값을 바꾼 뒤 다시 재서 **이걸 넘겨야** 개선이다:

| 지표 | 버퍼 12 | 버퍼 64 (현재) | 읽는 법 |
|---|---:|---:|---|
| 평균 최근접 거리 | 0.3144 | **0.3400** (+8.1 %) | 분리 반경 0.85 의 **40 %** 밖에 안 된다 |
| 0.5유닛 내에 이웃이 있는 적 비율 | 84.6 % | **85.1 %** | 🔴 **거의 안 줄었다** |

> ⚠️ **이 표가 이 절의 존재 이유다.** `D27` 이 이웃 절단(`B8`)을 고쳐 12칸 → 64칸으로 늘렸는데
> 겹침은 `84.6 → 85.1 %` 로 **꿈쩍도 안 했다.** 즉 **절단은 증상이었고 원인은 밀도다.**
> 상수를 만질 때 같은 함정을 조심할 것 — *"고쳤으니 나아졌겠지"* 로 넘어가면 안 되고,
> **`0.5유닛 내 비율`이 실제로 떨어지는지**를 봐야 한다.

#### I-3. 🟡 경험치 구슬이 필드에 쌓이는 것 — **수명 상한은 두지 않기로 했다** *(C28 결정)*

측정 중 구슬이 **500개 넘게** 쌓이는 것이 관측됐다. 수명 상한을 줄지 물어 왔고 **두지 않기로 했다** —
얻는 게 0.5 ms(예산의 3 %)인데 잃는 게 자석 픽업의 존재 이유다.
대신 **멀어진 구슬을 회수**하도록 요청했다 (`RecycleFarEnemies` 와 같은 원칙·같은 38유닛 기준).
전문은 [`Parallel/REQ/DEV.md`](Parallel/REQ/DEV.md) **요청-21**.

- [ ] 🔴 **회수로 경험치가 얼마나 늘었나** — ✅ **`D29` 로 들어왔다. 이제 실플레이만 하면 된다.**
      - ⚠️ **`D35` 재측정의 `ExpDrop 0` 을 답으로 읽지 말 것.** 그 시나리오는 **구슬이 생길 조건이 아니었다** —
        회수가 잘 돌았다는 증거가 아니다 (DEV 가 직접 못 박았다)
      예전엔 뒤에 흘린 구슬이 조용히 소실됐다. 이제 회수되므로 **수입이 는다.**
      얼마나 느는지는 **추측하지 않는다** — 요청-21 판정 ③ 의 로그
      `[WaveManager] 원거리 구슬 회수 N개 · XP +M` 의 **M** 을 보고 여기에 적는다
      - 늘어난 폭이 크면 손댈 곳은 `ExperienceManager.xpThresholds` 인데 **아직 자리표시 값이다.**
        §3 의 순서상 이걸 먼저 확정해야 하므로 **지금 미리 고치지 않는다**

- [ ] **지나온 자리에 구슬이 눈에 띄게 깔려 있는가** — 자동 흡수 반경이 `max(PickupRadius, 8)` 이고
      화면 반높이가 6 이라 **세로로 화면 안이면 무조건 따라온다.** 쌓이는 건 **가로로 흘린 것**뿐이다
      - 🔴 **이 `8` 이 `PickupRadius` 스탯을 통째로 죽이고 있다** — 스탯 천장이
        base 2 + 패시브 4 + 메타 1 = **7** 이라 8 에 못 닿는다. 구슬에 대해서는 아무 효과가 없다
      - 화면에 구슬이 **깔려 보이면** `ExpDrop.magnetActivationRange` 8 → 11 (화면 반너비 10.67)
      - 그러면 흘리는 구슬이 거의 없어져 자석 픽업이 무의미해진다 → **둘을 같이 보고 결정할 것**
- [ ] **자석 픽업이 "한 방"으로 느껴지는가** — 자석은 필드 전체를 거리 무관하게 쓸어 담는다.
      쌓인 구슬이 많을수록 세지는 물건이라 **쌓이는 것이 자석의 밥이다**
      - 자석이 시시하면 `Enemies.csv`·픽업 확률이 아니라 **여기(쌓이는 양)를 먼저 의심**할 것

### K. 적이 어떻게 오는가 — 등장 4종 · 행동 5종 *(C39 · D50·D51 인계)*

**로그로는 하나도 판정 안 된다.** 전부 사람이 보고 정하는 값이다.
🔑 두 표는 같은 질문의 앞뒤다 — **K-1 은 "어디서 나타나나", K-2 는 "어떻게 다가오나"** 다.
같이 보지 않으면 한쪽만 고쳐 놓고 *"왜 아직 답답하지"* 가 된다.

#### K-1. 등장 패턴 — `Economy.csv` *(D50)*

| 행 | 지금 | 무엇을 보나 | 이상하면 |
|---|---|---|---|
| `WaveManager,ringBatch` | `8` | 둘러싸일 때 **빠져나갈 틈**이 있나 (8이면 45° 간격) | 답답하면 `6` · 조이려면 `12` |
| `WaveManager,squadColumns` | `4` | 부대가 **한 덩어리로** 보이나 줄로 보이나 | 넓히려면 `6` · 뭉치려면 `3` |
| `WaveManager,squadSpacing` | `1.1` | 대형 간격. 적 지름(≈0.5)의 2배 | 겹쳐 보이면 `1.4` |
| `WaveManager,burrowMinDistance` | `3` | 너무 가까이서 솟나 | 겹치면 `4` |
| `WaveManager,burrowMaxDistance` | `6` | 멀면 땅굴 느낌이 안 난다 | `5`~`8` |
| 🔴 `WaveManager,burrowWindup` | `0.85` | **예고를 보고 비킬 수 있나.** 이 표에서 제일 중요하다 | 못 비키면 `1.2` · 늘어지면 `0.6` (코드 하한 `0.35`) |
| `WaveManager,burrowBatch` | `4` | 한 번에 몇 군데서 솟나 | 심심하면 `6` |

> 🔑 **`burrowWindup 0.85` 를 보스의 `SlamWindup 1.15` 와 같은 값으로 "정리"하지 말 것.**
> 둘은 다른 문제다 — 내려찍기는 **원 밖으로 나가야** 하니 `반경 ÷ 예고 ≤ 이동속도` 라는 부등식이 있고
> 그래서 `C30` 이 계산으로 1.15 를 뽑았다. 땅굴은 **최소 3유닛 밖**에서 솟으므로
> 플레이어가 그 자리에 서 있을 일이 없다 — **피하는 게 아니라 자리를 옮기는 예고**다.
> ⇒ 숫자가 다른 게 정상이다.

#### K-2. 행동 — `Enemies.csv` 의 새 열 *(D51)*

적 7종 중 **5종이 `Chaser` 하나**여서 색만 다른 같은 적이었다. 셋을 갈랐다.
`Zombie` 는 **대조군으로 일부러 `Chaser` 로 남겼다** — 우직한 기준선이 하나는 있어야 한다.

| 열 | 적 | 지금 | 무엇을 보나 | 이상하면 |
|---|---|---|---|---|
| `FlankArcWeight` | Goblin | `0.85` | 얼마나 크게 도나 (거리 20에서 **40.4°** 실측) | 답답하면 `0.5`(26.6°) · 더 감으려면 `1.0`(45°). **1 초과는 코드가 막는다 — 넘으면 영영 안 다가온다** |
| `FlankCloseRange` | Goblin | `3.5` | 어디서부터 직진으로 바꾸나 | 덜 감으려면 `5` · 끝까지 감으려면 `2` |
| `SwarmSoloMult` | Slime | `0.6` | 혼자 남았을 때 주춤하는 정도 (1.4 × 0.6 = **0.84**) | 너무 느리면 `0.75` |
| 🔴 `SwarmPackMult` | Slime | `1.35` | 무리가 꽉 찼을 때 (1.4 × 1.35 = **1.89**) | **플레이어 3.5~4.6 을 넘기지 말 것** — 넘으면 피하는 게 아니라 맞는 게 된다 |
| `SwarmFullCount` | Slime | `6` | 몇 마리부터 최고 속도인가 | 더 빽빽해야 하면 `10` |
| 🔴 `BlockLeadTime` | Ogre | `1.8` | **몇 초 앞을 노리나.** 이 표에서 제일 중요하다 | 헛돌면 `1.2` · 쉽게 지나치면 `2.5` |
| `BlockHoldRange` | Ogre | `2.5` | 코앞에서 예측을 끄는 거리 | 붙어서 헛돌면 `3.5` |

- [ ] 🔴 **다섯이 서로 다르게 오는 게 보이는가** — 이 두 표의 존재 이유다.
      안 보이면 개별 계수가 아니라 **차이의 폭**이 모자란 것이다 (`FlankArcWeight`·`SwarmPackMult`·`BlockLeadTime` 셋을 같이 키운다)
- [ ] **Ogre 가 `Blocker` 가 된 게 체감되는가** — 실속도 **1.2** 라 예전엔 영원히 못 따라잡아
      화면에 있어도 없는 것과 같았다. 이제 쫓지 않고 **가려는 곳을 막는다**
- [ ] **Slime 이 뭉칠수록 위협적인가** — 예전엔 30마리가 *"한 마리 × 30"* 이었다
- [ ] ℹ️ **성능은 안 늘었다** — `Swarmer` 의 이웃 수는 무리 분리가 **이미 세던 값**을 쓴다.
      새 `OverlapCircle` 이 0개라 9층 294마리에 얹히는 비용이 **0** 이다 (`D51` 확인)

---

### J. 보스 — 패턴이 성립하는가 *(C30)*

값의 근거는 [`BALANCE.md`](BALANCE.md) `Bosses.csv` 절에 있다.
🔑 **이 싸움은 셋이 맞물려 있다** — 보스도 소환수도 플레이어를 **못 쫓아온다**(1.08 · 2.2 vs 3.5~4.6).
그래서 *"플레이어가 딜을 넣으려고 제 발로 사거리(3.5~6) 안에 들어간다 → 소환수가 그 자리를 막는다 →
내려찍기가 그걸 벌한다"* 가 유일한 위협 경로다. **하나만 줄여도 보스가 통째로 무해해진다.**

#### J-1. 🔴 내려찍기를 실제로 피할 수 있는가 — **이 절의 핵심**

계산상으로는 전 직업이 반응 0.25초를 쓰고도 1.3배 여유로 벗어난다. **그 0.25초가 맞는지가 관건이다.**

- [x] ~~폭발광(이속 3.5)으로 예고를 보고 반응해 피할 수 있는가~~ — ✅ **`D34` 가 대조군으로 증명했다.**
      옛 값(r 3.2 · w 0.9)에서는 **HP 110 → 93** 으로 맞고, 현재 값(r 2.4 · w 1.15)에서는 **93 유지**다.
      🔑 **"안 맞았다" 하나만으로는 테스트 고장과 구별이 안 되므로 맞는 조건을 같이 돌린 것** — `D31` 에 빠졌던 게 이거다
- [ ] **그래도 실플레이(난전 + 소환수)에서 피해지는가** — 위는 대조군 실험이고 이건 실전이다.
      못 피하면 → **반응 시간이 0.25초보다 길다는 뜻**이다. 반경이 아니라 **`SlamWindup` 을 `1.35` 로** 올린다
      - ⚠️ **`SlamRadius` 를 먼저 줄이지 말 것.** 반경을 줄이면 "피한다"가 아니라 "안 맞는다"가 되어
        패턴 자체가 사라진다. 예고를 늘리는 쪽이 기술을 살린다
- [ ] **반대로 너무 쉽게 피해지는가** — 계속 움직이는 플레이어는 반경 2.4 를 **0.69초**면 벗어난다.
      즉 이 기술은 **멈춰 있거나 갇혔을 때만** 맞는 물건이다. 한 번도 안 맞으면 소환이 일을 안 하는 것이다 → J-2
- [ ] **예고 원이 어두운 타일 위에서 보이는가** *(C30 이 전용 스프라이트를 만들었다)*
      - ✅ **판정 원 = 보이는 원**은 이미 확인됐다 (`D34` 가 `scale 4.8` = 반경 2.4 ÷ 0.5 로 실측).
        예전 `ToxinField` 블롭은 달랐다 — 이 항목은 이제 **밝기만** 본다
      - 🟡 **`warnColor.a` 0.30 → 0.45 는 `D34` 가 일부러 안 올렸다.** 새 스프라이트는
        테두리 알파가 이미 1.0 이라 캡처에서는 또렷했고, **지금 같이 올리면 둘 중 무엇 덕에
        잘 보이는지 못 가르기 때문**이다. 🔴 **실플레이에서 흐리면 그때 올린다**(프리팹이라 DEV 요청)
- [ ] **`SlamDamage 18` 이 아픈가** — 어쎄신(HP 70·방어 −1)은 **19씩 3.7대**, 전사(130·방어 +2)는 **15씩 8.7대**다
      - 어쎄신이 두 대에 죽는 느낌이면 14 로. 맞아도 아무렇지 않으면 24 로

#### J-2. 소환 — 밀도가 맞는가 *(값을 안 건드렸다)*

`3\|4\|6` 마리를 `12\|9\|7` 초마다. **D31 임시값 그대로다** — 바꿀 근거가 없었다.

- [ ] **3페이즈에서 사거리 안에 서 있기가 어려워지는가** — 이게 소환의 존재 이유다.
      쉬우면 `SummonCount` 를 `4\|6\|8` 로, 화면이 고블린으로 덮이면 `3\|4\|5` 로
- [ ] **고블린이 경험치·골드 밭이 되어 오히려 이득인가** — Goblin 은 XP 3 · 골드 1 이다.
      3페이즈에 수십 마리면 보스전이 **파밍 구간**이 된다. 그러면 `SummonEnemyId` 를 `Slime`(XP 4지만 느리다)이 아니라
      **더 단단한 것**으로 바꾸는 게 낫다 — 숫자가 아니라 종류의 문제다
- [ ] ℹ️ `MaxAlive` 는 걱정 안 해도 된다 — 웨이브가 18마리뿐이라 90 중 72칸이 비어 있다

#### J-3. 페이즈 진행

- [ ] **240초 안에 3페이즈까지 가는가** — 보스 HP **1540** 이고 임계값이 `0.7\|0.4` 다.
      2페이즈도 못 보고 시간이 끝나면 임계값이 아니라 **`BossHpMult`(7)** 를 내리는 게 맞다 (`Enemies.csv`)
- [ ] **3페이즈에서 보스가 빨라진 게 보이는가** — 1.08 → **2.38** 이다.
      🔴 **여전히 플레이어보다 느린 건 의도다.** 따라잡히면 카이팅이 사라진다.
      "빨라진 걸 못 느끼겠다"면 `2.2 → 2.8` 까지는 올려도 된다(3.02 < 3.5)

---

## 3. 근거 없는 자리표시 수치 — **고치는 순서가 있다**

**감으로 넣었고 아무도 검증하지 않은 값들.** 위 체감 판정의 결과로 여기를 고친다.

### 왜 순서가 필요한가

15개를 아무 데나 손대면 **뭐가 효과를 냈는지 알 수 없다.** 실제로 한 번 겪었다 —
I-55 에서 소지 상한과 직업 `Bonus*` 를 같이 흔들 뻔했고, 그래서 일부러 상한만 넣고 멈췄다.

이 목록은 **평평하지 않다.** 위쪽 값은 아래쪽 값의 **판정 기준 자체를 움직인다.**
플레이어 HP 가 틀린 상태에서 "무기가 약하다"를 판정할 수는 없다.

🔴 **한 플레이 세션에 한 단계만 흔든다.** 고친 뒤에는 §4 에 반드시 기록한다.

| 단계 | 무엇을 정하나 | 다음 단계에 무엇을 넘기나 |
|---|---|---|
| **0** | 내가 얼마나 버티고 얼마나 빨리 크나 | 모든 TTK·난이도 판정의 **기준선** |
| **1** | 적 하나를 죽이는 데 걸리는 시간 | 패시브·진화가 **얼마나 보태야 하는지** |
| **2** | 직업이 서로 다르게 느껴지나 | 승급·진화가 **비집고 들어갈 자리** |
| **3** | 개별 무기·건물의 손맛 | (끝) |
| **막힘** | — | **기능이 없어서 지금은 판정 자체가 불가능** |

---

### 0단계 — 이걸 먼저 안 하면 아래가 전부 무의미하다

| 대상 | 현재 | 왜 근거가 없나 |
|---|---|---|
| 🔴 **플레이어 기본 스탯** | HP 100 · Speed 4 · Crit 1.5배 | I-21 로 실효값이 **전부 절반**이 됐는데 그 뒤로 재조정을 안 했다. 지금 값은 **2배 버그 시절에 잡은 감**이다. → **여기가 틀리면 1·2단계 판정이 전부 오염된다** |
| 🔴 **XP 곡선** | Lv1 에 **5** | 구슬 10개면 3레벨이 오른다. I-46(적 증가) + I-48(자석)으로 **더 빨라졌다.** 자석 하나가 사실상 "즉시 레벨업 2~3회". → **런의 길이 자체를 정하는 값**이라 무기·패시브 강도보다 먼저다 |

> 이 둘은 §2-A(타격감)·§2-F(빌드 설계)를 보며 같이 판정한다.
> **둘을 동시에 흔들어도 된다** — HP 는 "버티나", XP 는 "빨리 크나"라 체감이 안 섞인다.

### 1단계 — 전투의 뼈대 (0단계 확정 후)

| 대상 | 현재 | 왜 근거가 없나 |
|---|---|---|
| **무기 `Damage` vs 적 `MaxHp`** | — | 플레이어 스탯이 절반이 된 만큼 **TTK 가 2배 가까이 늘었을 수 있다.** 이 단계의 **본체**이고 나머지는 여기에 딸린 보정이다 |
| 🔴 **AoE `explosionRadius`** | 3 → **2** (11차) | 그림이 반경에 맞춰 커지면서 지름 6유닛이 화면의 1/3 을 덮어 낮췄다. **면적이 56% 줄었으므로 Mage 가 눈에 띄게 약해졌을 것.** 다수 적 상황에서 보고 `Weapons.csv` 의 `Damage` 로 보정할지 정한다 |
| 🔴 **패시브 Lv5 실효값** | `Damage` +0.55 | I-32 로 누적 버그를 고치며 **절반 이하로** 떨어졌다. `Passives.csv` 값 자체가 "누적된다는 전제로 잡힌 감"일 수 있다. **TTK 기준선이 잡힌 뒤에야** "+0.55 가 충분한가"를 물을 수 있다 |

> ⚠️ **반경과 데미지를 같은 판에서 흔들지 말 것.** 둘 다 "Mage 가 세졌나"로만 체감돼서 구분이 안 된다.
> 반경은 **눈으로**(화면을 얼마나 덮나), 데미지는 **시간으로**(몇 초에 죽나) 따로 본다.

### 2단계 — 직업이 서로 다르게 느껴지나 (1단계 확정 후)

🔴 **이 단계는 순서가 표 안에서도 강제된다.** 위에서 아래로 하나씩이다 (I-55).

| # | 대상 | 현재 | 왜 근거가 없나 |
|---:|---|---|---|
| ① | 🔴 **승급 소지 상한** | Warrior(건물 5) + Warden(+2) = **7칸** | **건물이 5종뿐이다.** Warden 을 타면 건물 상한이 사실상 사라져 18차에 만든 직업 차이가 무효화된다 → `BonusBuildingSlots` 를 0~1 로 낮출지 판단 |
| ② | **직업 3종 `Bonus*`** | Warrior +30HP/+2방어 · Mage −25HP/−1방어 | ⚠️ **18차에 일부러 안 건드렸다** (I-55). 상한과 스탯을 동시에 흔들면 **어느 쪽이 효과를 냈는지 알 수 없다.** ①을 먼저 판정할 것. 상한이 이미 충분한 차이를 만들고 있다면 `Bonus*` 는 **줄이는** 방향이 맞을 수도 있다 |
| ③ | 🔴 **승급 4종 수치** | Sentinel/Doomlord/Warden/Aegis | **누적된다는 걸 잊기 쉽다.** Warrior→Sentinel→Aegis 면 `armor 2+1+3=6`, `MaxHp +110`. 표의 숫자만 보면 작아 보인다 |
| ④ | **진화 무기 3종** | 원본의 약 **1.7배** | 진화는 **재료 무기를 소모**한다. "슬롯 하나를 비우고 얻는 값"이므로 **원본 2개분 이상**은 돼야 손해가 아니다. 1.7배는 손해일 수 있다. ①에서 슬롯 하나의 값어치가 정해져야 판정된다 |

> ③④는 **한 런에 도달조차 못 할 수 있다.** 그러면 그게 결론이다 — 수치가 아니라
> 도달 시간을 먼저 고쳐야 한다 (§4 에 "승급까지 몇 분"을 반드시 적는 이유).

### 3단계 — 개별 손맛 (언제 해도 되지만 위가 흔들리면 다시 봐야 한다)

| 대상 | 현재 | 왜 근거가 없나 |
|---|---|---|
| **건물 산출** | 식당 회복 15~85 · 농장 골드 1~6 | `Buildings.csv`. 범위가 넓은 것부터 근거가 없다 |
| **곡사포** | `bombSpeed` 6 · `arcHeight` 1.2 | 사거리 끝(3.5)까지 약 0.6초. 느려서 적이 빠져나가면 속도를, 던지는 맛이 없으면 `arcHeight` 를. ⚠️ 폭탄이 **날아가는 동안 적이 이동하면 빗나간다** — 착탄 지점을 좌표로 굳혀 쏘기 때문(의도된 동작) |
| **파이어볼** | `ProjectileSpeed` 11 | I-52 의 자리표시값 |
| **자석 드랍** | `0.006` (웨이브당 1~2개) | 체감상 안 나오면 올린다 (`SceneWiring.csv`) |
| **적 타격감 12값** | `CombatFeel` (→ §1) | 값 자체는 **지금 게임에 들어 있는 것 그대로**라 "틀렸다"가 아니라 **아직 아무도 안 물어봤다**가 맞다 |
| 🔴 **수리검 뭉침 배수** (C12 → **D12 실측**) | `Weapons.csv` `Shuriken` — 피해 `6\|9\|13\|17\|23` · 쿨 `0.9→0.5` · 동시발사 `1\|1\|1\|2\|2` + `Proj_Shuriken.prefab` 의 `pierceCount` **3** | **설계대로 됐다. 남은 질문은 "얼마나"다.** 발사체당 명중이 흩어짐 **0.91** → 뭉침 **3.10** = **3.4배**, `pierceCount 3` 상한에 정확히 붙는다. Lv5 뭉침 상한 ≈ **276 DPS**. 🔴 **이 숫자만 보고 내리지 말 것** — 뭉침 이득은 어쎄신의 **정체성**이고 276 은 "적이 줄을 섰을 때"의 **상한**이지 평균이 아니다. 판정은 **"떼가 오는 게 무섭지 않고 반갑기만 한가"** 로 한다. 반갑기만 하면 과한 것이다 → **`ProjectileCount` 를 먼저** 내린다(관통과 곱해지는 건 이쪽이라 `Damage` 보다 효과가 크다). ⚠️ 1단계(무기 Damage vs 적 MaxHp) 뒤에 다시 본다 |
| ~~**검의 `Damage`**~~ (D10·C12 → **D12 로 판정 끝**) | `Weapons.csv` `Sword` — `10\|15\|22\|30\|40` **유지** | ✅ **대조 실험이 끝났고, 답은 "올리지 않는다"다.** Lv5(사거리 2.50·쿨 0.70·3연타) 14초에 **14킬** — 수리검 Lv5 의 16킬과 동급이다. 사거리를 `10 → 2` 로 5분의 1 로 자르고도 **맞기만 하면 안 약하다.** 그러므로 "붙어야 하는 대가"를 **피해로 치르는 건 오답**이다. 진짜 문제는 아래 **검 사거리** 줄로 넘어갔다. ⚠️ 1단계에서 전체 TTK 가 흔들리면 그때 다시 본다 |
| 🔴 **검 사거리 — "세기"가 아니라 "존재"의 문제** (D12 발견) | `Weapons.csv` `Sword` 의 `Range` `2\|2.1\|2.2\|2.35\|2.5` | 🔴 **§3 전체에서 가장 먼저 판정할 항목이다.** D12 실측: 가만히 선 플레이어에게 **가장 가까운 적이 4.67유닛**에 머물러 **6초 동안 0회** 휘둘렀다. 세지만 **닿을 일이 없다** — 카이팅하면 검은 자주 논다. 고를 수 있는 건 셋이다: **(가) `Range` 를 올린다** — 가장 싸지만 "근접"이라는 정체성이 흐려지고 `halfAngle`·호 그림(`1.074`)까지 같이 봐야 한다. **(나) 접근 유인을 준다** — 붙어야 이득이 나는 구조(근접 시 피해 증가 등). 제일 재미있지만 **코드가 필요하다**(DEV). **(다) 그대로 둔다** — 검을 "붙는 플레이 전용"으로 못 박고 원거리와 역할을 가른다. 🔴 **무엇을 보나: 피해량이 아니라 "몇 번 나갔나"를 센다.** 한 판에서 검이 실제로 휘둘러진 횟수가 판정 대상이다 |

**D10 이 넣은 근접·관통 7값** — 전부 **프리팹 필드**다 (CSV 아님). 값은 게임에 들어가 있고
동작도 검증됐다. 모르는 건 **느낌뿐**이다.

| 값 | 지금 | 어디 | 무엇을 보나 · 이상하면 |
|---|---:|---|---|
| `pierceCount` | `3` | `Proj_Shuriken.prefab` | 3명 뚫는 게 **과한가.** 어쎄신의 정체성이라 낮으면 무기가 안 특별하고, 높으면 줄 선 적을 쓸어버려 다른 무기가 무의미해진다 → 과하면 2 |
| `spinSpeed` | `540` | `Proj_Shuriken.prefab` | 자전이 **어지러운가** → 360 |
| `halfAngle` | `70` | `Weapon_Melee.prefab` | 앞쪽 **140°**를 벤다. 옆의 적이 안 맞아 답답하면 올린다. ⚠️ 그림의 부채꼴(142°)에 맞춘 값이라 **올리면 안 보이는 걸 때리게 된다** |
| `hitDelay` | `0.067` | `Weapon_Melee.prefab` | 칼이 몸을 지나는 **순간에** 피가 뜨나. 늦게 뜨면 내린다 |
| `comboInterval` | `0.18` | `Weapon_Melee.prefab` | 연타(`ProjectileCount`)가 **따로 노나 / 겹쳐 보이나** |
| `frameRate` | `30` | `Fx_SwingArc.prefab` | 휘두름 한 번이 **0.2초**다. 굼뜨면 올린다 |
| `spriteRadiusAtScaleOne` | `1.074` | `Fx_SwingArc.prefab` | 🔴 **체감값이 아니다. 손대지 말 것** — 아래 |

🔴 **`1.074` 는 취향이 아니라 그림의 실측값이다.** `SwingArc.png` 셀 256px 중
호의 바깥 반지름이 **107.4px**, PPU 100 이라 1.074 유닛이다. 호를 `Range` 에 정확히
맞추는 나눗셈(`localScale = Range ÷ 1.074`)에 쓰인다. 흔들면 **그림이 사거리를 속인다** —
호는 2 만큼 보이는데 실제로는 2.2 를 벤다. 크기를 바꾸고 싶으면 **`Range` 를 고칠 것.**

> ✅ **무기 전용 효과음 2종 — 배선 끝났다** *(C17·C18 → D14, 2026-08-30)*
> 검은 `WeaponSwing`(4), 장판은 `ToxinSpill`(5) 로 **자기 소리를 낸다.**
> 빌려 쓰던 `WeaponFire`·`WeaponCast` 는 프로브 대조군에서 **0회**로 확인됐다.
> 🔑 **이제 위 근접 7값(`halfAngle`·`comboInterval`…)을 판정해도 된다** —
> 배선 전에 판정했으면 **소리 탓을 수치 탓으로 오해**할 뻔했다. 베는 맛의 절반은 소리다.
> 남은 건 볼륨·길이뿐이고 전부 위 **§2-C-2** 의 체크박스다.
> ✅ **소환수 2종(`SummonWeapon.cs:159·174`)도 배선이 끝났다** (C20 → **D16**).
> 촉수에서 총소리가, 드래곤에서 시전음이 나던 상태는 **해소됐다** —
> 이제 소환수 체감도 판정해도 된다.

**C7~C15 가 넣은 그림·수치의 느낌값** — 위 표와 같은 성격이지만 출처가 달라 따로 둔다.

| 대상 | 현재 | 왜 근거가 없나 |
|---|---|---|
| **화살 크기** (C7 → **D9 실측**) | `Arrow.png` PPU **512** → 알파 bbox `200×72px` = **`0.391 × 0.141` 유닛** | 전에 쓰던 총알 `Bullet.png`(0.41 유닛)과 폭을 맞춘 값일 뿐, 화면에서 본 적이 없다. 아래 표대로 **PPU 숫자 하나만** 고친다 — CSV·프리팹은 안 건드린다. ⚠️ 잘 안 보인다고 `ProjectileSize`(`Weapons.csv`) 를 올리지 말 것 — 그건 **판정 크기까지 같이 커진다** |
| **바닥 폭탄 신관** (C9 → **C15 켜짐**) | `Proj_BombGround.prefab` 의 `fuseTime` **1.1s** (프리팹) + `Weapons.csv` `Bomb` 의 `ProjectileSpeed` **9** (CSV) | 🔴 **이 둘이 폭탄을 즉발에서 예측 사격으로 바꾼다.** 길면 적이 다 빠져나가고, 짧으면 달아오르는 4프레임을 볼 시간이 없어 **개조한 의미가 사라진다.** 비행(속도 9)까지 합친 **총 지연**으로 판정할 것. ⚠️ 🔴 **`Damage` 는 일부러 안 올렸다** — 검(C12)과 같은 대조 실험이다. 약하면 그건 버그가 아니라 **측정값**이고, 늦어진 대가만큼만 나중에 얹는다 |
| **독 장판 3값** (C9 → **D12 일부 확인**) | `Proj_ToxinField.prefab` 의 `fieldDuration` **4.0s** · `tickInterval` **0.5s** · `slowMult` **0.6** | ✅ **`slowMult 0.6` 은 눈에 보인다** — D11 에서 수치(`ratio=0.600`)를, D12 에서 화면(안쪽 적이 눈에 띄게 처짐)을 확인했다. **감속은 더 안 만져도 된다.** 남은 둘은 미판정이다. 0.5 아래로 내리면 적이 사실상 멈춰 게임이 서고, 0.75 위면 아무도 못 알아챈다. 그다음 `fieldDuration` 으로 "지대"가 되는지(장판이 겹치는 구간이 생기는지)를 본다. `tickInterval` 은 마지막 — 초당 2번보다 잦으면 **데미지 팝업이 화면을 덮는다.** ⚠️ 전부 **프리팹 필드**다 |
| 🔴 **독 장판 헛방** (C9·C15 → **D12 에서 눈으로 확인**) | `Weapons.csv` `Toxin` 의 `Range` **4\|5\|5\|6\|6** | 🔴 **헛방은 가설이 아니라 관측된 사실이다.** D12 캡처에서 살아 있는 장판 **2개 중 1개 안에 적이 0마리**였다. `FieldWeapon` 은 조준하지 않고 주변 무작위 좌표에 까는 물건이라 **구조적으로 빗나간다** — 버그가 아니다. 문제는 **얼마나 자주 빗나가야 참을 만한가**다. 고칠 방향은 둘이고 **성격이 반대다**: **(가) 반경 축소** — 발밑에 붙어 명중률이 오르지만 "지대를 만든다"는 맛이 죽고 **스스로 갇힌다**(적은 통과하지만 시야가 막힌다). **(나) 개수 증가**(쿨 단축) — 맛은 사는데 확률로 덮는 거라 **총 피해가 같이 오른다**(위 피해 5값과 충돌). 🔴 **Lv1 부터 보라** — 쿨 3초 > 지속 4초라 **장판이 한 번에 하나뿐**이고, 그 하나가 빗나가면 그 판은 무기가 아예 없는 것과 같다. Lv5(2~3장 동시)에서만 보면 문제가 안 보인다. 여기만 CSV 다 |
| 🔴 **독 장판 피해 5값** (C15) | `Weapons.csv` `Toxin` 의 `Damage` **3\|4\|6\|8\|11** — **틱당**이다 | 🔴 **표에 적힌 숫자로 세지 말 것. ×8 이 진짜 값이다** (4초 ÷ 0.5초 틱) → 한 번 깔면 **24\|32\|48\|64\|88**. Bomb 한 방(30\|44\|62\|86\|118)의 **75%** 로 맞췄고, 남은 25% 는 **슬로우 값으로 지불**한 셈이다. **무엇을 보나:** 장판만 들고 있을 때 적이 *죽는가* — 안 죽으면 그건 정상이다(다른 무기가 때릴 시간을 버는 무기다). 판정은 **다른 무기와 같이 들었을 때 확실히 편해지는가**로 한다. ⚠️ `tickInterval` 을 고치면 총 피해가 **곱으로** 바뀌므로 여기도 같이 봐야 한다 |
| **장판 하나의 크기** (D11 + C15) | `Weapon_Field.prefab` 의 `fieldRadius` **1.2** × `Weapons.csv` 의 `ProjectileSize` **1\|1.1\|1.2\|1.35\|1.5** = 반경 **1.2 → 1.8** | ✅ **D12 에서 답이 나왔다: 안 덮는다.** 웅덩이 하나가 화면 세로의 약 **1/4**, 2장 동시여도 시야를 안 먹는다 → **`ProjectileSize` 를 지금 줄일 이유가 없다.** 크기 증가폭을 다른 무기보다 완만하게 잡은 건(Bomb 은 `1.2→2`) Lv5 에 2~3장이 동시에 살기 때문이다. ⚠️ 위 **헛방** 줄에서 **(가) 반경 축소**를 고르면 이 값이 같이 흔들린다 — 낙하 반경이 좁아진 채 장판까지 크면 발밑이 통째로 덮인다. 그때 다시 볼 것. `fieldRadius` 를 건드리면 전 레벨이 같이 움직이므로 손대는 건 언제나 `ProjectileSize` 쪽이다 |
| **장판 애니메이션** (D11) | `Proj_ToxinField.prefab` 의 `frameRate` **12** (6프레임 루프) | 끊겨 보이면 올린다. 이음매는 없으니 **그냥 루프**다 |
| 🔴 **`spriteRadiusAtScaleOne`(장판)** | `Proj_ToxinField.prefab` **0.97** | 🔴 **체감값이 아니다. 손대지 말 것** — D11 이 그림의 알파를 실측한 값이다(그려진 원 반지름 ≈97px / PPU 100). 셀 중심도 (128,128)이 아니라 (134,132)라 **Custom pivot** `(0.5234, 0.4844)` 가 같이 걸려 있다. 흔들면 **맞는 원과 보이는 원이 어긋난다** (검증된 화면 오차 0.7px → 보정 없으면 6.7px). 크기는 위 `ProjectileSize` 로 고칠 것 |
| **소환수 크기** (C13·C14 → **D13 실측**) | 드래곤 **132px** · 문어 **136px** (셀 256 · PPU 512) = 오거(204px)의 **64.7% / 66.7%** | ✅ **통과. 다시 그릴 필요 없다.** 목표 65% 에 둘 다 ±2%p 안이다 → `Tools/Art/gen_*.py` 의 **`TARGET_H`(34) 는 그대로 둔다.** 🔴 **`localScale` 로 고치지 말 것** — 그림 자체가 작게 그려져 있어 스케일로 줄이면 뭉개지고 키우면 외곽선이 두꺼워진다. ℹ️ **`sprite.bounds` 로 재면 안 된다** — 소환수도 오거도 똑같이 `0.500` 이 나온다. `bounds` 는 **셀 사각형** 기준이라 그림이 셀 안 어디에 얼마나 그려졌는지를 모른다 (교훈 138). 알파 bbox 로 잰다 |
| **촉수 공격 반경** (C14 → **C19 재작업**) | `TentacleLash.png` 바깥 반지름 **0.9951유닛** (실측 99.51px / PPU 100) | 🔴 **체감값이 아니다.** 호(`1.074`)와 달리 **1.0 에 맞춰 그렸으므로** 코드가 나눌 필요가 없다 — `localScale = Range` 로 두면 그림이 곧 판정이다. 크기는 **`Weapons.csv` 의 `Range`** 로 고친다. ✅ **C19 에서 그림을 다시 구웠지만 이 값은 99.51 그대로다** — 계약을 지켰으므로 프리팹은 손댈 필요가 없다 |
| 🔴 **촉수가 문어를 덮는가** (D13 ⑥-3 → **C19 에서 그림으로 해결**) | `TentacleLash.png` 가운데를 **비웠다** — 안쪽 구멍 반지름 **13.2~17.7px**, 잉크 밀도 13.7% → **7.3%** | 🔴 **처음엔 반경 2.5 가 큰 줄 알았는데 아니었다.** 잰 결과 **가운데 빛 원반**이 범인이었다 — 배율 2.512 에서 **0.384유닛**으로 렌더되는데 문어 몸통 전체가 **0.281유닛**이다(2.7배). 촉수를 가리키려던 장식이 **가리킬 대상을 덮고** 있었다. 반경은 **안 줄였다** — D12 가 반대를 증명했다(최근접 적 4.67유닛, 검 2.5는 6초간 0회). **무엇을 보나:** 후리는 동안 **문어 몸통이 보이는가.** 아직도 묻히면 `gen_octopus.py` 의 **`R_IN`(20)** 을 올려 구멍을 더 판다. 반대로 **너무 휑하면** `BASE_W` 를 올린다 (C19 이전 값은 `7.0/6.4/6.0/5.6/4.6/3.2`) |
| **소환수 애니메이션 속도** (C13·C14 → **D13 확정**) | 몸통 `frameRate` **12** (16프레임 ≈ **1.33초**) · 촉수 `Fx_TentacleLash` **30** (6프레임 = 0.2초) | 16프레임이 **한 번의 날갯짓 / 한 호흡**이다. 느리면 **16**(1초), 빠르면 **8**(2초). **무엇을 보나:** 드래곤이 **날고 있어 보이나**(느리면 떠 있는 게 아니라 멈춘 것 같다) · 문어가 **물속에 있어 보이나**(빠르면 경련한다). ⚠️ 위아래 흔들림(bob)은 **그림에 이미 그려져 있다**(드래곤 ±1.6px · 문어 ±1.5px) — 코드로 또 넣으면 두 번 흔들린다. ⚠️ 프리팹 필드다 |
| **뒤처지는 정도** (D13 → 🔴 **D19 에서 뜻이 바뀌었다**) | `Weapon_Summon*.prefab` 의 `followLerp` **6** | 🔴 **소환수는 이제 공전하지 않는다 — 플레이어가 지나온 길(`PlayerTrail`)을 되짚어 따라온다.** 감쇠라 **주행 중 지연 = 속도 ÷ `followLerp`** 다 — 속도 4 면 **0.667유닛** 뒤(실측: 정지 1.20 → 주행 1.87). **무엇을 보나:** 도망칠 때 줄이 **뚝 끊겨 보이나**(→ **8~10** 으로 올려 붙인다) / **몸에 딱 붙어 답답한가**(→ **3~4** 로 내려 늘어뜨린다). ⚠️ CSV 가 아니라 **프리팹 필드**다 |
| **칸 간격 — 기차가 얼마나 늘어지나** (D19) | `Weapon_Summon*.prefab` 의 `offsetDistance` **1.2** | 🔴 **더 이상 "플레이어와의 거리"가 아니라 앞칸과의 간격**이다. 뒤처짐은 **앞칸들의 누적 합** — 1번칸 1.2 · 2번칸 2.4 · 3번칸 3.6. 실측(속도 4, ㄱ자 주행): 정지 **1.20/2.40** · 주행 **1.87/3.07** · 칸 간격은 언제나 **1.20** · 꺾은 뒤 `x` 편차 **0.00**. **무엇을 보나:** 소환수 2마리가 **한 덩어리로 보이나** → 1.6~2.0 으로 올린다. ⚠️ **올리면 뒤칸일수록 훨씬 멀어진다**(곱이 아니라 합이라서) — 1.2→2.0 이면 3번칸은 3.6→**6.0** 이다. ℹ️ **3칸 이상은 실측이 없다** — 소환수 무기가 2종뿐이라 2칸까지만 봤다. `C6` 으로 늘면 그때 다시 잰다 |
| **길의 매끄러움 · 길이** (D19) | `Assets/Scripts/Player/PlayerTrail.cs` 의 `minStep` **0.08** · `maxLength` **14** | `minStep` = 자국 하나를 남기는 **최소 이동 거리**. 크면 꺾인 자리가 **각져 보인다.** `maxLength` = 기억하는 길의 총 길이 — **맨 뒤 칸이 설 거리보다 넉넉해야** 한다(14 면 칸 간격 1.2 기준 **11칸**까지 여유). **무엇을 보나:** 맨 뒤 소환수가 **길을 무시하고 직선으로 질러오나** → `maxLength` 를 올린다 · 꺾은 자리가 **각져 보이나** → `minStep` 을 내린다. ⚠️ 프리팹이 아니라 **스크립트 필드**다 |
| ✅ **촉수 정렬 순서** (D13 ⑥-1 → C22 실측 → **D21 에서 닫힘**) | `Fx_TentacleLash.prefab:83` 의 `m_SortingOrder` **`-5`** (바닥 `-100` 보다 앞 · 플레이어·적 `0` 보다 뒤). `Fx_SwingArc` 는 **`20` 그대로**다 | ✅ **더 이상 덮지 않는다 — 체감 판정 없음.** D21 이 같은 프레임을 order `20`/`-5` 로 두 번 렌더해서 뺐다: 기사 실루엣 336px 중 가림 **93px → 0px**(최악 프레임 12.2% → 0). 대가인 촉수 자기 잉크 손실은 **1.37%**, 적 8마리를 링에 붙인 최악 조건에서도 **1.5%** 라 폴백 `-1` 은 **불필요**하다. **이상하면:** 링이 적에 묻혀 안 읽히는 순간이 있으면 `-1` 로 올린다(실측상 필요 없어 보인다). 🔑 **남길 교훈** — 검 호는 `m_SortingOrder` 가 **똑같이 20** 이고 플레이어 **바로 위에서** 소환되는데 가림이 **0.00%** 였다. 가운데가 비어 있어서다. 즉 원인은 "order 가 높다"도 "링이 크다"도 아니라 **문어 링만 플레이어 위를 통과한다**는 것 — **기하가 아니라 합성 문제**였고, 그래서 `Range` 축소·`offsetDistance` 확대는 둘 다 헛다리였다. ℹ️ CONTENT 가 예상한 대가 3.7~4.2% 가 실측 1.37% 로 줄어든 건 **D19(꼬리 추적)로 전제가 바뀐 것**이다 — 문어가 플레이어 뒤로 물러나 링 중심과 기사 중심이 어긋났다 |
| **촉수 피해 타이밍** (D13) | `Weapon_SummonOctopus.prefab` 의 `lashHitDelay` **0.1** | 촉수가 **가장 멀리 뻗는 프레임 3** 에 피해가 들어간다 (30fps × 3프레임 = 0.1초, 도달 99.51px). 🔴 **`Fx_TentacleLash` 의 `frameRate`(30) 를 바꾸면 이 값도 같이 바꿔야 한다** — 안 그러면 촉수가 아직 안 뻗었는데 피가 뜨거나, 다 접힌 뒤에 뜬다. `hitDelay = 3 ÷ frameRate` |
| **소환수 피해·쿨·사거리** (C19) | `Weapons.csv` — 드래곤 `9\|13\|18\|25\|34` / 쿨 `1.4→0.8` / `Range 7→9`, 문어 `7\|10\|14\|19\|26` / 쿨 `1.2→0.8` / `Range 1.8→2.6` | **감으로 넣은 자리표시다.** 근거는 형제 무기와의 비교뿐이다 — 드래곤은 Fireball 의 **피해 절반·쿨 60%**(대상당 Lv5 42.5 DPS = Fireball 56.7 의 75%), 문어는 대상당 Lv5 **32.5 DPS** 인데 D13 실측 후리기당 **3.1마리**를 곱하면 약 100 DPS. **무엇을 보나:** ① 소환수만 들고도 **혼자 굴러가나**(소환사 컨셉이라 굴러가야 한다) ② 반대로 **플레이어가 할 일이 없어지나**. 🔴 **드래곤 `Range`(7→9)를 먼저 본다** — 화면 세로 절반이 6 이라 9 는 거의 화면 전체다. 포대처럼 느껴지면 5~6 으로 내린다 |

**화살 PPU 환산표** *(D9 실측 기준. 그림을 다시 그리지 않는다 — 임포트 설정 한 곳이다)*

| 원하는 것 | PPU | 길이 |
|---|---:|---:|
| 25% 크게 | 410 | 0.488 |
| **지금** | **512** | **0.391** |
| 25% 작게 | 640 | 0.313 |
| 절반 | 1024 | 0.195 |

> ⚠️ **`sprite.bounds.size` 와 실제 그림 크기는 다르다** (D9 에서 걸렸다).
> Single 스프라이트의 `bounds` 는 **텍스처 전체**를 쓴다 — 화살은 `256/512 = 0.500` 이 나온다.
> 눈에 보이는 크기는 **알파 bbox** 기준 `200/512 = 0.391` 이다.
> 다음에 판정 기준을 쓸 때 **어느 쪽 숫자인지 같이 적을 것.**
>
> 🔴 재임포트는 에디터라 **DEV 요청**이다. `.png.meta` 가 CONTENT 소유 경로에 있어도 마찬가지다.

#### 픽업 드랍 확률 6종 + 행운 (C23 / D20, 2026-08-31 배선 완료)

사용자 지시(2026-08-31): *"낮은 확률 1~3%대로 픽업 아이템 뜨게"* + *"패시브 행운 추가해서 해당 확률 올릴수 있게"*.
**A(각 1~3%) / B(합쳐서 1~3%) / C(종별 차등)** 중 사용자가 **C** 를 골랐다.

> 🔴 **이 표의 숫자는 전부 잠정이다. 사용자가 그렇게 정했다** —
> *"현재 라운드별 몬스터는 늘릴예정이야 그니까 수치는 추후 플레이 보고 조정할거야"* (2026-08-31).
> 아래 **"몬스터가 늘면 어떻게 되나"** 절을 반드시 같이 볼 것.

**고치는 곳:** `SceneWiring.csv` 의 **`ExperienceManager,pickupChances` 한 줄**(6값을 `|` 로 구분) ·
`Passives.csv` 의 `BonusLuck` 열. 고친 뒤 **CSV Import 가 필요하다**(DEV 요청).

> ℹ️ **`magnetDropChance` 는 없어졌다.** 자석 하나만 떨구던 구조가 6종 드랍표로 바뀌면서
> `magnetPrefab` 과 함께 C# 필드가 사라졌고, `SceneWiring.csv` 의 두 줄도 `C25` 에서 지웠다.
>
> 🔴 **`pickupPrefabs` 와 `pickupChances` 는 순서와 길이가 서로 맞아야 한다.**
> 코드가 **짧은 쪽 길이로 잘라 쓰므로 어긋나도 예외가 안 난다 — 조용히 틀린다.**
> 순서는 아래 표 그대로: Gold / Heal / Haste / Bomb / Magnet / Invincible.

**실측으로 확인된 것 (D20)** — 위 6값은 **하나도 안 바뀌고** 그대로 코드에 들어갔다.
드랍 판정은 **적 1마리당 굴림 1번**이라 **6.7 % 가 문자 그대로 드랍률**이고
(6종을 따로 굴렸으면 실제는 6.5 % 로 어긋났다), 행운은 **종별로 각각** 곱해져
**6종 비율(= C 안)이 행운과 무관하게 유지된다.** 5만 회 × 2 시행: 기본 **6.688 %** ·
행운 Lv5 **13.368 %** (= **2.001배**, 기대 6.7 / 13.4).
⇒ 아래 *"스폰 수가 `k` 배면 6값을 전부 `1/k` 배"* 규칙은 **그대로 유효하다.**

| 픽업 | 확률 | 왜 이 자리인가 | 런당 기대 개수 |
|---|---:|---|---:|
| **Gold** | **2.5 %** | 먹어도 판이 안 바뀐다. 자주 나와야 "줍는 맛"이 된다 | 약 13개 |
| **Heal** | **1.5 %** | 이득이 크지만 **체력이 안 깎였으면 가치가 0** 이라 자기 조절이 된다 | 약 8개 |
| **Haste** | **1.0 %** | 8초짜리 버프. 세지만 **끝난다** | 약 5개 |
| **Bomb** | **0.7 %** | 화면을 지운다. 위기 탈출 카드라 **아껴져야** 한다 | 약 4개 |
| **Magnet** | **0.6 %** *(지금 값 유지)* | 🔴 **일부러 안 건드렸다** — 이미 게임에 있는 유일한 픽업이라 **대조군**이다. 새 5종이 이것보다 흔한지 드문지로 체감을 잰다 | 약 3개 |
| **Invincible** | **0.4 %** | 🔴 **가장 낮다.** 3초라도 난이도가 통째로 사라지는 구간이다. **런에 2번**이 목표 | 약 2개 |
| **합계** | **6.7 %** | | **약 35개** |

**근거 — "웨이브당 수백 마리"는 사실이 아니었다.** `ExperienceManager` 주석의 그 문장이
자석 0.6% 의 근거인데, `Waves.csv` 를 실제로 세어 보면 훨씬 적다.

| | 실제 스폰 수 |
|---|---:|
| `Normal1` / `Normal2` / `Normal3` | **65** / **82** / **104** |
| `Elite1` / `Elite2` | **66** / **72** |
| `Boss1` | **18** |

*(계산: `Enemy*N@간격:시작-끝` 항목별로 `min(N, ⌊(끝−시작)÷간격⌋+1)`. 항목들은 병렬로 돈다.)*

10층 런에서 전투 층은 **층0(항상 Normal) + 층1~8 중 70 %(Normal 50 + Elite 20) + 보스층** =
기대 **7.6개** ⇒ **런당 약 530마리**. 즉 위 6.7% 는 **런에 35개 · 전투 층 하나에 5~6개**,
층이 60~90초니 **약 15초에 하나**다. 바닥이 덮이지도, 아무것도 안 나오지도 않는 간격이다.

> ⚠️ **530 은 상한이 아니라 기대값이다.** ① 전투 노드만 골라 가면 8층 전부 전투가 될 수 있어 **900 근처**까지 오른다.
> ② 반대로 `MaxAlive` 40 에 걸려 웨이브가 끝나 버리면 위 표보다 **적게** 나온다 — **느리게 죽일수록 적이 덜 나온다.**
> 즉 드랍은 이미 **스스로 제동이 걸린다.** 잘 굴러갈 때만 많이 떨어진다

##### 🔴 몬스터가 늘면 — 표를 통째로 나눈다

사용자가 라운드별 몬스터를 늘릴 예정이다. **드랍은 처치 수에 정비례**하므로
`Waves.csv` 의 스폰 수를 `k` 배 하면 위 6개 확률을 **전부 `1/k` 배** 해야 지금 간격이 유지된다.

🔑 **지키는 값은 확률이 아니라 이 두 개다** — 숫자를 다시 잡을 때 이걸 기준으로 잡는다.

| 앵커 | 목표 | 왜 이게 기준인가 |
|---|---|---|
| **픽업 하나 사이의 간격** | **약 15초** | 화면이 덮이는지 / 아무것도 안 나오는지는 **개수가 아니라 간격**으로 느껴진다 |
| **Invincible 이 나오는 횟수** | **런에 2번** | 이게 넘어가면 난이도 자체가 없어진다. **6종 중 이것만 절대 기준**이다 |

⇒ 스폰 수가 2배가 되면 6값은 `1.25 / 0.75 / 0.5 / 0.35 / 0.3 / 0.2 %` 다. **6개를 같이 움직인다** —
하나만 고치면 종별 비율(= C 안 그 자체)이 깨진다.

##### 지속시간 · 피해량 — 로그로는 판정이 안 된다

| 무엇 | 값 | 무엇을 보나 · 이상하면 |
|---|---|---|
| **무적 지속** | **3초** *(사용자 선택)* | 🔴 **"위기 탈출" 이지 "무쌍 시간" 이 아니다.** 3초는 **적 무리를 뚫고 나오는 데 딱 걸리는 시간**(이동속도 5 → 15유닛, 화면 세로 12보다 조금 길다). **보는 것:** 먹고 나서 *도망쳤나 vs 싸웠나*. 싸웠으면 긴 거다(→ 2초). 먹었는데 그냥 죽으면 짧은 거다(→ 4~5초). ⚠️ **5초는 넘기지 말 것** — 0.4% × 5초면 화면이 통째로 안전지대가 되는 구간이 생긴다 |
| **공속 지속** | **8초** *(사용자 선택)* | 무적과 달리 **길어야 의미가 있다** — 짧으면 버프가 켜진 줄도 모르고 끝난다. **보는 것:** 버프가 켜졌다는 걸 **소리·화면으로 알아채나.** 못 알아채면 그건 시간 문제가 아니라 **연출이 없는 것**이다(DEV 에 알릴 것) |
| **공속 세기** | **쿨다운 ×0.5** (= `BonusAttackSpeed` 기준 **−0.5**) | ⚠️ **DEV 가 안 물었지만 없으면 못 만든다.** 근거: `AttackSpeed` 패시브 Lv5 가 −0.3 이다. **만렙 패시브보다 세야** 8초짜리 픽업이 픽업답다. **보는 것:** 켜진 8초 동안 **화면이 눈에 띄게 시끄러워지나.** 안 그러면 −0.6~−0.7 |
| **폭탄 피해** | **200** | 🔴 **잡몹 전멸 · 오거 생존**이 기준선이다. 적 체력은 Slime 45 · Goblin 30 · Zombie 70 · Wolf 40 · **Demon 160** · **Ogre 220** — 200 이면 Demon(방어 4 → 196)까지 죽고 Ogre 는 26 남는다. **보는 것:** 먹었을 때 *"싹 지웠다"* 가 보이나. Demon 이 살아남으면 그게 제일 김빠진다(→ 250) |
| **폭탄 반경** | **10** | `ortho 6` 이라 화면 세로 반이 6 · 가로 반이 10.67 이다. 반경 10 은 **세로는 전부 · 가로는 94 %** — **네 모서리만 살아남는다.** 🔴 **보는 것은 피해가 아니라 폭발 그림의 크기다** — 안 닿은 것처럼 보이는데 죽거나, 닿았는데 안 죽으면 그건 반경이 아니라 **그림과 판정이 어긋난 것**이다(장판에서 이미 한 번 겪었다 — `spriteRadiusAtScaleOne` 줄). 모서리 생존이 버그처럼 보이면 **12.3**(= 화면 대각선) |
| **Gold 픽업 금액** | **15G** *(`WorldPickup.cs:66` — 프리팹 필드, CSV 아님)* | ⚠️ **DEV 가 안 물었지만 없으면 못 만든다.** 런당 13개 = 약 200G 로, 런 수입 ≈1,100G 의 **18 %** — **비율은 지금도 맞다.** 🔴 **그런데 근거로 적었던 문장은 `C27` 이 깨뜨렸다** — "상점 아이템이 5~12G 라 한 개는 확실히 산다"고 잡은 값인데 이제 **가장 싼 게 40G** 라 픽업 하나로는 아무것도 못 산다. **보는 것:** 금화를 주웠을 때 *"샀다"* 가 아니라 *"모았다"* 로 느껴지는데, 그게 **밋밋한가.** 밋밋하면 **40G**(= 가장 싼 아이템 한 개)로 올린다 — 대신 런당 520G 이 되어 수입의 **47 %** 가 되므로 그때는 `CurrencyDrop` 을 같이 내려야 한다. ℹ️ 적 하나가 떨구는 `CurrencyDrop` 은 1~8 이다 · 🔴 **프리팹 필드라 바꾸려면 DEV 요청이다** |
| 🔴 **힐 회복량** | **30** *(`SceneWiring.csv` `ExperienceManager,healPickupAmount`)* | 🔴 **DEV 가 지어낸 값이고 내가 그대로 뒀다** — 이 표가 힐 **확률만** 주고 회복량을 안 줬다. 식당이 뱉는 힐은 `RestaurantBuilding` 이 **건물 레벨**로 정하는데(`Buildings.csv` `Output` = **15\|25\|40\|60\|85**) 드랍에는 건물이 없어 정할 사람이 없었다. 기준 최대체력 **100** 기준 **30 %** = 오거 접촉(25) 한 대를 되돌리는 양이다. **보는 것:** 먹었을 때 *"살았다"* 가 되나 *"먹으나 마나"* 가 되나. 🔴 **같이 볼 것 — 식당이 초라해 보이나.** 30 은 식당 **Lv1(15)·Lv2(25)보다 크다.** 건물 슬롯을 쓴 투자가 랜덤 드랍보다 못해 보이면 값이 아니라 **순서가 틀린 것**이다 → 드랍을 **15**(= 식당 Lv1) 로 내린다. 반대로 먹은 줄도 모르겠으면 **40** |
| ⚙️ **`minAttackSpeed`** | **0.1** *(`PlayerStats.cs:21·260` — 코드 상수, CSV 아님)* | ⚠️ **체감값이 아니라 안전장치다.** 공속 픽업(−0.5)이 들어오면서 쿨다운이 0 이하로 갈 수 있게 됐고, `WeaponBase.cs:54` 가 쿨다운에 `Final.AttackSpeed` 를 **곱하므로** 0 이면 한 프레임에 무한 발사다. 지금 최악 누적은 `Aegis −0.05` + `AttackSpeed` Lv5 `−0.30` + 픽업 `−0.50` = **0.15** 라 **하한에 아직 안 닿는다.** **보는 것:** 공속 8초 동안 발사가 **부자연스럽게 끊기나.** 끊기면 하한에 걸린 것이고, 그때 고칠 것은 **하한이 아니라 위의 공속 세기(−0.5)** 다 — 하한을 내리면 무한 발사 쪽으로 다시 열린다. 🔴 **바꾸려면 코드라 DEV 요청이다** · 🔴 **어쎄신(`C26`)이 들어오면 이 "아직 안 닿는다"가 거짓이 된다** — `−0.25` 가 더해져 `−0.10` 이라 **하한에 실제로 물린다.** 어쎄신 값을 `−0.15` 로 낮춰도 `0.00` 이라 똑같이 물리니 **공속을 주는 직업이 하나라도 생기면 피할 수 없다** ⇒ 하한이 안전장치에서 **밸런스 숫자**로 바뀐다 ([`DESIGN_CLASSES.md`](DESIGN_CLASSES.md) §7-E) |
| **행운 5값** | `Passives.csv` `BonusLuck` **0.15\|0.3\|0.5\|0.7\|1** | 곡선을 `GoldGain` 과 **똑같이** 맞췄다 — 둘 다 "이미 떨어지는 걸 더 받는" 패시브라 플레이어가 세기를 짐작할 수 있어야 한다. Lv5 = **정확히 2배**(6.7 %→13.4 %, 층당 5.6개→11.2개). **보는 것:** 행운을 5까지 올린 판이 **눈에 띄게 다른가.** 아무 차이가 없으면 그건 값이 작은 게 아니라 **픽업 자체가 약한 것**이다 — 먼저 위 6종 효과를 의심할 것. 🔴 **Invincible 도 같이 2배(런 2→4번)가 된다** — 그게 유일한 위험이다. 그러면 `BonusLuck` 을 깎지 말고 **Invincible 기본 확률만** 0.3 % 로 내린다 |

### 막힘 — 기능이 없어서 판정 자체가 불가능하다

**수치 문제가 아니다.** 여기 있는 동안은 아무리 플레이해도 답이 안 나온다.

| 대상 | 현재 | 무엇이 먼저 필요한가 |
|---|---|---|
| 🔴 **층별 난이도** | **없음** | 1층과 10층이 동일하다. 방향은 확정(전역 배율 먼저) → [`TODO.md`](TODO.md) §3. **배율 기능이 들어오기 전엔 "10층이 쉽다"밖에 말할 게 없다** |
| **런 골드 배수구** | 상점 **1.2회** × **3칸** | ✅ 재화 분리(`D25`)·가격 재산정(`C27`)은 끝났다. 남은 건 **쓸 곳이 모자란 것**이다 — 수입 ≈1,100G 대 최대 지출 ≈400G. 판정과 고치는 순서는 **§2-H** 에 있다. 배수구를 늘리려면 `Economy.csv` `weightShop`·`shopSlotCount` 이고 **둘 다 CSV 라 코드는 필요 없다** |

---

## 4. 플레이 세션 기록

> 🔑 **입력지는 [`PLAYTEST.md`](PLAYTEST.md) 다** (`D58` 신설 · DEV 소유).
> 사용자가 거기 `[ ]` 를 채우면 **답이 이 절로 옮겨 온다.**
> ⇒ **거기는 질문, 여기는 기록**이다. 같은 항목을 양쪽에 적어 두지 말 것 —
> 물어보는 문장이 두 군데 있으면 **둘이 어긋난다.**

한 번 플레이할 때마다 아래 형식으로 **추가**한다. 고친 수치는 커밋에도 남지만,
**"왜 그 값으로 정했나"** 는 여기에만 남는다.

```
### YYYY-MM-DD — 직업 / 도달 층 / 결과

무엇을 보려고 했나:
느낀 것:
바꾼 값:  A → B  (파일)
판단 보류:
```

> 특히 **승급까지 몇 분 걸렸는지**를 반드시 적을 것.
> [`TODO.md`](TODO.md) §2 의 미결정 1건(**T2 를 3개만 둘지 / T4 를 만들지**)이
> 이 값 하나로 결정된다. 한 런에 T3 까지 못 간다면 T4 를 만들 의미가 없다.

### 2026-09-04 — **계측 세션 (사람 플레이 아님)** / `D58` / 17개 중 **6개 닫힘**

**무엇을 보려고 했나:** `C40`(요청-32)이 넘긴 17개. 그중 **숫자로 답이 나오는 것**부터.

**닫힌 것 6개**

| # | 무엇 | 결과 | 판정 |
|---|---|---|---|
| 0-a | vSync | 🔴 **켜져 있었다** (`vSyncCount 1` · 모니터 144 Hz). 측정 동안 껐다 | 경고가 맞았다 |
| 2-a | 🔴 **9층 294마리 프레임** | **111.3 fps (8.99 ms)** · 9.61초 내내 294 유지 | ✅ 통과선 60 |
| 2-c | 층별 바닥 | `dL 0.0727` (`D53`) | ✅ 통과선 0.04 |
| 2-g | 깊은 층 얼룩 | 0.0211 | 🟡 그대로 두기로 결정 (§D) |
| 2-h | 보스 | HP 바 · `BONECALLER` · ★★ · `BossBrain` · HP **2926**(=1540 × 1.90) | ✅ |
| 3-a | 🔴 **내 탄 ↔ 적 탄** | 색상각 **170도** · 밝기 차 **0.383** | ✅ **두 축이 동시에 갈린다** |

**느낀 것 (숫자에서 읽은 것)**

- 🟢 **`layerSpawnGrowth 0.14` 를 안 내려도 된다.** 294마리에서 111 fps 면 여유가 크다.
  `C38` 이 계산으로 "400 아래면 안전"이라 한 게 실측으로 확인됐다
- 🔑 **3-a 가 `C33` 의 설계 의도를 숫자로 증명했다.** 색상만 갈랐으면 색약에서 무너지는데
  **밝기 차 0.383 이 같이 있어서** 색을 못 봐도 갈린다. 모양(방향성)까지 셋째 축이다
- 🔴 **`EditorLoop` 는 못 쟀는데 필요 없어졌다** — 111 fps 는 **에디터 오버헤드가 포함된** 값이고
  빌드에는 그게 없으므로 **빌드는 더 빠를 수만 있다.** 통과 판정이 안 뒤집힌다

**바꾼 값:** 없음 (전부 통과)

**판단 보류 / 못 한 것:**

- 🔴 **`1-a` (남은 런 골드 `M`) 는 자동화가 안 된다.** 레벨업 패널이 `timeScale 0` 으로 멈춰 세우고,
  **그 카드 선택이 빌드를 → 빌드가 처치 속도를 → 처치 속도가 골드를 정한다.**
  자동으로 고르면 `M` 이 아니라 **"아무 M"** 이 나온다 → 아래 예측만 남긴다
- **나머지 9개는 사람이 해야 한다** — `1-b`~`1-e` · `2-b` · `2-d` · `2-e` · `2-f` · `3-b`~`3-d`.
  전부 *"느낌이 맞나"* 라 로그로 못 만든다

> 🔴 **`D58` 의 "품질이 Medium 이다"는 틀렸다** (`C41` 확인).
> `ProjectSettings/QualitySettings.asset` 이 `m_CurrentQuality: 5` · `Standalone: 5` = **Ultra** 다.
> 다만 `vSyncCount` 가 레벨 **2~5 전부 `1`** 이라 **vSync 가 켜져 있었다는 결론은 그대로**다.

---

_(사람이 하는 판 9개는 아직 없다 — 위 §1-9 의 셋이 우선이다)_
