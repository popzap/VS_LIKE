# 병렬 작업 현황판

> 세션을 열면 **가장 먼저 읽고**, 착수 전에 내 줄을 등록한다.
> 규칙 전문은 [`SESSION_PROMPT.md`](SESSION_PROMPT.md).

---

## 0. 세션과 소유 경로

| 세션 | 접두어 | 하는 일 | 소유 경로 (쓰기 가능) |
|---|---|---|---|
| **DEV** | `D` | 개발·뼈대. 코드 · 씬 배선 · **Unity 에디터 전담** · 실플레이 검증 | `Assets/Scripts/**` · `Assets/Editor/` · `Assets/Scenes/` · `Assets/Prefabs/` · `Assets/GameObjects/` · `Assets/Game/*Data/` (SO) · `Assets/Settings/` · `ProjectSettings/` · `Docs/DESIGN_EVENTS.md` |
| **CONTENT** | `C` | 코드 외적. 그림 · 소리 · 애니메이션 소스 · 수치 · 문서 | `_Incoming/` · `Assets/Game/{Sprites,ICON,Tiles,Materials,Shaders,Audio}` · `Assets/Game/Balance/*.csv` · `Assets/Fonts/` · `Tools/Art/` · `Tools/Audio/` · `Docs/BALANCE.md` · `Docs/TUNING.md` · `Docs/DESIGN_CLASSES.md` · `Docs/DESIGN_ART.md` |

**소유하지 않은 경로는 읽기만 한다.** 고쳐야 하면 `REQ/<대상>.md` 에 요청을 남긴다.

공용(아무도 직접 안 씀): `Docs/SETUP_STATUS.md` · `Docs/TODO.md` · `Docs/ROADMAP.md` · `CLAUDE.md`

> 🔴 **`Docs/PLAYTEST.md` 는 사용자 소유다** (D59). 세션은 **질문만** 넣고 **답은 사용자가 적는다**.
> 세션이 답을 대신 채우면 검증이 아니라 창작이 된다. 답이 채워지면 DEV 가 `TUNING.md` 로 옮긴다.
→ **DEV 세션이 통합해서 반영한다** (검증까지 끝난 걸 아는 유일한 세션이므로).

> ℹ️ **`_Incoming/` 은 저장소 최상단이다 — `Assets/` 밖이다.**
> CONTENT 가 만든 png·wav 원본을 여기 둔다. Unity 가 아예 보지 않으므로
> DEV 가 플레이 중이어도 임포트가 끼어들지 않는다. DEV 가 `Assets/` 로 옮기고 배선한 뒤 지운다.

---

## 1. 지금 돌고 있는 작업

| 세션 | 이슈 | 만지는 경로 | 시작 | 상태 |
|---|---|---|---|---|
| DEV | `D22` | `Docs/DESIGN_EVENTS.md`(신설) · `Docs/ROADMAP.md` | 2026-08-31 | 🅿️ **보류(사용자)** — "이벤트 관련은 나중에". 문서는 **다 썼다** (`E1 함정방` 상세 + `E2`~`E6` 자리표). 🔴 **코드 변경 0.** 재개 조건 = **§7 결정 6건.** ROADMAP §0·§7 에 "이벤트 노드가 비어 있다" 갭 등재 완료 |
| DEV | `D38` | `Docs/**` (문서만) | 2026-09-04 | `완료` — **`C32` 순서를 받았다** (사용자 결정 A). 요청-27(보스 그림)을 **보류로 내리고** `요청-28` 로 회신 — `DESIGN_ART.md` §6 의 1~5번은 **CONTENT 가 만들고 DEV 는 배선만**. 🔴 §7 정리 후보 3개를 guid 로 실측: **셋 다 참조가 있다.** 특히 `Exp_Orb.gif` 는 `ExpDrop.cs` 가 안 덮어쓰므로 **지우면 구슬이 안 보인다** |
| DEV | `D39` | `Assets/Scripts/Building/BuildingManager.cs` · `Assets/Scripts/UI/HUDManager.cs` · `Docs/Parallel/BUGS.md` · `Docs/{TODO,SETUP_STATUS}.md` | 2026-09-04 | `완료` — **`B3` 닫음 (C안)**. 대기열을 **신규 구간 + 증설 구간**으로 나눴다. 🔑 원안의 "맨 앞에 넣는다"는 **나중 해금이 먼저 해금을 추월**해서 안 썼다 — **신규 구간의 끝**에 넣는다. 판정 2/2 PASS (재현 절차 + `LockBuilding` 장부) |
| DEV | `D40` | `Assets/Game/Sprites/{Projectiles,UI}/*.meta` · `Assets/Prefabs/*.prefab` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **요청-25 (C33)**: 그림 5장 임포트 + 탄환 2종 교체 + **9-slice 배선 49곳**. 판정 **6/6**. 🔑 `spriteBorder` 만으로는 안 됐다 — 38×38 버튼에 20+20px 테두리라 `pixelsPerUnitMultiplier` 규칙을 세웠다. 🟡 CONTENT 의 `50px` 전제 정정(실측 41px). 🔴 곁일로 `B11`(상점 대사 한글 빈칸) 발견·등재 |




> ✅ **`C31` 끝났다 — CONTENT 가 지웠다** (2026-09-04). **`D35`·`D36` 인계 반영 · 요청 0건.**
> 둘 다 내 소유 경로를 건드렸는데 **고칠 게 없었다** — `D35` 의 승급 4종 임포트 수정(`B7`)으로
> 직업 스프라이트 **20장이 PPU·필터·압축·알파 전부 일치**하고(실측), `D36` 의 폰트 머티리얼도 파생본이라 안전하다.
> 대신 **판정 항목 4개를 등재했다** → [`TUNING.md`](../TUNING.md)
> §G **`EditorLoop` 가 프레임의 45 %** — 에디터 수치로 게임 성능을 판정하면 안 된다 ·
> §G 무기 렉은 `D35` 가 **재현 못 한 것**이지 고쳐진 게 아니다 ·
> §I-3 `D35` 의 `ExpDrop 0` 은 **구슬이 생길 조건이 아니었던 것**이라 `D29` 의 증거가 아니다 ·
> §D 안내 문구 외곽선 0.22 / 그림자 (0.6,−0.6) — 🔴 **원본 머티리얼은 텍스트 75개가 공유하므로 손대지 말 것**.
>



| CONTENT | `C42` | `Docs/PLAYTEST.md`(⚠️ **DEV 소유 · 사용자 지시로 직접 씀**) · `Docs/{BALANCE,TUNING}.md` | 2026-09-04 | `대기(사용자 플레이)` — **`PLAYTEST.md` 에 숫자 3개 추가**(사용자 지시 *"필수적인 것만"*). 기존 13개는 안 건드렸다. **D-1 승급까지 몇 분**(`TODO` §2 미결정 **2건**이 이 숫자만 기다린다) · **D-2 검만으로 Demon 을 잡을 수 있나** · **D-3 무기 렉이 실제로 나는가 + 그때 뭘 하고 있었나**(`PERF.md` 유일한 미해결 · `D35` 가 하네스로 재현 실패). 🔴 **그러다 내 문서의 숫자가 틀린 걸 찾았다** — 여러 곳에 쓴 *"무기 사거리 3.5~6"* 이 사실이 아니다(**검 2.0~2.5** · 활 14~18 · 총 11~15). `SETUP_STATUS.md:1586` 옛 메모를 확인 없이 옮긴 것이고 `BALANCE.md`·`TUNING.md` 두 곳을 정정했다. 🟡 **`SlamWindup 1.15` 결론은 안 바뀐다**(회피 계산은 사거리와 무관) — 틀린 건 곁들인 설명이다 → [`REQ/DEV.md`](REQ/DEV.md) **요청-34** · [`DONE/C42.md`](DONE/C42.md) |



| CONTENT | `C46` | `Assets/Game/Sprites/Pickups/Swift.png.meta` · `REQ/DEV.md` | 2026-09-05 | `대기(REQ→DEV)` — **`Pickup_Swift` 배선 단독 요청**(요청-36·37 안에 두 번 적었는데 큰 요청에 묻혔다 — 🔑 **일의 크기와 요청의 크기를 맞춰야 한다**). 🔴 **요청을 쓰다 임포트 설정 오류를 찾았다** — `Swift.png.meta` 가 Unity **기본값**(`PPU 100`·Bilinear·Multiple·압축)이라 그대로 꽂았으면 **10.24유닛 = 20배 크기의 흐릿한 아이콘**이 떴다. 내 잘못인데 화면에선 *"이상한 그림을 줬다"* 로 보인다. ✅ 네 항목 다 고쳐 Haste 와 동일하게 맞췄다(0.500유닛). 🔑 **새 애셋은 `.meta` 까지가 납품물** — `C28`(걷기 PPU)·`C33`(9-slice border)에 이어 **세 번째**고, 앞의 둘은 *적어서* 막았는데 이번엔 **내 소유 경로라 고쳐서** 막았다. **적는 것보다 맞추는 게 싸다** → [`REQ/DEV.md`](REQ/DEV.md) **요청-38** · [`DONE/C46.md`](DONE/C46.md) |

> 상태값: `진행중` · `대기(REQ→DEV)` · `검증대기` · `막힘(B1)`
> 끝나면 **자기 줄을 지운다.**
>
> ✅ **`C43`·`C44`·`C45` 끝났다 — CONTENT 가 지웠다** (2026-09-05). 회신 `D78` 통과.
> `C43` 플레이 답 회수(값 3개) · `C44` Ogre 배치 + Swift 그림 + **타일 경보** · `C45` 확인.
>
> 🔑 **`C44` 의 경보가 맞았다** — 타일 `PPU 2048` 은 커밋됐는데 씬 `Grid.Cell Size` 가 `1,1` 이라
> **바닥이 실제로 깨져 있었다**(칸 채움률 25 %). `D78` 이 고치면서 **어긋난 자리를 하나 더 찾았다** —
> `GroundTiler.EnsureBuffer` 가 월드 유닛을 칸 수로 썼는데, **셀이 1.0 인 동안은 두 값이 우연히 같아**
> 안 보였다. ⇒ **한 값이 두 뜻으로 쓰이면 그 두 뜻이 갈라지기 전까지 안 보인다.**
>
> 🔴 **`C45` 에서 내 문서가 낡은 걸 찾았다** — `D69` 가 상점을 **고정가 → 층·레벨 스케일**로 바꿔
> `Items.csv` 의 `ShopPrice` 가 **정가에서 기준가**가 됐는데 `BALANCE.md` 는 옛 뜻으로 남아 있었다.
> 🔑 그리고 **`priceBaseMult 0.6` 이 내가 준비해 둔 답보다 나았다** — 나는 *"패시브만 내린다"* 였는데
> **초반이 비싼 건 품목 문제가 아니라 시점 문제**였다. 내 답을 버리고 그쪽으로 갈아 끼웠다.
>
> 🟡 환급 차익(0층 42 구매 → 8층 69 환급 = **+27G**, 잉여의 7 %)은 **재고 나서 안 고쳤다.**
> 🔴 남은 건 `Pickup_Swift.prefab` 의 `m_Sprite` **한 줄**뿐이다.
>
> 🎉 **`C40`·`C41` 끝났다 — `TUNING.md` §4 에 플레이 기록이 처음으로 생겼다** (2026-09-04).
> `C40` 이 미체크 **111개 중 17개**만 골라 **2판 프로토콜**로 넘겼고(판 수는 `DevPanel` 에 층 점프가
> 없다는 사실이 정했다), `D58` 이 그중 **숫자로 닫히는 6개**를 닫았다.
> **9층 294마리 111.3 fps** · 탄 구분 **색상각 170도 + 밝기차 0.383** · vSync 경고가 실제로 걸렸다.
> ⇒ `C38` 의 `layerSpawnGrowth 0.14` 를 **안 내려도 된다**(계산이 실측으로 확인됐다).
>
> 🔴 **`D58` 의 "품질이 Medium" 은 확인해 보니 아니었다** — `QualitySettings.asset` 이
> `m_CurrentQuality: 5` · `Standalone: 5` 로 **둘 다 Ultra** 다. 메모를 안 고쳤다.
> 🟢 다만 `vSyncCount` 가 레벨 2~5 전부 `1` 이라 **vSync 결론은 그대로**다.
> 🔑 **`C38`(최댓값 vs 평균) · `C39`(밝기 공식) 에 이어 세 번째** — 매번 결론은 같고 전제가 달랐다.
>
> 🔑 **이번에 제일 값진 것: `M`(남은 런 골드)은 계측으로 대신할 수 없다.**
> 런은 레벨업마다 `timeScale 0` 으로 멈추고 **그 카드 선택이 빌드를→처치 속도를→골드를** 정한다.
> 자동으로 고르면 *"아무 M"* 이 나온다. ⇒ **사람이 해야 한다는 데 이유가 붙었다** (`TUNING.md` §1-9).
>
> 🔴 **남은 9개는 사용자 대기다** — 그중 `2-b`(폭발광 내려찍기 회피)가 가장 중요하다.
> `C30` 계산과 `D34` 대조군이 둘 다 **난전이 아니었다.**
>
> ✅ **`C37`·`C38`·`C39` 끝났다 — CONTENT 가 지웠다** (2026-09-04). 회신 `D52`·`D53` 둘 다 통과.
> `C37` 바닥 타일 층별 색 분리 — **dL 0.0069 → 0.0727(10.5배) · 겹침 0 %** ·
> `C38` 층별 난이도 계수(다섯 중 둘만) · `C39` 확인 + 적 계수 14개 등재.
>
> 🔑 **`C37` 은 "선택지 셋이 다 틀렸다"에서 시작했다** — DEV 가 A(PNG 다시 칠하기)·B(새 타일)·C(포기)를 줬는데
> 타일 원본 밝기 폭은 **0.175** 인데 `I-28` 평준화가 10종을 **한 점**으로 끌어내리고 있었다.
> **손잡이가 PNG 가 아니었다.** `I-28`(층 안 퀼트)과 `D49`(층 간 구분)는 *"언제 같이 깔리나"* 를 넣으면 안 부딪힌다.
>
> 🔑 **두 방법이 서로를 검증했다** — 내가 타일 색 공간에서 계산한 퀼트 편차 **0.0138** 과
> `D53` 이 화면 32px 블록으로 잰 **0.0134** 가 0.0004 차이다. 한쪽만 있었으면 둘 다 "그럴듯한 값"이었다.
>
> 🔴 **`D52` 가 찾은 함정을 `BALANCE.md` 에 박았다** — `Economy.csv` 값이 C# 기본값과 같으면
> **씬에 아무 흔적도 안 남아** 씬 grep 으로는 Import 판정이 안 된다.
> (`WaveManager.prefab` 이 최초 커밋 뒤 재저장된 적이 없어 새 필드가 프리팹에 없다.)
> ⇒ *"게임이 그 값으로 도나"* 는 **런타임뿐**이라 앞으로 DEV 에 물어본다.
>
> ✅ **`C32`~`C36` 끝났다 — CONTENT 가 지웠다** (2026-09-04). **`DESIGN_ART.md` §6 의 1~6번이 다 나갔다.**
> `C32` 공백 분석(씬 `Image` 60개 중 **59개가 단색**이었다) · `C33` 탄환 2 + UI 프레임 3 ·
> `C34` 강화 아이콘 + 승급/레벨업 연출 · `C35` 상점 주인·배경 요청 · `C36` 검수.
> DEV 회신 `D40`(6/6) · `D41`+`D43`(7/7) · `D42`(7/7) — **재실측까지 숫자가 맞는다.**
>
> 🔑 **만든 것보다 "안 만든 것"이 많았다** — 강화 아이콘 7장 중 **6장은 패시브 재사용**,
> 승급·레벨업은 **흰색 2장을 틴트로** 갈랐다. 겹치는 의미를 찾는 게 그리는 것보다 싸고 결과도 일관된다.
>
> 🔴 **내가 틀린 것 셋을 기록으로 남겼다** — ① `Bullet.png` 을 50px 로 봤는데 rect 는 **41px**
> (결론은 맞고 이유가 틀렸다 — 판정이 스프라이트와 무관하다) ② *"`goblin.png`·`Exp_Orb.gif` 는 안 쓰인다"*
> 가 틀렸다(**각각 1곳 참조** — `Exp_Orb` 를 지웠으면 구슬이 통째로 안 보였다)
> ③ 9-slice 를 넘기며 **테두리 px 만 주고 "어디에 얹히나"를 안 줘서** 38px 버튼에 40px 테두리가 들어갈 뻔했다.
> ⇒ **셋 다 "재기 전에 적어서" 생겼다.**
>
> ➕ `I-41` 이 상점 주인에서 **또 났고**(프롬프트에 명시했는데도 알파 전부 255)
> `C28` 이 만들어 둔 함정 표가 막았다. 🔑 **"투명하게 그려 줘"는 안 통한다.**
>
> ✅ **`C27`·`C28`·`C29`·`C30` 끝났다 — CONTENT 가 지웠다** (2026-09-03).
> **네 건 모두 DEV 회신까지 닫혔다** (`D28`·`D29`·`D33`·`D34`, 판정 5/5·5/5·6/6·6/6).
> `C27` 상점·해금 가격 재산정 · `C28` `D27` 성능 인계 반영 · `C29` 영구 강화 7종 · `C30` 보스 수치 + 예고 원.
>
> 🔑 **네 건 다 같은 모양이었다 — 요청받은 축으로는 답이 안 나왔다.**
> `C27` 은 가격이 아니라 **배수구**가 문제였고(그래서 `StageMapManager` 가중치를 CSV 로 꺼냈다),
> `C28` 은 수명이 아니라 **`ClearWave` 가 구슬을 안 치우는 것**이었고,
> `C29` 는 수치가 아니라 **카드 두 장이 게임이 못 하는 일을 약속하고 있던 것**이었고,
> `C30` 은 "플레이해 봐야 안다"가 **`radius ÷ windup` 나눗셈 한 번**이었다.
> ⇒ **값을 넣기 전에 그 값이 흘러가는 코드를 먼저 읽은 것**이 네 번 다 방향을 바꿨다.
>
> 🔴 **남은 것은 전부 "사람이 플레이해야 답이 나오는 것"이다** → [`TUNING.md`](../TUNING.md)
> §G(vSync 함정) · §H(런 골드 잔액 · 강화 30판) · §I(적 밀도 · `MaxAlive` 천장 400) · §J(보스 3항목).
>
> ✅ **`C6`·`C26` 끝났다 — CONTENT 가 지웠다** (2026-09-03). **6단계(직업 3종)까지 전부 닫혔다.**
> `D32` 가 Import 로 배선을 끝냈고 판정 **7/7 PASS** — 세 애셋의 `WalkFrames` 가 **16개**로 기존 7종과 같다(C29 실측).
> 🔑 **8월 31일부터 "그림이 먼저"라며 CSV 를 안 쓰고 버틴 게 이걸 지켰다** — 순서를 뒤집었으면
> 임포터가 **경고 없이** 빈 직업 3개를 만들고 새 직업이 기사 모습으로 나왔을 것이다(`I-57` 이 실제로 그랬다).
>
> ✅ **`D27` 끝났다 — DEV 가 지웠다** (2026-09-02, 50차 → [`SETUP_STATUS.md`](../SETUP_STATUS.md) 2-54).
> **성능을 처음으로 쟀다** — 시나리오 A/B × 적 130~800 · 40회 이상 → [`PERF.md`](../PERF.md)(신설).
> 🔑 **측정 전에 판정 기준을 못박았고**(§1~§5), 그중 **2개가 실패로 나와 낮추지 않고 실패로 남겼다.**
> **가설 11개를 숫자로 죽였다** — 적↔적 충돌은 매트릭스에서 이미 꺼져 있었고, `GC.Collect` 는 0.000 이고,
> 적만 800마리면 121 fps 이고, `OverlapCircleAll` 할당 8곳은 프레임의 1.3 % 이고, 투사체는 1~3개뿐이고,
> `Update()` 호출 단가는 0.139 µs 로 내 예측과 10배 차이였다.
> 🔴 **찾은 것 둘** — **`B8`**(이웃 12칸 무언 절단, 적 400에서 12~19 % · 800에서 58~71 %) ·
> **적 겹침의 원인은 밀도**(800에서 85 %가 0.5유닛 안. 버퍼를 키워도 84.6→85.1 % ⇒ **절단은 증상이었다**).
> 고친 것: `NeighborBufSize` 12→64 · `ExpDrop`/`WorldPickup` 의 씬 전체 순회 제거 · `DevPanel` 에 `Enemy Spawn`.
> 🔴 **스스로 무너뜨린 결론 3개** — "렉이 없다"(A에서만) · **`B9` 오진**(사용자 지적으로 `StageClearUI` 정상 정지임이 드러남) ·
> "무기 렉의 범인은 사망 부산물"(근거가 **열화된 세션 한 번**의 값이었다). 셋 다 **그럴듯한 설명을 찾자마자 검증을 건너뛴 것**이다.
> ⚠️ **툴 함정 둘을 새로 확인했다** — `Assets/Refresh` 는 **재컴파일을 보장하지 않고**,
> **Unity 컴파일 에러는 `Error` 가 아니라 `Log` 타입으로 온다**(`Types:["Error"]` 는 0건이었다).
> ✅ 계측 전량 제거(심볼 소멸까지 조회 확인) · **`SampleScene.unity` 한 줄도 안 바뀜** · 콘솔 에러 0.
> ➕ 곁가지: **건물 앞 `E` 승급이 촬영 중 처음으로 실제 조작 경로 검증됨**(외형 변화까지) — `TODO §1` 항목 닫힘.
> 🔴 **무기 렉의 원인은 미확정으로 남는다** — `BehaviourUpdate` 의 71~81 %(정정 후 0.2~0.7 ms/frame)가 미설명이다.
>
> ✅ **`D28` 끝났다 — DEV 가 지웠다** (2026-09-03, 51차 → [`SETUP_STATUS.md`](../SETUP_STATUS.md) 2-55).
> 요청-20 대로 **Import 1회**. 판정 **5/5 PASS** → [`DONE/D28.md`](DONE/D28.md).
> `Economy.csv 49/49` · `SceneWiring.csv 12/12` · `Items 28` · 실패 줄 0 ·
> `Sword.ShopPrice 8→70` · 씬 `ShopManager 3→25 / 1→15` · 해금 `30/15→150/75` · 레벨업 리롤 `1→10`.
> 🔑 **네가 no-op 으로 넣은 판정 ⑤ 가 판정 하나를 공짜로 더 만들었다** — 씬에 `StageMapManager`
> 오버라이드가 0개 생긴 건 *값이 같다* 와 ***임포터가 그 컴포넌트를 찾았다*** 를 동시에 뜻한다
> (못 찾았으면 `43/49` + `!`). ⇒ **`weightShop` 튜닝 경로가 부작용 없이 증명됐다.**
> ⚠️ 알아 둘 것 둘 — 필드가 `shopPrice` 가 아니라 **`ShopPrice`**(소문자로 조회하면 NRE) ·
> **Import 값은 프리팹이 아니라 씬 인스턴스 오버라이드로 들어간다**(`ShopManager.prefab` 은 여전히 `3/1`).
> ➕ **`D27` 인계를 마저 반영했다** — 새 함정 둘(`Assets/Refresh` 가 재컴파일을 보장하지 않는다 ·
> 컴파일 에러가 `Log` 타입으로 온다)이 `PERF.md` 에만 있고 **`CLAUDE.md` 에 없었다.** §4 에 2행 넣었다.
> 🔴 **품질 레벨 `0→5`(Ultra) 는 사용자 판단으로 유지하되 대가를 등재했다** — Ultra 는 `vSyncCount: 1` 이고
> 꺼 주던 `PerfHarness` 는 지워졌다. **에디터 플레이가 이제 144 Hz 에 고정된다** → `TODO.md` §1-B.
> `TUNING.md` §G 는 CONTENT 소유라 안 쓰고 [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-20** 으로 넘겼다.
> ℹ️ **요청-19 의 `진행중(3/6)` 상태 칸은 안 건드렸다** — 요청자 것이다.
> 반려가 stale 이라는 근거(`mtime`)는 요청-19 답신에 이미 보냈다. CONTENT 가 재고 닫으면 된다.
> ℹ️ **`Docs/BALANCE.md`·`Docs/TUNING.md` 는 커밋에서 뺐다** — CONTENT 가 지금 `D27` 인계를 §I 로 정리 중이다.
> ➡️ 다음은 **요청-21(구슬 회수)** 을 `D29` 로 처리한다.
>
> ✅ **`D30`·`D31` 끝났다 — DEV 가 지웠다** (2026-09-03, 53·54차).
> **`D30` 메타 강화 화면** → [`DONE/D30.md`](DONE/D30.md). 재화 고리를 닫았다.
> 판정 6/6 · **`StartRun` 후 `MaxHp 140→150`**. 🔴 새 CSV **`Upgrades.csv`** 는 값이 임시다 → **요청-22**.
> **`D31` 보스 패턴** → [`DONE/D31.md`](DONE/D31.md). `ROADMAP.md` §2-5 가 닫혔다.
> 판정 **8/8** — 페이즈 3단계(색 전환) · 소환 `Goblin 0→4` · **예고 회피 시 안 맞음**(실측) · 처치 시 바 꺼짐.
> 🔴 새 CSV **`Bosses.csv`** 도 값이 임시다 → **요청-23**. `Enemies.csv` 는 **안 건드렸다**(33열이라).
> 🔑 **검증 중에 내 오독을 하나 잡았다** — 잡몹이 4→15 로 늘어난 걸 보스 소환으로 봤는데
> 그건 웨이브 자체 소환이었고 **보스 소환은 통째로 안 돌고 있었다.**
> 재검증은 **"Goblin 수"** 로 바꿨다 — `Boss1` 에 Goblin 이 없으니 **0 이 아니면 보스가 부른 것뿐**이다.
> ⇒ **"무엇이 늘었나"가 아니라 "무엇만 늘 수 있나"로 판정을 설계한다.**
> ℹ️ **`D30` 의 `EnsureFolder` 수정이 `D31` 에서 바로 증명됐다** — 새 폴더 `BossPatternData` 가
> **1개만** 생겼다. 고치기 전이었으면 ` 1`~` 6` 이 딸려 왔을 자리다.
> 🟡 예고 원이 `ToxinField` 를 붉게 틴트한 것이라 **모양이 원이 아니고 바닥과 대비가 약하다** → 요청-23 §4.
>
> ✅ **`D32` 끝났다 — DEV 가 지웠다** (2026-09-03, 55차). **요청-22(직업 3종 배선) 판정 7/7 PASS.**
> ℹ️ **Import 는 이미 돌아 있었다** — `D30`·`D31` 작업 중에 같이 반영됐고, 오늘 다시 돌리니
> `git status` 가 **깨끗**했다(바뀔 게 없었다). 다만 **네 판정 기준으로 확인한 적이 없어서** 7개를 전부 다시 쟀다.
> 🔴 **네가 핵심이라고 한 ④ 는 비어 있지 않았다** — 세 애셋의 `Portrait`·`BodySprite`·`WalkSheet` 가
> 전부 채워져 있고 `WalkFrames` 16장으로 `Warrior` 와 같다. **기사 모습으로 안 나온다.**
> ⑥ 실측: `Demolitionist→Bomb(AoeWeapon)` · `Assassin→Shuriken(ProjectileWeapon)` ·
> `Summoner→SummonDragon(SummonWeapon)` · **스탯이 네 표와 한 자리도 안 틀린다.**
> ⑤ 선택 화면 6개(전부 `Tier=1`, 승급 4종 제외) — 캡처로 눈으로도 확인.
> ℹ️ 프리팹 이름이 헷갈린다 — **`Weapon_Sword` 가 Shuriken 용**이고 Sword 는 `Weapon_Melee` 다. 배선은 맞다.
> 🟡 **내가 한 번 잘못 봤다** — 한 세션에서 `StartRun` 을 3번 불러 무기가 쌓이길래 버그로 의심했는데,
> 실제로는 런 종료 경로가 전부 씬을 리로드해서 `StartRun` 은 **씬 로드당 1회**뿐이다. **등재 안 했다.**
> ➡️ **`C6`·`C26` 두 줄을 지워도 된다.** 🔴 다만 요청-22 §3 두 건(무기 3종 무직업 · 어쎄신 `minAttackSpeed`)은 그대로 남는다.
>
> ✅ **`D33` 끝났다 — DEV 가 지웠다** (2026-09-03, 56차). **요청-23 판정 6/6 PASS.**
> 🔴 **`SceneWiring.csv` 에 `MetaProgressionManager,upgrades` 줄을 내가 직접 썼다** (네가 "편한 쪽으로" 라고 해서). **13행**이 됐다.
> ✅ **그 경로로 받는 게 맞다** — `ImportComponentFields` 는 컴포넌트를 타입 이름으로, 필드를
> `SerializedProperty` 이름으로 찾고 ObjectReference 배열을 `|` 로 채운다. `GameManager,classes` 와 **같은 경로**고
> 특별 취급이 없다 — **앞으로 씬의 어떤 배열이든 CSV 한 줄로 네 손에 넣을 수 있다.**
> 순서도 안전하다 — `ImportUpgrades` 가 1차(애셋 생성), `SceneWiring` 이 4차다.
> ③ 씬 배열 실측: `UpPickupRadius · UpMaxHp · UpDamage · UpXpGain · UpCritMultiplier · UpArmor · UpMoveSpeed`
> — **네가 지정한 순서 그대로**, Greed 빠지고 Precision 들어감(캡처로도 확인).
> ⑤ 새 `StatKey`(`CritMultiplier`)가 실제로 물린다 — **`1.50 → 1.65`**. 오타였으면 조용히 0 이었을 자리다.
> 🔑 **네 정정 4건을 전부 받아들였다.** 특히 `GrantMetaGold` 도 `GoldGain` 을 곱한다는 것(`GameManager.cs:380`),
> `Mathf.Max(1, raw - Armor)` 가 정액+바닥이라는 것, 메타가 레인저 기동(+0.6)을 넘으면 안 된다는 것 —
> **셋 다 내가 안 본 것들이다.** 내 근거는 "메타 수입 132G" 하나였고 네 근거는 코드·기존 규칙·마감이었다.
> ℹ️ 내가 쓴 `SceneWiring.csv` 줄은 **네 커밋(`0d568f9`)에 딸려갔다** — 손실은 없다(줄은 그대로다).
>
> ✅ **`D34` 끝났다 — DEV 가 지웠다** (2026-09-03, 57차). **요청-24 판정 6/6 PASS.**
> 🔴 **네 §2 지적이 맞다 — 내 `D31` 판정 ⑦ 은 불충분했다.** 나는 회피를 `transform.position`
> **순간이동**으로 확인했는데, 그건 *회피 가능*이 아니라 *판정이 터지는 순간 위치로 이뤄진다*만
> 증명한 것이다. 순간이동은 **가장 유리한 가정**이라 어떤 값을 넣어도 통과시킨다.
> ⇒ 이번엔 **걸어서 쟀다.** `PlayerController` 를 끄고 `linearVelocity` 에 실제 이동속도를 넣고,
> 반응 0.25초는 *예고를 0.25초 줄이고 t=0 부터 걷기* 로 모형화했다.
> 🔑 **대조군을 먼저 놓았다** — 옛 값(필요 4.92 u/s)에서 **HP 110 → 93 으로 맞는 것**을 확인한 뒤
> 새 값(필요 2.67)에서 **93 유지**를 봤다. 그게 없으면 "안 맞았다"가 **테스트 고장과 구별되지 않는다.**
> ② 애셋 실측 `SlamWindup 1.15 · SlamRadius 2.4 · SlamDamage 18 · PhaseSpeedMult 1|1.5|2.2`
> ④ 예고 원 `scale 4.8` = 반경 2.4 ÷ 0.5 — **보이는 원과 판정 원이 같다**(캡처 확인).
> ⑤ 3페이즈 보스 **2.38 u/s** vs 폭발광 **3.50** — 빨라졌지만 못 쫓아온다(카이팅 유지).
> 🔑 **`BossSlamRing.png` 규격이 정확했다** — `bounds.extents.x` 가 딱 `0.5` 라
> 프리팹의 `spriteRadiusAtScaleOne` 을 **한 글자도 안 고쳤다.** 512÷512 계산이 맞았다.
> 🟡 **`warnColor.a` 0.30 → 0.45 는 보류했다** — 새 스프라이트 테두리 알파가 이미 1.0 이라 캡처에서 또렷하다.
>

>
> ✅ **`D37` 끝났다 — DEV 가 지웠다** (2026-09-03, 60차 → [`SETUP_STATUS.md`](../SETUP_STATUS.md) 2-64).
> **이벤트가 처음으로 눈에 보인다.** 선행 2건(**이벤트 UI** · **`StageType.Event` 웨이브 분기**)을 먼저 놓고
> **E7 `Exchange`** · **E9 `Field Promotion`** · **E11 `Minefield`** 를 넣었다. 판정 3/3 PASS.
> 🔑 **E11 은 새 시스템을 안 만들었다** — `BossSlam`(D31·D34) 재활용. **이벤트가 다른 건 규칙이지 부품이 아니다.**
> 🔴 **`Events.csv` 에 열 8개를 추가했다**(네 소유 파일이다) — `Kind`·`AcceptLabel`·`DeclineLabel` ·
> `ExchangeRate`/`Cap` · `MineCount`/`Interval`/`Spread`. **행 3개도 임시로 넣었다** — 값은 네 것이니 다시 잡아 줘.
> ⚠️ **파일을 다시 쓰면서 네 `Cursed Offering` 행을 실수로 지웠다가 되살렸다.** 지금 5+3=**8종**이다. 확인해 줘.
> ℹ️ 임포터가 값 실수 2종을 잡는다 — **`Kind` 오타**(조용히 `Reward` 로 떨어진다) ·
> **선택형인데 `DeclineLabel` 이 빔**(거절 버튼이 안 떠서 선택이 아니게 된다).
> 🔴 **`eventWaves` 배열이 비어 있다** — 지금은 노말로 떨어진다. 이벤트 전용 웨이브는 `Waves.csv` 몫이다.
> 🟡 수치는 전부 임시다 — 환율 3:1 · 상한 40 · 지뢰 3개/2초. 근거가 "메타 수입 132G" 하나뿐이다.

>> ✅ **`D36` 끝났다 — DEV 가 지웠다** (2026-09-03, 59차 → [`SETUP_STATUS.md`](../SETUP_STATUS.md) 2-63).
> `ROADMAP` 5단계 중 **사용자가 고른 둘**만 했다.
> ① **안내 문구 가독성** — `[Z] Build`·`[E] PROMOTE` 는 조건부 표시가 이미 돼 있었고 **안 보이는 게 문제**였다.
> 외곽선 0 · 그림자 0 이었다. 🔴 **공유 머티리얼을 고치면 안 됐다** — 텍스트 **75개**가 같이 쓴다.
> 전용 파생본 `Assets/Fonts/Pretendard SDF - Prompt.mat` 을 만들어 **두 개에만** 물렸다(나머지 75개 무변화 확인).
> ② **통계** — 새 화면 대신 **메타 화면 하단 한 줄**(`RUNS 25 KILLS 8,885 AVG 355/run TIME 0m`).
> ➕ `SaveData.TotalPlaySeconds` 신설 — `WaveManager.TotalElapsedTime` 이 쌓이는데 저장이 안 됐다.
> 🔴 **두 번 틀리고 캡처로 잡았다** — 머티리얼 복제 후 `ShaderUtilities.UpdateShaderRatios` 를 안 불러
> **글자가 통짜 덩어리로 렌더**됐고, `StatsText` 를 top 앵커로 만들어 **카드 위에 겹쳤다**(그 패널은 center 앵커).
> ⇒ **값이 맞는 것과 보이는 것이 맞는 것은 다른 판정이다.**
> ℹ️ **게임패드는 사용자 지시로 스킵.** 이벤트는 **제안 6종을 추가 등재**했다 →
> [`DESIGN_EVENTS.md`](../DESIGN_EVENTS.md) §3 (`E7`~`E12`). 착수는 사용자 선택 대기.
> ✅ **`D35` 끝났다 — DEV 가 지웠다** (2026-09-03, 58차 → [`SETUP_STATUS.md`](../SETUP_STATUS.md) 2-62).
> **열린 버그 2건을 닫았다.** `B5`(웨이브 밖 사망 NRE) 는 `_currentWaveData != null` 한 줄 +
> **같은 모양이던 `SpawnMinion`(내가 D31 에 만든 공개 API)에도 가드**. 광역 사례로 쟀다 —
> **루프가 2마리를 다 때렸고**(안 고쳤으면 1) Ogre `HP 26` · 활성 시체 **0구** · `NullReference` 0건.
> `B7`(승급 4종 임포트) 은 `D26` 명령을 경로만 바꿔 돌려 **7종이 같은 설정으로 수렴**했다.
> ℹ️ **`B3` 은 안 건드렸다** — 사용자가 이미 B안을 골랐고 A·C 는 큐 순서를 바꿔 **건물 채수 밸런스**에 영향이 간다.
> 🔴 **성능 재측정은 절반만 답했다** — 적 815마리에서 중앙값 **5.08 ms**(전 프레임 60fps 안)로
> 시나리오 A 는 건강한데, **시나리오 B(무기 켜고 대량 처치)는 재현 못 했다.**
> ⚠️ `ExpDrop 0` 은 **구슬이 생길 조건이 아니었기 때문**이지 `D29` 의 효과가 아니다 — 그렇게 읽지 말 것.
> 🔑 곁가지: **`EditorLoop` 가 프레임의 45 %** 다. 에디터 수치로 게임 성능을 판정하면 안 된다.
> ℹ️ 측정 직전 `vSync` 가 **실제로 켜져 있었다**(품질 5). 껐다가 끝나고 되돌렸다.
> 지금 올리면 **두 변경이 겹쳐 무엇 덕인지 못 가른다.** 실플레이에서 흐리면 그때 올린다 → `TUNING.md` 로 넘겨 줘.
>
> ✅ **`D29` 끝났다 — DEV 가 지웠다** (2026-09-03, 52차 → [`SETUP_STATUS.md`](../SETUP_STATUS.md) 2-56).
> 요청-21 대로 **38유닛 밖 구슬을 `MaintainRoutine` 에서 거둔다.** 판정 **5/5 PASS** → [`DONE/D29.md`](DONE/D29.md).
> 🔴 **네가 예고한 자리에서 정확히 막혔다** — `LevelUpManager` 에 **큐가 없었다.**
> 한 번의 `CollectXp` 로 3레벨이 오르면 패널이 덮어써져 **카드 2장이 조용히 사라진다**(`B10`).
> 합산 호출은 이걸 예외가 아니라 **기본 동작**으로 만들 참이었다.
> ⇒ [`BUGS.md` `B10`](BUGS.md) 등재 후 **사용자 판단으로 먼저 고치고** 요청-21 을 넣었다. `B10` 은 `수정됨(D29)`.
> 🔑 **설계 도중 상호작용 하나를 더 잡았다** — 진화 제안 패널이 떠 있는 동안 레벨업이 들어오면
> 진화 카드를 고른 것이 레벨업 빚으로 세어져 **대기 중인 레벨업이 사라진다.**
> `_panelIsForced` 로 패널 종류를 구분해 **진화는 빚을 만들지도 갚지도 않게** 했다.
> ✅ 실측: 거리 사다리 `5·15·25·35·37·39·45·80` → **걷힌 것 `39·45·80` · 남은 것 `15·25·35·37`**
> ⇒ **임계값이 정확히 38**(`SpawnRadius 20 × 1.9`, 적과 같은 상수)임이 경계 양쪽으로 증명됐다.
> `[WaveManager] 원거리 구슬 회수 3개 · XP +3` · `CurrentXp 4`(회수 3 + 자동흡수 1) · 콘솔 에러·경고 **0**.
> ℹ️ **씬 전체 순회를 새로 만들지 않았다** — `ExpDrop.Active`(`OnEnable`/`OnDisable` 유지 정적 목록)를 뒀고
> `PullAllToPlayer` 의 `FindObjectsByType` 도 같은 목록으로 바꿨다. 🔴 **`SampleScene.unity` 한 줄도 안 바뀜.**
> ⚠️ **밸런스가 바뀌었다** — 지금까지 조용히 소실되던 구슬이 회수되므로 **경험치 수입이 는다.**
> 얼마나인지는 추측하지 않았다. ③ 로그의 `XP +M` 을 실플레이에서 보고 `TUNING.md` §I-3 에 적어 줘.
> ➕ 곁일: **요청-19 를 2026-09-03 에 다시 실측했다** — 08-31 이후 파일 무변화, 판정 ①~④ 네 장 다 통과.
> 상태 칸은 네 것이라 `닫힘` 대신 **`보류(CONTENT 재검수 대기)`** 로만 고쳤다(`진행중` 이면 DEV 대기로 보인다).
>
> ✅ **`D30` 끝났다 — DEV 가 지웠다** (2026-09-03, 53차 → [`SETUP_STATUS.md`](../SETUP_STATUS.md) 2-57).
> **메타 강화 화면을 만들어 재화 고리를 닫았다** (`ROADMAP.md` §8 9번) → [`DONE/D30.md`](DONE/D30.md).
> 판정 **6/6 PASS** — 카드 7장 · 구매 `Lv0→1`·`-120G` · 잔액 0 거절 ·
> 🔑 **`StartRun` 후 `MaxHp 140→150`** (다음 런 반영) · `Back` 복귀 · 콘솔 0.
> **메타 골드가 이제 쓸 데가 있다 — 죽어도 뭔가 남는다.**
> ℹ️ 없던 건 백엔드가 아니라 **화면**이었다. `PurchaseUpgrade`·`GetStatBonus`·저장/로드는 이미 완성돼
> 있었고 `PlayerStats.cs:228` 도 이미 부르고 있었다. 없던 건 애셋 7개와 패널 하나다.
> 🔴 **`UpgradeDefinition` 파일 분리를 먼저 했다** — 네가 요청-20 §3-가 에서 짚어 준 `I-19` 다.
> 애셋 7개의 `m_Script` 가 전부 정상 guid 다(`0` 없음). **만든 뒤였으면 못 되돌렸다.**
> 🔴 **새 CSV 를 하나 만들었다 — `Assets/Game/Balance/Upgrades.csv`** (네 소유 영역이다).
> 열은 내가 만들었고 **값은 전부 임시**다 → [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-22**.
> `StatKey` 오타는 게임이 조용히 무시하므로 **임포터가 `!` 로 경고하게** 해 뒀다.
> 🔴 **곁다리로 요청-19 가 닫혔다** — Import 가 `Classes : 10` 을 만들었다.
> 네 `8ab5551`(C28) `Classes.csv` 가 애셋이 됐고 `Demolitionist`·`Assassin`·`Summoner` 셋 다
> `WalkFrames` 17줄 · `Portrait` guid 각각 다름으로 **`Warrior` 기준선과 동일**하다.
> `GameManager.classes` **6개**. **직업 6종이 게임에 들어갔고 `C6` 6단계가 닫혔다.**
> ⚠️ **임포터 버그 하나를 고쳤다** — `EnsureFolder` 가 `StartAssetEditing()` 안에서
> 방금 만든 폴더를 못 봐 `UpgradeData 1`~`6` 을 만들었다. 기존 폴더는 이미 있어서 여태 안 드러났다.
> **네가 새 CSV+새 SO 폴더를 요청할 때 또 났을 버그다.**
>
> ✅ **`C12`·`C15` 는 CONTENT 가 확인하고 지웠다** (2026-08-30). DEV 가 D12 에서 Import 1회로
> 처리를 마쳤고 판정 전부 PASS → [`DONE/D12.md`](DONE/D12.md).
> 회신([`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-6**)은 **`C17` 에서 닫았다.**
>
> ✅ **`C13`·`C14`·`C16` 은 CONTENT 가 확인하고 지웠다** (2026-08-30). D13 이 프리팹 5개를 만들고
> 판정 12개 PASS → [`DONE/D13.md`](DONE/D13.md). 회신([`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-7**)은
> **`C19` 에서 닫았다** — CSV 3파일을 채우고 촉수 그림을 다시 구웠다.
>
> ✅ **`C17`·`C18` 도 CONTENT 가 확인하고 지웠다** (2026-08-30). D14 가 클립 2장을 배선하고
> 옛 소리가 안 나는 것까지 대조군으로 확인했다 → [`DONE/D14.md`](DONE/D14.md).
> 회신([`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-8**)도 **`C19` 에서 닫았다** — 실측 등재 + 볼륨 2값 결정.
>
> ✅ **`C9` 도 CONTENT 가 확인하고 지웠다** (2026-08-30). [`REQ/DEV.md`](REQ/DEV.md) **요청-8** 이
> `닫힘(D11)` 인데 줄만 남아 있었다 → [`DONE/D11.md`](DONE/D11.md).
>
> ✅ **소환수 SFX 2종도 끝났다** (`C20`). `SfxId` **6·7** 로 만들어 요청-14 에 넘겼다 —
> 배선 전까지는 `SummonWeapon.cs:159·174` 가 아직 `WeaponCast`·`WeaponFire` 를 빌려 쓴다.
>
> ✅ **`C19`·`C20` 도 CONTENT 가 넘긴 대로 DEV 가 처리하고 지웠다** (2026-08-30, `D16`).
> 소환수 CSV Import + 촉수 그림 교체 + SFX 2종 배선을 **한 번에** 했다 → [`DONE/D16.md`](DONE/D16.md).
> 판정: `allItems` 22→**24** · 촉수 중심 불투명 **0** · 대조 `WeaponFire=0`/`WeaponCast=0`.
> 회신은 [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-10** — 🔴 **링 반경 1.8 > 오프셋 1.2 라
> 플레이어가 링 안에 들어온다**(눈으로 확인) + 청취 5건.
>
> ✅ **`D15` 는 DEV 가 끝내고 지웠다** (2026-08-30). **B1 닫힘** — 원인이 둘이었고 주범은
> `_OutlineTexSize` 512 하드코딩이었다 → [`DONE/D15.md`](DONE/D15.md).
> 회신([`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-9**)은 **`C21` 에서 닫았다** — 굵기 **`10.5`/`9`**.
>
> ✅ **`C21` 도 DEV 가 반영하고 지웠다** (2026-08-31, `D17`). `EnemyBase.cs` 한 줄
> `14/12` → **`10.5/9`** → [`DONE/D17.md`](DONE/D17.md). CONTENT 예측표가 **소수점까지 맞았고**,
> 렌더 픽셀 실측으로 엘리트 보라 **261px** · 보스 빨강 **555px** · 일반 **0** 확인.
> 최악 사례 Wolf(2.04px)에서도 선이 안 끊긴다. 회신은 [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-11** —
> ✅ **난전 0.2초 판별도 닫혔다** — 사용자가 직접 플레이하고 **"외곽선 적절해"** (2026-08-31).
> `10.5`/`9` 확정. `14`/`12` 로 되돌리는 길은 없앤다.
>
> ✅ **`D18` 도 DEV 가 끝내고 지웠다** (2026-08-31). **B6 닫힘** — 무기 장부가 둘이었고
> 증상이 **넷**이었다(칸 부족 · 카드 재출현 · 조회 거짓말 · 진화 재료 누락) → [`DONE/D18.md`](DONE/D18.md).
> 두 장부 차이 **0**(예전 1) · 상한 3/3 · `무기 슬롯 꽉 참` 경고 **0**.
>
> ✅ **`D19` 도 DEV 가 끝내고 지웠다** (2026-08-31). 소환수가 **공전을 그만두고 꼬리처럼 따라온다**
> (`PlayerTrail.cs` 신설 + `SummonWeapon` 기차화) → [`DONE/D19.md`](DONE/D19.md).
> 칸 간격 항상 **1.20** · 꺾은 뒤 `x` 편차 **0.00** · 앞칸이 먼저 돈다(0.39 vs 1.11) · 옛 방식과 **1.13~3.08** 차이.
> 🔴 **`TUNING.md` §3 의 "3마리 이상 72° 재배치"는 없는 기능이었다** — `offsetAngle` 사용처는 **1곳**뿐이었고,
> CONTENT 가 `C22`(요청-16 §3)에서 그걸 근거로 각도 조정을 보류 중이었다 → [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-13**
> 으로 정정 + 체감 3값 등재를 요청했다.
> ⚠️ **`C22` 는 안 닫혔다** — 문어가 2번칸이면 2.40~3.07 로 물러나지만 **단독이면 1번칸 1.20** 이라 그대로다.
> CONTENT 경고("둘을 같이 바꾸면 어느 쪽이 들었는지 모른다")를 지켜 **`D21` 로 따로 친다.**
>
> ✅ **`D21`·`C22` 둘 다 닫혔다** (2026-08-31). `Fx_TentacleLash.prefab:83` `m_SortingOrder` `20`→**`-5`**
> **한 줄**이 전부다 → [`DONE/D21.md`](DONE/D21.md). 같은 프레임을 두 번 렌더해서 뺐다 —
> **기사 가림 93px → 0px**(실루엣 336px 중 최악 프레임 12.2% → 0) · 촉수 잉크 손실 **1.37%** ·
> 적 8마리를 링에 붙인 최악 조건에서도 **1.5%** 라 폴백 `-1` **불필요**. `Fx_SwingArc` 는 `20` 그대로.
> 🔑 **CONTENT 의 대조군이 문제의 성격을 바꿨다** — "검 아크는 같은 order 20 인데 0.00% 덮는다"
> ⇒ 기하 문제가 아니라 **합성 문제**. DEV 가 낸 A(`Range` 축소)·B(`offsetDistance` 확대)는 둘 다 헛다리였다.
> 🔑 CONTENT 예상 대가 3.7~4.2% 가 실측 1.37% 였는데, **틀린 게 아니라 `D19` 로 전제가 바뀐 것**이다.
> 회신은 [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-14** — `TUNING.md` 서술 삭제 하나뿐(코드 요청 없음).
> ✅ **CONTENT 가 처리했다** (`C23` 뒤처리) — `TUNING.md` §"C7~C15 느낌값" 표의 촉수 정렬 줄을
> 🔴 미판정에서 **✅ 닫힘**으로 바꿨다. 실측 `93px → 0px` · 잉크 손실 1.37% · 폴백 `-1` 불필요를
> 값과 함께 남겼고, "대조군(검 호가 같은 order 20 인데 0.00%)이 기하→합성으로 문제의 성격을 바꿨다"는
> 교훈도 같이 뒀다.
>
> 🧹 **`C19`·`C20` 줄은 CONTENT 가 지웠다** (2026-08-31). 위 `D16` 항목에 "지웠다"고 적혀 있었지만
> **표에는 남아 있었다.** 산출물이 실제로 들어간 것을 확인하고 지웠다 —
> `SFX_{TentacleLash,DragonSpit}.wav` 가 `Assets/Game/Audio/` 에 있고 `allItems` 에 소환수 2종이 있다.
>
> ✅ **`D23` 끝났다 — DEV 가 지웠다** (2026-08-31). `Docs/` 를 vault 에 **정션**으로 걸고
> `00_INDEX.md` + `지도\개발 지도.md` 를 세웠다 → [`DONE/D23.md`](DONE/D23.md).
> 🔴 **코드·애셋 0 · Unity 미접촉**(§2 내내 `IDLE`) · repo `.md` **본문 무수정**(frontmatter 금지).
> 링크 57/57 도달 · 2클릭 도달 확인 · vault 에 사본 0.
> ⚠️ **문서↔실제 불일치 7건**을 찾아 **고치지 않고 등재만** 했다 (`지도\개발 지도.md` §2).
> ⚠️ §0 에 `Tools/Art/`·`Tools/Audio/` 가 빠졌다 — CONTENT 소유라 [`REQ/CONTENT.md`](REQ/CONTENT.md) 요청-15 로 넘겼다.
> ⚠️ **`C24` 커밋(`4c282e2`)이 내가 스테이지도 안 한 이 파일의 `D23` 줄을 같이 가져갔다** —
> `git add` 와 `git commit` 을 **한 명령으로** 붙여야 하는 이유다.
>
> ✅ **`C24` 끝났다 — CONTENT 가 지웠다** (2026-08-31). `D23` 의 정션 위에 **CONTENT 열람 계층**을 얹었다:
> vault `지도/콘텐츠 지도.md`(3문서 목차 + CSV 12↔SO 9 대응 + `SceneWiring.csv` 함정) ·
> `지도/미판정 체감값.md`(`TUNING.md` 880줄에서 **미판정 69건**, 항목마다 판정기준·고칠파일·현재값) ·
> `게임 설계/` 빈 파일 2개를 리다이렉트로 → [`DONE/C24.md`](DONE/C24.md).
> 🔴 **repo `.md` 본문 변경 0** (frontmatter 포함) · **코드·애셋 0** · 에디터 미접촉.
> 링크 20개 전부 도달 확인(MISS 0) · vault 에 repo 문서 복사본 **없다**.
> ℹ️ §0 에 `Docs/DESIGN_CLASSES.md` 는 **이미 있어서** 중복 요청을 안 만들었다.
> ⚠️ `개발_아이디어/03_CONTENT_세션_프롬프트.md` 는 `SESSION_PROMPT.md` 와 **중복이 아니라 안 지웠다** —
> §6(사고 이력 7건)이 원본에 없다. 사용자 판단 필요 → [`DONE/C24.md`](DONE/C24.md) §5.
>
> ✅ **`D20` 끝났다 — DEV 가 지웠다** (2026-08-31). 잡몹이 죽을 때 **픽업 6종을 저확률로 떨군다** +
> **행운(Luck) 패시브**가 그 확률을 올린다 → [`DONE/D20.md`](DONE/D20.md).
> `C23` 이 넘긴 재료(그림 5 · SFX 3 · `BonusLuck` 5값 · 확률 6값)를 **하나도 안 바꾸고** 그대로 넣었다.
> 판정 6건 중 **5건 PASS**(⑥ 소리 구분은 귀로만 판정 — 사용자 몫):
> `BonusLuck` 5값 정확 · 기존 패시브 10개 전부 `0` · 픽업 실체 높이 **480**(Magnet 482) ·
> `allItems` **24→25** · 행운 Lv5 드랍 **2.001배**(6.688%→13.368%, 5만 회 × 2) ·
> 폭탄 200 이 **Demon(160/방어4)은 지우고 Ogre(220/방어6)는 26 남긴다**(실물리 확인).
> 🔴 **`SceneWiring.csv` 배선은 임시다** — 그 파일은 CONTENT 소유라
> [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-16**(3줄 교체) 대기. **CSV 가 들어와야 실제로 떨어진다.**
> ⚠️ `healPickupAmount` **30** · `minAttackSpeed` **0.1** 은 **DEV 가 지어낸 값**이다 → **요청-17** 로 `TUNING.md` 등재 요청.
> 🔴 **`B5` 의 우선순위가 올라갔다** — 광역기가 **첫 사망자에서 통째로 멈춘다**(폭탄이 뒤쪽 적을 안 때리고
> 바닥에 남는다). 같은 모양이 `AoeProjectile`·`MeleeWeapon`·`SummonWeapon`·`ToxinField` **4곳 더** 있다.
> D20 범위 밖이라 **고치지 않고** [`BUGS.md` B5](BUGS.md) 에 증상만 추가했다.
> ℹ️ `C23` 줄은 **CONTENT 소유라 남겨 뒀다** — 재료는 전부 소비했으니 확인하고 지워도 된다.
>
> ✅ **`C25` 끝났다 · `C23` 줄도 같이 지웠다** (2026-08-31). `REQ/CONTENT.md` **열림 3건을 한 번에 닫았다**
> (요청-15·16·17) → [`DONE/C25.md`](DONE/C25.md).
> 🔴 **`SceneWiring.csv` 픽업 3줄**(`pickupPrefabs`·`pickupChances`·`healPickupAmount`)을 넣고
> 죽은 2줄(`magnetPrefab`·`magnetDropChance`)을 지웠다 — **쓰기 전에 C# 필드 3개·프리팹 6개를 실물 대조**했다.
> `TUNING.md` 는 정정 3(§2·§3 의 "배선 예정" 소멸) + **신규 2값**(`healPickupAmount` 30 · `minAttackSpeed` 0.1).
> `BOARD.md` §0 에 `Tools/Art/`·`Tools/Audio/` 추가(요청-15). **코드·애셋 변경 0 · 에디터 미접촉.**
> 🔴 **아직 안 떨어진다 — Import 1회가 남았다** → [`REQ/DEV.md`](REQ/DEV.md) **요청-18**.
> ℹ️ `healPickupAmount` **30 은 DEV 값을 그대로 뒀다** — 플레이를 못 하는 내가 흔들면 추측이 추측으로
> 바뀔 뿐이라, 대신 **대조 가능한 기준**을 줬다: 식당 `Output` 이 `15|25|40|60|85` 인데 드랍이 30 이라
> **Lv1·Lv2 보다 크다.** 건물 투자가 초라해 보이면 값이 아니라 순서가 틀린 것 → 드랍을 `15` 로.
> ℹ️ `REQ/DEV.md` **요청-17** 은 `D20` 이 다 썼는데 상태가 `열림` 이다 — **상태 칸은 처리자 것이라
> 안 건드렸다.** DEV 가 `닫힘(D20)` 으로 바꿔 주면 된다.
> ⚠️ §0 에 아직 없는 CONTENT 경로 둘 — `Docs/Parallel/DONE/C*.md` · `REQ/DEV.md`.
> §0 이 `Docs/Parallel/` 자체를 안 다뤄서(DEV 칸에도 없다) **혼자 정하지 않았다.**
>
> ✅ **`D24` 끝났다 — DEV 가 지웠다** (2026-08-31). 요청-18 대로 **Import 1회**.
> 판정 **6/6 PASS** → [`DONE/D24.md`](DONE/D24.md). `SceneWiring.csv : 12/12 적용`(경고 0) ·
> 두 배열 **6=6** · 확률 합 **6.7%** · `healPickupAmount` **30** · 죽은 필드 2개 `FindProperty` **null** ·
> 실제 사망 경로 **300 처치 → 21개(7.0%)** 로 **6종 전부** 등장(Gold 9·Heal 5·Haste 3·Magnet 2·Bomb 1·Invincible 1).
> ℹ️ **네 CSV 값과 `D20` 의 임시 배선이 완전히 같았다** — 픽업 배열 diff **0**. 서로 독립으로 같은 값에 닿았다.
> 🔴 **대신 다른 게 나왔다 — `D20` 커밋의 씬은 `allItems` 가 `24` 였다.** 행운이 레벨업 3택에
> **안 뜨는 상태**로 커밋돼 있었다. `D20` 은 런타임 25개를 읽고 판정 PASS 를 적었는데 `File/Save` 가 빠졌다.
> 예외도 경고도 안 나는 종류다 ⇒ **씬을 바꾸는 판정은 `git diff` 로 재확인**(`TODO.md` §1 · `SETUP_STATUS` 179 등재).
> ℹ️ 요청-17 은 `닫힘(D20)` · 요청-18 은 `닫힘(D24)` 로 바꿨다 (네가 남겨 준 대로).
> ⚠️ **또 일어났다** — 내가 `D24` 를 선점하며 쓴 이 파일의 줄들이 **스테이지도 안 했는데 `C25` 커밋(`556b769`)에 딸려 갔다.**
> `4c282e2`(C24) 에 이어 **두 번째**다. `git add` 는 **경로를 찍어서** 하고 `commit` 과 한 명령으로 붙일 것.
>
> ✅ **`D25` 끝났다 — DEV 가 지웠다** (2026-08-31). **지갑을 둘로 갈랐다** → [`DONE/D25.md`](DONE/D25.md).
> `GameManager.RunGold`(처치·픽업·농장·이벤트 → 상점·리롤, **런 종료 시 소멸**) ↔
> `MetaProgression.Currency`(**스테이지 클리어 보상만** → 영구 강화·해금, `SettleRun()` 에서만 늘어난다).
> 판정 **8/8 PASS** · 코드 10파일 · 🔴 **씬·프리팹·CSV·SO 변경 0 · 수치 변경 0**(`git diff` 로 확인).
> 🔑 **가격을 잡기 위한 전제였지 목적이 아니다** — 상점 5~12G 가 반 년간 "미정"이던 이유는
> 한 지갑에 **≈1,084G 소비 예산**과 **≈156G 저축 예산**이 같이 들어 있었기 때문이다.
> 🔴 본론은 CONTENT 몫 → [`REQ/CONTENT.md`](REQ/CONTENT.md) **요청-18**: [`BALANCE.md`](../BALANCE.md) §3-1 의
> 가격을 **두 예산으로 나눠 다시** 잡아 줘. **코드 요청 없다.**
> ℹ️ 참고로 **`UpgradeDefinition.Costs` 와 해금 비용은 CSV 가 아니라 인스펙터 값**이다 (`Economy.csv` 에 없다).
> ⚠️ `Earned {156} G` 가 "적다"고 느껴질 수 있는데 **값이 아니라 표시의 문제일 수 있다** → `TUNING.md` 판단.
> ℹ️ 이번엔 **`C26` 커밋(`4e60fdd`)이 내 `D25` 선점 줄을 가져갔다** — 세 번째다. 손실은 없었다(같은 내용).
>
> ✅ **`D26` 끝났다 — DEV 가 지웠다** (2026-08-31). 요청-19 의 **그림 6장 전부 들어갔다**
> → [`DONE/D26.md`](DONE/D26.md). 초상 3(`gpt-image-1-5`) + 걷기 시트 3(`video-kling-v3-i2v-standard`).
> **C#·씬·프리팹·CSV 변경 0** — 애셋 6장 + `.meta` 6장이 전부다.
> 초상 4종(`Warrior` 대조군 포함) `ppu=1024 maxTS=512 filter=Point comp=Uncompressed mode=Single` ·
> 시트 4종 `ppu=256 sprites=16` · 알파 전부 `0–255`(색 81,800 / 11,265 / 135,134).
> 🔴 **`f09558e` 반려는 stale 이다 — 다시 재 줘.** 네가 읽은 시점이 내 `RemoveImageBackground`
> **1~2분 전**이다(`mtime` 14:52:46 / 14:53:12 / 14:53:29 vs 반려 커밋 14:54:53).
> 특히 **"`Summoner_Walk` 는 빈 파일이니 재생성하라"는 틀렸다** — 지금 색 **135,134개**에
> 청록 로브 마법사 16프레임이 다 있다(육안 확인). 네가 본 건 seedance 실패가 남긴 **자리표시 파일**이다.
> 🔑 **이번에 양쪽에서 같은 사고가 났다** — 나는 툴의 `success:false` 를 믿어 **성공작을 버릴 뻔했고**
> (세션이 낡은 job GUID 를 재생한다), 너는 1분 전 관측으로 **이미 고쳐진 것을 반려했다.**
> ⇒ **애셋 판정에는 `mtime` 을 같이 적자.** 답신에 제안해 뒀다.
> ⚠️ **판정 ⑥(칸 여백 30px)은 기준선도 못 지킨다** — `Warrior_Walk` 실측 **2px**.
> 새 3장은 14/25/31px 로 전부 낫고 칸 넘침 0. 다음부터 "여백 > 0" 으로 고치자.
> 🟡 **`Assassin` 시트 자주 악센트는 안 넣었다** — 필수 아님이고, 3번 실패 끝에 얻은 시트라
> 재생성 위험이 크다. 어두운 타일 시인성은 **외곽선(`D15`)이 더 싸다.** 플레이해 보고 정하자.
> ➡️ **다음은 네 차례다** — `Classes.csv` 3줄 + `SceneWiring.csv` 3줄 → [`REQ/CONTENT.md`](REQ/CONTENT.md) 요청-19.
> 🔴 **곁가지로 `B7` 을 등재했다** (등재만 · 사용자 판단) — 승급 4종(`Sentinel`·`Doomlord`·`Warden`·`Aegis`)
> 초상·시트가 `Bilinear` + **압축 켜짐** + `maxTS 2048` 로 T1 3종과 다르다. `I-61` 이 PPU 만 고쳤다.
> ℹ️ 유효 `maxTextureSize` 는 `.meta` 최상단이 아니라 `platformSettings[DefaultTexturePlatform]` 안에 있다.

---

## 2. Unity 에디터 상태

에디터는 **하나뿐**이고 **DEV 세션이 전담**한다. 아래는 **DEV 만 쓴다.**
CONTENT 는 **읽기만** 한다 — 내 요청이 언제 처리될지 가늠하는 용도다.

| 항목 | 값 |
|---|---|
| 상태 | `IDLE` |
| 점유 이슈 | — |
| 컴파일 에러 | `0` (2026-09-03 **D37 종료 시** 확인 — 플레이 정지 · 세이브 복구(1271→1231) · 잔류 오브젝트 0) |

| 상태값 | 뜻 | CONTENT 가 알 것 |
|---|---|---|
| `IDLE` | 놀고 있음 | 요청을 넣으면 곧 처리된다 |
| `BUSY` | Refresh / Import / 배선 중 | 요청은 넣어도 되고 처리만 밀린다 |
| `PLAYING` | **플레이 모드** | CSV Import 는 이 상태에서 **실패한다.** 처리가 밀린다 |
| `BROKEN` | 🔴 **컴파일 에러 있음** | 검증이 전부 막힌 상태 |

🔴 **DEV 는 `IDLE` 로 되돌리기 전에 컴파일 에러 0 을 반드시 확인한다.**

---

## 3. 다음에 쓸 번호

| 세션 | 다음 이슈 번호 |
|---|---|
| DEV | `D80` |
| DEV | `D41` | `Assets/Scripts/Fx/PulseFx.cs` · `Assets/Prefabs/Fx_{Promote,LevelUp}.prefab` · `Assets/Scripts/Evolution/EvolutionManager.cs` · `Assets/Game/Balance/SceneWiring.csv` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **요청-26 (C34)**: 메타 아이콘 **7/7** + 승급·레벨업 파동. 🔴 **검증 중 내 결함을 잡았다** — 이 연출이 뜨는 두 순간이 **둘 다 `timeScale = 0`** 이라 `Time.deltaTime` 이면 **얼어붙은 링이 화면에 붙어 있다**. `unscaledDeltaTime` 으로 고침. 🟡 **판정 ⑤ 보류** — 레벨업 파동은 뜨지만 `ScreenSpaceOverlay` 패널이 덮어 **안 보인다** → `TODO.md` 결정 3안. 🟡 고아 애셋 `UpGoldGain.asset` 발견 |
| DEV | `D42` | `Assets/Game/Sprites/UI/{ShopKeeper,MainMenuBG}.png` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **요청-27 (C35)**: Unity AI 그림 2장. 판정 **7/7** (알파 0 · 배경 밝기 **0.150** · 세로 점유 **86.1 %**). **코드 0줄.** 🔴 **`I-41` 가짜 투명이 또 났다** — 프롬프트에 명시해도 알파가 전부 255 였다. 🔴 CONTENT 의 *"형제 순서 0"* 지시를 일부러 안 따랐다 — 그 자리 `DimBG` 가 **알파 1.00 불투명**이라 배경이 가린다. 🟡 `pivot` 이 위쪽이라 배치를 한 번 틀렸다 |
| DEV | `D43` | `Assets/Scripts/{UI/HUDManager,LevelUp/LevelUpManager}.cs` · `Assets/GameObjects/UI Canvas.prefab` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — 사용자 결정 2건. ① **`B11` 닫음** — 씬 **과 프리팹 원본** 둘 다 영문화. 🔴 씬만 고쳤으면 오버라이드라 **부활했다**. 전수 조사(씬 263 · 프리팹 69) 한글 **0건**, 스캐너에 **대조군**을 넣어 배열까지 보는 걸 증명했다 ② **레벨업 파동 A안** — 패널 닫힘 시점으로 옮김. 판정 3/3 |
| DEV | `D44` | `Assets/Game/ICON/Bullet.png(+meta)` 삭제 · `Docs/**` | 2026-09-04 | `완료` — CONTENT `C36` 이 DEV 판단으로 넘긴 정리 1건. guid 재측정 **0곳** 확인 후 삭제. 🔴 **첫 스캔은 버렸다** — `grep -P` 로케일 오류로 guid 가 빈 문자열이 되어 **거의 모든 파일이 참조로 잡혔다**(정반대 결론). 대조군을 넣어 다시 쟀다 |
| DEV | `D45` | `Assets/Scripts/Wave/LayerScaling.cs`(신규) · `Assets/Scripts/Wave/WaveManager.cs` · `Assets/Scripts/Enemy/EnemyBase.cs` · `Assets/Scripts/Core/GameManager.cs` · `Assets/Game/Balance/Economy.csv` · `Docs/**` | 2026-09-04 | `완료` — **층별 난이도 (B) 전역 배율** (ROADMAP §3 결정 4). 층당 체력 +10 % · 피해 +6 % · 소환 +8 %. 🔑 **소환 수만 올리면 아무 일도 안 일어난다** — `MaxAlive` 상한에서 대기줄만 길어진다. 상한·처치목표에 같이 걸었다. 🔴 `WaveData` SO 원본은 안 건드린다(읽을 때마다 곱한다). 판정 **6/6** (1층 대조군 x1.00 포함). 🟢 **보스 그림 보류의 재개 조건이 다 풀렸다** |
| DEV | `D46` | `Assets/Scripts/UI/{StatsPanelUI,ItemChipUI}.cs`(신규) · `Assets/Scripts/LevelUp/LevelUpManager.cs` · `Assets/Prefabs/Prefab_ItemChip.prefab` · `Assets/Scenes/SampleScene.unity` · `Assets/Game/Balance/Economy.csv` · `Docs/**` | 2026-09-04 | `완료` — **TAB 스탯·보유 아이템 창** (`ROADMAP` §3-1). 🔑 사용자 요구로 **멈추지 않고 느려진다**(`timeScale 0.25`) — 그래서 창은 **왼쪽만** 덮는다. 🔴 `timeScale` 은 공유 자원이라 `DoHitstop` 과 **같은 3단 계약**을 따랐고, 시험 중 레벨업이 끼어들자 **실제로 물러났다**. 🟢 아무도 안 쓰던 `Prefab_ItemChip` 을 살려 썼다. 판정 **4/4** |
| DEV | `D47` | `Assets/Scripts/Core/DisplaySettings.cs`(신규) · `Assets/Scripts/UI/{OptionPanel,CreditsPanel}.cs` · `Assets/Scripts/UI/MainMenuUI.cs` · `Assets/Scripts/Core/GameManager.cs` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **화면 설정(해상도·전체화면·품질) + 크레딧** (`ROADMAP` §3-3). 키 바인딩은 **사용자가 안 하기로**. 🔴 **비침 결함** — `UI_Panel` 스프라이트 채움 알파가 **0.92** 라 `Image.color` 알파 1 로도 뒤가 비친다. 가리는 일은 `DimBG` 몫이라 1.0 으로. 판정 **5/5**. 🔴 곁일: 저장 볼륨이 **BGM 0.059 · SFX 0.043** — 사실상 안 들린다 → `TODO.md` |
| DEV | `D48` | `Assets/Game/Sprites/Enemies/Bonecaller.png`(신규) · `Assets/Game/EnemyData/Bonecaller.asset`(신규) · `Assets/Game/Balance/{Enemies,Waves,Bosses}.csv` · `Docs/**` | 2026-09-04 | `완료` — **보스 전용 그림** (`ROADMAP` §2-5 마지막 줄, `요청-27` 재개). 🔑 **잡몹 Ogre 는 안 건드렸다** — 보스 전용 `EnemyData` 행을 새로 만들고 수치는 Ogre 복사(밸런스 변화 0). 🔴 **컨셉을 내가 정했다** (CONTENT 미가동) — 지어내지 않고 보스가 하는 일에서 끌어냈다. 이름·보스 수·걷기 시트는 **CONTENT 확인 대기** → 요청-36. 🔴 `maxTextureSize` 축소가 **PPU 도 같이 낮춰** 보스가 2배가 될 뻔했다. 판정 **7/7** |
| DEV | `D49` | `Assets/Scripts/Stage/GroundTiler.cs` · `Assets/Game/Balance/Economy.csv` · `Docs/**` | 2026-09-04 | 🟡 `절반` — **층별 바닥 테마** (`ROADMAP` §7). 🔑 새 애셋 0 — `tiles` 가 이미 얕은 것→깊은 것 순서라 **가중치를 역순으로 보간**했다. 0층 대조군 `Grass 38.4 %`(예전과 같다) · 9층 `Flagstone 38.6 %`. 🔴 **그런데 화면은 1.4 % 만 달라진다**(밝기 0.1510→0.1447) — 타일 10종이 서로 너무 비슷하다. 판정 기준과 함께 CONTENT 에 넘김 → 요청-37 |
| DEV | `D50` | `Assets/Scripts/Wave/{WaveData,WaveManager}.cs` · `Assets/Scripts/Fx/PulseFx.cs` · `Assets/Editor/BalanceImporter.cs` · `Assets/Prefabs/Fx_Burrow.prefab`(신규) · `Assets/Game/Balance/{Economy,SceneWiring,Waves}.csv` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **적 출현 패턴 4종**(사용자 요구). 흩뿌리기·**원으로 조이기**·**부대**·**땅굴**. 🔑 `Waves.csv` 에 **열을 안 늘렸다** — 맨 뒤 **`~패턴` 접미사**로 넣고 `Scatter` 는 안 쓴다 ⇒ 기존 행 바이트 동일. 판정 **7/7** (Ring 45.0~45.0도 · Squad 각도 폭 9.4도 · Burrow 예고 4/적 0 · 거리 3.43~5.55 · **배선 끊으면 20.0** 대조군). 🔴 **`D41`↔`D46` 사이를 가로지르는 결함** — `PulseFx` 는 unscaled 인데 땅굴 대기는 scaled 라 TAB 저배속에서 **예고가 2.5초 먼저 사라진다**. `useUnscaledTime` 신설. 🟡 계수 7개는 사람이 해 봐야 안다 → 요청-38 |
| DEV | `D51` | `Assets/Scripts/Enemy/{EnemyData,EnemyBase}.cs` · `Assets/Editor/BalanceImporter.cs` · `Assets/Game/Balance/Enemies.csv` · `Assets/Game/EnemyData/*.asset` · `Docs/**` | 2026-09-04 | `완료` — **행동 분화**(사용자 요구의 나머지 반쪽). 적 7종 중 **5종이 `Chaser` 하나**였다. `Flanker`(Goblin) · `Swarmer`(Slime) · `Blocker`(Ogre) 추가, Zombie 는 **대조군으로 유지**. 🔑 컨셉을 수치에서 끌어냈다 — Ogre 1.2 vs 플레이어 3.5~4.6 이라 **추격이 성립하지 않는다**. 🔑 새 프리팹 0 · **새 물리 질의 0**. 🔴 enum 은 정수 직렬화라 **뒤에만 붙였다**(보존을 판정에 넣었다). 판정 **8/8**. 🔴 **시험에서 세 번 헛짚었고 세 번 다 대조군이 잡았다** — 이미 도착한 적을 쟀고, `timeScale 0.0002` 로는 `FixedUpdate` 가 안 돌았고, 두 무리를 겹쳐 놓아 난수 방향을 쟀다. 🟡 계수 7개는 사람이 해 봐야 안다 → 요청-39 |
| DEV | `D52` | `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **요청-30 (C38) Import 1회.** 코드 0줄 · 씬 **2줄**. 🔑 Import **전** 값을 먼저 읽어 대조군을 만들었다. 판정 **7/7** — 바뀐 둘(0.14 / 320) · **유지된 셋이 안 움직였다** · 9층 Elite2 **294**(천장 320 아래). 🔴 **`WaveManager.prefab` 이 필드보다 오래됐다** — 오버라이드가 없으면 C# 기본값으로 떨어져 **"Import 했는지"를 파일로 판정할 수 없다** → `TODO.md`. 🟡 CONTENT 예측 293 vs 실제 294(`RoundToInt`) → 요청-40 |
| DEV | `D53` | `Docs/**` (검증만 · **코드·애셋 변경 0**) | 2026-09-04 | `완료` — **요청-29 (C37) 검증.** 🔑 `D49` 의 캡처가 남아 있어 **같은 크롭·같은 공식**으로 전후를 쟀다. dL **0.0069 → 0.0727 (10.5배)** · **겹침 0 %** · 층0 은 0.1510→0.1530 으로 유지 · 퀼트 **0.0134** vs CONTENT 예측 0.0138(다른 방식인데 겹친다) · 가독성 4/4 · 콘솔 0. 🟡 예측 0.0443 vs 실측 0.0727 은 **밝기 공식 차이**(단순 평균이면 0.0406). 🔴 **적탄을 오진할 뻔했다** — 1.13배가 나와 반려 직전이었는데 크롭해 보니 **그 자리에 아무것도 없었다**(`Initialize` 를 안 불러 자멸). 살려서 재니 통과 → 요청-41 |
| DEV | `D54` | `Assets/Scripts/UI/CodexPanel.cs`(신규) · `Assets/Scripts/Meta/MetaProgressionManager.cs` · `Assets/Scripts/{LevelUp/LevelUpManager,Player/PlayerStats,Enemy/EnemyBase,Evolution/EvolutionManager,Wave/WaveManager,UI/MainMenuUI}.cs` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **도감** (`ROADMAP` §3-3 의 마지막 화면). 탭 5개 한 화면. 🔑 **목록을 따로 배선하지 않는다** — 배선된 매니저에서 읽어 *"게임에 나오는 것 = 도감에 있는 것"* 이 구조적으로 보장된다. 🔑 **없던 것은 발견 기록 하나뿐**이었다(`SaveData` +3 목록, `JsonUtility` 라 옛 세이브 무사). 진화 조건은 **재료를 따로 가린다** — `??? + ???` → `Gun Lv.5 + ???` → `Gun Lv.5 + Turret Lv.3`. 판정 **8/8** (대조군: 안 나온 적은 X 로 남는다). 🔴 **검증이 결함을 잡았다** — `ApplyClass` 갈고리 누락으로 **고른 직업이 영원히 `???`** 였다. 내 스크립트가 인코딩 에러로 죽으며 `io.open('w')` 가 자기 자신을 0바이트로 날린 탓 (이후 encode-먼저 방식으로 전환). 컴파일·화면 다 멀쩡해 **런타임 판정에서만 드러났다**. ⚠️ 시험이 사용자 `save.json` 을 건드려 **원본 복구함** |
| DEV | `D55` | `Assets/Scripts/UI/CodexPanel.cs` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **도감을 아이콘 격자로 다시 짰다**(사용자 스케치). 7열 · 칸 108px · 진화 조건은 아이콘 아래 전용 줄. 🔑 **가리는 방향이 두 개다** — 이름은 `???` 로 감추고 **윤곽은 준다**. D54 는 아이콘이 40px 라 실루엣이 사실상 안 보였다. 판정 **6/6** (5탭 칸 수 12·16·7·10·7 · 배선 null 0/8 · 콘솔 0). 🔵 **52칸 전부 그림이 있다** — 예비 글자 경로는 안 쓰였지만 남겼다. 🟡 `--amend` 금지라 D54 를 고치지 않고 새 커밋으로 |
| DEV | `D56` | `Assets/Scripts/UI/CodexPanel.cs` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **도감 가시성**(사용자 스크린샷). **실루엣은 검정으로 두고 미발견 칸을 밝게 뒤집었다** · 칸 108→132px · 본문 25→32pt(자동 축소). 판정 **5/5** — 실루엣 대비 **0.1468→0.4533 (3.1배)** · 본문 4종 전부 상자 안 · 콘솔 0. 🔴 **고치다가 정답을 새게 했다** — "실루엣을 밝게" 로 갔는데 `Image.color` 는 곱셈이라 원색이 흐려질 뿐이었고 0/12 인데 무기 12종이 다 보였다. **숫자는 좋아졌으므로 캡처를 안 봤으면 개선이라고 보고했을 것이다** |
| DEV | `D57` | `Assets/Scripts/UI/CodexPanel.cs` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-04 | `완료` — **도감 닫기를 우측 상단 X + ESC 로**(사용자 요구). 격자·상세 **+60px**. 🔵 PauseMenuUI 도 ESC 를 읽지만 CanPause 가 Wave 뿐이라 안 부딪힌다 — **전제를 주석에 못박았다**. 판정 **4/4**(X · ESC · 대조군 일시정지 안 열림 · 664→724). 🔴 **처음엔 ESC 가 안 닫혔는데 시험이 문제였다** — wasPressedThisFrame 은 엣지인데 InputSystem.Update() 를 직접 불러 엣지를 소모했다 |
| DEV | `D58` | `Docs/**` (측정만 · **코드·애셋 변경 0**) | 2026-09-04 | `절반` — **C40 요청-32 실플레이 검증**. 17개 중 **6개를 숫자로 닫았다** — 0-a **vSync 켜져 있었다**(1·144Hz) · 2-a **294마리 111.3 fps** · 2-h BONECALLER·HP 2926 · 3-a **색상각 170도**+밝기 0.383 · 2-c·2-g 는 D53 값. §0-b 는 ProfilerDriver 접근 불가라 **논리로 닫았다**(에디터 오버헤드 포함 111fps ⇒ 빌드는 더 빠르다). 🔴 **§1 자동화 불가 이유를 찾았다** — 레벨업 카드 선택에서 timeScale 0 으로 멈춘다. 🟡 예측 **M ≈ 600**(이론 최대 1757G vs 배수 241G, 실측 처치당 1.65G). **사람이 해야 하는 9개**는 사용자에게 넘김 → 요청-42 |
| DEV | `D59` | `Docs/PLAYTEST.md`(신규) · `Docs/{ROADMAP,TODO,SETUP_STATUS}.md` | 2026-09-04 | `완료` — 사용자 요구 2건. ① **PLAYTEST.md 신설** — C40 중 사람만 판정 가능한 것을 체크박스로. 소유를 첫 줄에 박았다(세션은 묻기만). ② **완성률** — 기능 95 % · 콘텐츠 70 % · **체감 검증 3 %** ⇒ 가중 68 %(포트폴리오 기준 85 %대). 🔴 **곁일이 본일보다 컸다** — ROADMAP §0 현황표가 2026-08-28 값으로 방치돼 거의 모든 줄이 틀렸다(적 6→7 · 행동 3→6 · 무기 5→12 · 아이템 23→28). 같은 사실이 §0·§7 두 곳에 있어 §7 만 갱신돼 온 탓 |
| DEV | `D61` | `Docs/{SETUP_STATUS,Parallel/REQ/CONTENT}.md` (측정만 · 코드 변경 0) | 2026-09-05 | `완료` — **`C40` `1-a` 실측으로 닫음.** `Editor.log` 백그라운드 감시(MCP 불필요). 판1 `M=89`(3노드 사망) · 판2 🔴 `M=1168`(**9노드 · 상점 0회**) ⇒ **통과선 300 의 3.9배 · 실패**. 🔑 원인은 배수구를 **지나지도 못하는 것** — `weightShop 0.28` · `shopSlotCount 4` 가 맞는 자리. 🔴 **표본 하나로 일반화해 중간 보고가 틀렸다**(판2 노드당 130 G ≈ CSV 계산 134 G). 🔴 `open('wb').write(encode())` 로 SETUP_STATUS 663KB 를 0바이트로 날렸다가 복구(D54 이후 두 번째) → 요청-44 |
| DEV | `D62` | `Docs/{SETUP_STATUS,Parallel/BUGS}.md` (측정·등재만 · 코드 변경 0) | 2026-09-05 | `완료` — 세 번째 판(폭발광 `M=465`)이 진단을 바꿨다. 3판 동안 상점 **3회 열고 구매 0 · 리롤 0** ⇒ *"못 만난다"* 가 아니라 **"만나도 안 산다"**. 🔴 앞 보고 정정 2건 — 판2 는 사망이 아니라 **완주(Victory)** · 원인 진단이 좁았다. 🔴 그 자리에서 **B12 발견** — 상점 어디에도 `CanAcquire` 가 없어 칸이 꽉 차면 **골드만 사라지고 성공 로그가 찍힌다**(이번엔 구매 0건이라 안 터졌다 · 고치지 않고 등재) |
| DEV | `D63` | `Assets/Scripts/{Dev/DevPanel,Wave/WaveManager}.cs` · `Docs/**` | 2026-09-05 | `완료` — Dev 패널 **`Clear Node`** 버튼(사용자 요구). `ClearWave()` 를 그대로 부른다 — 보상·이벤트 해제·맵 진행이 그 안에 있어 흉내 내면 어긋난다. `#if UNITY_EDITOR || DEVELOPMENT_BUILD` 로 릴리즈에 치트 입구를 안 남긴다. 판정: 누른 뒤 **적 0 · 상태 StageMap · 보상 8** ✅ (맵이 넘어갔나까지 봤다). 🟡 백틱 주입으로 패널이 안 열려 렌더링은 미확인. 🔴 **D62 정정** — 상점 "구매 0" 은 설계 문제가 아니라 **사용자가 9층까지 스킵한 것**이었다. 같은 데이터를 두 번 잘못 읽었다. 🟢 B12 는 코드로 확정한 것이라 유효 |
| DEV | `D64` | `Assets/Scripts/{Dev/DevPanel,Wave/WaveManager}.cs` · `Docs/**` | 2026-09-05 | `완료` — Dev 패널 **`Spawn Boss`** + **`Wave timer`**(−30s/−10s/+30s/5s left) (사용자 요구). 🔑 **넘어가는 일(`Clear Node`)과 보는 일(`Spawn Boss`)을 안 섞었다.** 보스는 정규 경로와 **같은 세 줄**(`SpawnEnemy(isBoss:true)` → 등장음 → `AttachBossBrain`)을 쓰고, 보스 노드가 아니면 `bossWave` 것으로 대신한다. 🔴 `TimerRoutine` 의 **지역 변수를 필드로** 바꿔야 했다 — `WaveRemainingTime` 만 고치면 다음 프레임에 덮어써진다. 판정 **5/5** — 타이머 47.0→17.0 조절 후 **17.0→1.5 로 계속 흐른다** · 보스 `sprite=Bonecaller · scale 3.20 · BossBrain=True` · 경고 0. 🔴 **D63 정정** — `Clear Node` 가 보스를 건너뛴 게 아니라 **사용자가 `BossTime` 전에 눌렀다**. 로그로 의도를 추측한 게 D62·D63 에 이어 **세 번째**. 🟡 렌더링은 미확인(D63 과 같다) |
| DEV | `D65` | `Assets/Scripts/{Player/PlayerVisual,Experience/ExperienceManager,LevelUp/LevelUpManager,Wave/WaveManager,UI/ShopUI,UI/HUDManager,UI/EvolutionPromptUI}.cs` · `Assets/Game/Balance/Economy.csv` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 플레이테스트 피드백 **1차** (`C 조사 4건 → A 8건`). 🔴 **조사 3건은 고칠 게 없었다** — 폭파광 T1·Bomb 한 자루 · 승급 전용 직업은 `classes` 에 애초에 없음(`classesLen=6`) · Ogre 는 `Elite2` EliteOverride **한 군데뿐**(🔴 `D51` 의 `Blocker` 를 하필 그 적에게 줬다). 🔴 **16번은 진짜 버그** — `PlayerVisual` 의 `* (flipX ? -1 : 1)` 이 틀렸고 **주석이 반대**였다. 카메라를 플레이어 레이어만 남겨 실측: 기울기 성분 **+39.10 / +38.58 / −38.57 / −39.10** ⇒ flipX 무관. 🔴 **XP 의 진짜 원인은 `Lv11 = 810` 벽** — 예산을 40 % 늘려도 예전 곡선은 Lv13 고정(`D61` 판2 가 정확히 13). `xpTailStep` 신설, 실측 `Lv11=89`. 🔴 **D-1 은 지시와 실제가 어긋나 안 건드렸다**(승급 조건은 이미 무기 Lv5+건물 Lv3). 14번: 보스 처치 시 **남은 240s·적 2 인데 Victory**. ⚠️ **`Economy.csv` 를 건드렸다**(CONTENT 소유) — `xpThresholds` · `xpTailStep`(신규 행) → 요청-45 |
| DEV | `D66` | `Assets/Scripts/UI/{BossArrowGraphic,BossOffscreenArrowUI}.cs`(신규) · `Assets/Scripts/UI/RunEndUI.cs` · `Assets/Scripts/{Wave/WaveManager,Enemy/EnemyBase}.cs` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 플레이테스트 **B 그룹** 2건. **둘 다 애셋 0개.** 🔑 화살표는 `Graphic.OnPopulateMesh` 로 삼각형을 직접 그렸다 (폰트 `▲` 는 문자표에 없으면 빈칸 · `I-60`). 보스는 `OnBossSpawned` 구독 — `BossHealthBarUI` 와 같은 이벤트라 어긋날 수 없어 **같은 오브젝트에 얹었다**. 화살표 오브젝트는 런타임에 코드가 만든다(씬 배선 0). 클리어 화면은 **새로 잰 값이 `BiggestHit` 하나**뿐 — 나머지는 있던 값을 안 버린 것이다. 판정 화살표 **3/3**(우상단=위 테두리 30.96° · 좌하단=왼 테두리 192.68° · 화면 안 숨김) — 🔑 **두 경우가 서로 다른 축에서 잘렸다**. 🔴 상자 넘침을 실제로 쟀다(최악 391자에서 렌더높이 229.6 ≤ 230.0). 🟡 `preferredHeight` 는 자동 크기가 켜지면 넘침 판정에 못 쓴다 |
| DEV | `D67` | `Assets/Scripts/Player/{PlayerStats,PlayerVisual}.cs` · `Docs/**` | 2026-09-05 | `완료` — 버프 오라(사용자 요구 8). 🔴 **먼저 무적 타이머를 갈랐다** — 피격 i-frame 과 픽업 무적이 `_invincibleTimer` 하나를 공유해서, 피해 판정으로는 맞아도 **표시를 붙이면 스칠 때마다 오라가 번쩍인다**. 오라는 **새 애셋 0 · 새 오브젝트 0**(`SpriteOutline` 셰이더의 외곽선을 빌렸다). 🔴 그래서 `_SpriteRect` 가 필수가 됐다(4×4 걷기 시트 · `B1`). 판정 **6/6** — 대조군(피격 무적만 켜졌을 때 오라 0) 포함. 🔴 **숫자는 통과했는데 그림이 틀렸다** — 늘어난 픽셀이 전부 오라색(4876=4876)이었는데 보니 **검이 주황으로 메워져 있었다**. 실제 플레이 배율에서 다시 골라 굵기 14 → **9**. `D56` 에 이어 두 번째 |
| DEV | `D68` | `Assets/Scripts/{Evolution/EvolutionData,Evolution/EvolutionManager,Player/PlayerStats,Player/PlayerVisual,Pickup/WorldPickup,Weapon/WeaponBase,Weapon/ProjectileWeapon,Weapon/MeleeWeapon}.cs` · `Assets/Editor/BalanceImporter.cs` · `Assets/Prefabs/Pickup_Swift.prefab`(신규) · `Assets/Game/Balance/{Evolutions,SceneWiring}.csv` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 진화 특전(사용자 요구 12). **Devastator=투사체 2배 · Windforce=필드 드랍 지속 2배 · Excalibur=골드 2배**(+@ 는 DEV 판단 — 불만 2 '상점이 비쌈'과 맞물린다). `PickupKind.Swift` 신설(드랍 0.008) + 연두 오라. 🔑 투사체 배율은 `WeaponBase` **한 곳**(근접 '연타'와 원거리 '투사체'는 이름이 달라 각자 곱하면 한쪽만 고쳐진다). 판정 **6/6** — 중복 대조군 포함 · **활성 투사체 2**(화면의 발사 수로 셌다). 🔴 **D67 정정** — 커밋에 '굵기 9' 라고 적었는데 실제로는 **씬에 직렬화된 14** 가 돌고 있었다. `[SerializeField]` 로 내놓는 순간 정본은 씬이다 — **B11 을 알고도 새 필드에서 같은 함정**. 씬 값을 9 로 바로잡았다. ⚠️ `Evolutions.csv`·`SceneWiring.csv` 를 건드렸다(CONTENT 소유) → 요청-46 |
| DEV | `D69` | `Assets/Scripts/Shop/ShopManager.cs` · `Assets/Game/Balance/Economy.csv` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 상점 가격 스케일(사용자 요구 2). 🔑 *"너무 비싸다"* 와 *"비싸지게"* 는 **모순이 아니었다** — `D61` 의 두 판이 초반(`M=89` 못 산다)과 후반(`M=1168` 남아돈다)을 정반대로 가리킨다. `가격 = ShopPrice × 0.6 × (1+0.35×레벨) × (1+0.18×층)`. 환급도 **지금 가격**의 절반으로. 판정 **8/8**(Sword 70 → 0층 42 / 8층 Lv4 246 / 환급 123). 🔴 **배선 순서를 읽다 결함을 찾았다** — `LayerScaling.Layer` 는 `WaveManager.StartWave` 에서만 갱신돼 **상점 노드가 한 층 뒤처진다**. `_currentNode.Layer` 를 직접 본다(대조군: 전역값 0 인데 층 4 반영). 🟡 `B12` 여전히 열림 — 싸지면 구매가 늘어 터질 확률도 는다. ⚠️ `Economy.csv` 3행 추가(CONTENT 소유) → 요청-47 |
| DEV | `D70` | `Assets/Scripts/Stage/StageMapManager.cs` · `Assets/Game/Balance/Economy.csv` · `Docs/**` | 2026-09-05 | `완료` — 노드 배치 규칙(사용자 요구 5). 🔑 **굴림의 단위를 노드에서 층으로.** 규칙 5개(상점 연속 금지 · 자비 4층 · 엘리트 2층부터 · 한 층 전부 같은 특수 금지 · 보스 직전 선호). 판정 **400판** — 위반 4종 0 · 상점 판당 **2.65** · **0회 판 0건**(D61 이 겪은 것). 🔴 **통계가 내 결함을 두 번 잡았다** — 0층이 300/300 상점(강제 규칙이 Normal 을 갈아 끼웠다) · 1층이 400/400 상점(시작값 탓). **매판 같은 자리면 갈림길이 아니다**. ⚠️ `Economy.csv` 2행 추가(CONTENT 소유) → 요청-48 |
| DEV | `D71` | `Assets/Scripts/{Shop/ShopManager,UI/ShopCardUI,Core/GameManager}.cs` · `Assets/Game/Balance/Economy.csv` · `Docs/**` | 2026-09-05 | `완료` · 🟢 **B12 닫음** — 상점 휴식(요구 6) + 골드 전환. `ShopSlot` 에 종류(`Item`/`Heal`/`Exchange`). 평상시 `Item·Item·Heal`, 꽉 차면 `Exchange·Exchange·Heal`. 🔑 앞서 적은 A~C 는 전부 *"막는다"* 라 상점을 밟은 게 헛수고가 되는데, 사용자 답은 **다른 걸 판다** 였다. 🔴 `CanAcquire` 만으로는 만렙까지 통과 · `Purchase` 순서를 뒤집음 · 전환은 `AddPendingMetaGold`(배율 없음). 판정 **6/6**, 대조군 둘(만렙 구매 시 **골드 2000→2000** · `GoldGain 2.00` 에서도 메타 **+40 그대로**). ⚠️ 아이템 칸 3→2 · `Economy.csv` 4행 추가 → 요청-49 |
| DEV | `D72` | `Assets/Scripts/{Stage/StageMapManager,UI/StageMapUI,Shop/ShopManager}.cs` · `Docs/**` | 2026-09-05 | `완료` — **사용자가 스크린샷으로 짚은 3건 정정.** ① 세로 정렬 `(i-(n-1)/2)` 로 0 중심 대칭 ② 연결을 **단조**로(교차 판당 1.0 · 먼 점프 0) ③ 선을 **사각형 교점**까지만(최소 길이 90.0 = 200−110) ④ `D71` 의 휴식을 **살 게 없을 때만**. 🔴 **숫자가 통과한 것을 화면이 잡은 세 번째**(D56·D67) — 앞의 둘은 내가 캡처해 잡았고 이번엔 못 잡았다. `ScreenSpaceOverlay` 캔버스는 `SubmitRenderRequest` 로 못 찍어 **UI 를 볼 수단이 없다** |
| DEV | `D73` | `Assets/Scripts/Enemy/{BossBrain,BossPatternData,EnemyBase}.cs` · `Assets/Editor/BalanceImporter.cs` · `Assets/Game/Balance/Bosses.csv` · `Assets/Game/BossPatternData/BossOgre.asset` · `Docs/**` | 2026-09-05 | `완료` — 보스 패턴 2종(사용자 요구 B-1). **쫄 소환+돌진** · **예고 일직선**. 🔑 소환과 돌진을 **하나로 묶었다** — 쫄이 나타난 것 자체가 돌진의 예고가 되고 길을 막아 피할 자리를 좁힌다. 🔑 일직선은 **새 시스템이 아니다**(`BossSlam` 을 줄 세웠다 · D37 지뢰밭과 같은 부품). `EnemyBase.AiSuspended`/`DriveVelocity` 신설. 판정 **7/7** — 직선 이탈 **0.000** · 간격 1.71 균일 · 돌진 **6.16**(기대 6.05). 🔴 판정 중 결함을 찾아 **그 결함으로 판정했다**(등장 첫 프레임에 둘 다 나갔다 → 쿨다운을 안 기다리고 쟀다 → 고쳤다). ⚠️ `Bosses.csv` 12열 추가(CONTENT 소유) → 요청-50 |
| DEV | `D74` | `Assets/Scripts/Player/{StatBlock,PlayerStats}.cs` · `Assets/Scripts/Passive/{PassiveData,PassiveEffect}.cs` · `Assets/Editor/BalanceImporter.cs` · `Assets/Game/Balance/{Passives,Items,Economy,SceneWiring}.csv` · `Assets/Game/{PassiveData,ItemData}/HpRegen.asset`(신규) · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 체력 재생(사용자 요구 11). 기본 **0.4/s** + 패시브 `Regeneration`(0.4~2.6/s). 판정 **4/4**(기본 재생 오차 **0.001** · 패시브 0.40→0.80→1.20 · 만피 정지). 🔴 **판정이 아니었으면 0 인 채로 넘어갔다** — `RecalculateStats` 가 `Final` 을 필드별 나열로 만들어 새 필드가 빠졌다. 🔴 **그 자리에서 `Luck` 도 같은 이유로 빠진 것을 찾아 `B13` 등재**(고치지 않음). ⚠️ `Passives.csv` 열 추가 · 아이콘 임시 재사용 → 요청-51 |
| DEV | `D75` | `Assets/Scripts/{Evolution/EvolutionManager,Enemy/EnemyBase}.cs` · `Assets/Game/Balance/Economy.csv` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 승급 연출 강화(사용자 요구 B-5). **새 애셋 0개** — 히트스톱·충격파·흔들림·파동 3연을 **같은 순간에** 겹친다. 🔴 `TakeDamage(0)` 은 최소 1 피해가 들어가서 못 쓴다 → `PushAway` 신설(피해 0). 🔴 `WaitForSecondsRealtime` — 히트스톱이 timeScale 을 0 으로 만든다. 판정 **5/7** — 충격파 **안 16.0 / 밖 0.0**(대조군). 🟡 2·3번째 파동·흔들림은 **못 쟀다**(왕복 1초+ > 파동 수명 0.55s) ⇒ 화면 확인은 사용자 몫. 🔴 시험 헛짚음 1건(`SyncTransforms` 없이 재서 충격파가 0 으로 나왔다). ⚠️ `Economy.csv` 8행 추가 → 요청-52 |
| DEV | `D76` | `Assets/Scripts/{Wave/WaveManager,Meta/EventManager}.cs` · `Assets/Editor/BalanceImporter.cs` · `Assets/Game/Balance/{Waves,Events,SceneWiring}.csv` · `Assets/Game/WaveData/Ambush1.asset`(신규) · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 기습 이벤트(사용자 요구 9) · 🟢 **플레이테스트 24건 전부 닫힘**. 🔴 `eventWaves` 배선이 **아예 없어** 기습이 그냥 노말이었다. `Ambush1` 신설(35s · 100마리를 8초에 · 전부 `~Ring` · **Demon 제외**). 🔑 `~Ring`(D50)이 *"전맵 테두리에서"* 를 그대로 만족 — 새 패턴 불필요. 🔴 *"기습만"* 때문에 `GameEvent.WaveId` + `StartWave(node, overrideWave)` 신설(무작위로 뽑으면 **지뢰밭에도 100마리**가 쏟아진다). 판정 **6/6** — 4분면 **18/20/20/18** · 평균 거리 17.2. 🔴 또 *"이미 도착한 적을 쟀다"*(D51 과 같은 함정) → `timeScale 0.25` 로 재측정 |
| DEV | `D77` | `Docs/PLAYTEST_2.md`(신규) · `Docs/{SETUP_STATUS,TODO}.md` (문서만 · 코드 변경 0) | 2026-09-05 | `완료` — 세션 정리. `SETUP_STATUS` 2-103 에 **D63~D76 요약**(작업 14개 · 교훈 4개 · **새 애셋 2개뿐**), `PLAYTEST_2.md` 신설(**항목 56개** · 사용자가 채우는 문서). 🔴 §1 은 **DEV 가 볼 수단이 아예 없는 것들** — `ScreenSpaceOverlay` 캔버스는 렌더 텍스처로 안 잡힌다(승급 연출 3연 · 오라 · 이동 기울기 · 맵 · 보스 화살표 · 클리어 화면 · Dev 패널 렌더링). §3 에 **결정 2건**(`B13` 처리 방식 · 마감까지 무엇을 더 할지) |
| DEV | `D78` | `Assets/Scripts/Stage/GroundTiler.cs` · `Assets/Scenes/SampleScene.unity` · `Docs/**` | 2026-09-05 | `완료` — 🟢 **요청-35 처리**(이틀째 미처리였다). CONTENT 가 `C44` 에서 짚은 대로 바닥이 실제로 깨져 있었다. **어긋난 자리가 둘이었다** — ① 씬 `Grid.Cell Size` 1.0 → **0.5**(타일 0.500u 와 일치 · 칸 채움률 **25 % → 100 %**) ② `GroundTiler.EnsureBuffer` 가 **월드 유닛을 칸 수로 그대로** 썼다 → 셀 크기로 나눈다(버퍼 20.5x12.0u → **39.0x22.0u** · 화면 36.8x20.0u). 🔑 **②는 ①을 고쳐야 드러난다** — 셀이 1.0 인 동안은 두 값이 우연히 같았다. ①만 고쳤으면 가장자리가 검게 남았다. 🔴 **CONTENT 의 PPU 를 지적하는 요청을 쓰다가 내 전제가 틀린 걸 찾아 안 보냈다** — 원본이 256px 이 아니라 **1024px** 이고 `maxTextureSize` 256 이 ¼ 로 줄여 `sprite.pixelsPerUnit` 이 2048→**512** 로 따라 내려간다. **임포터 2048 은 처음부터 맞았다.** `.meta` 한 줄만 읽고 플랫폼 오버라이드 블록을 안 본 탓 |
| DEV | `D79` | `Assets/Scripts/{Stage/GroundTiler,Player/PlayerVisual}.cs` · `Assets/Game/Shaders/SpriteOutline.shader` · `Assets/Game/Sprites/Tiles/*.meta` · `Assets/Game/Tiles/*.asset` · `Assets/Game/Balance/Enemies.csv` · `Assets/Game/EnemyData/*.asset` · `Assets/Scenes/SampleScene.unity` · `Tools/Art/retint_tiles.py` · `Docs/**` | 2026-09-05 | `완료` — 사용자가 스크린샷으로 **넷**을 짚었다. 🔑 **앞 둘은 한 문제였다** — `Goblin 0.500u == 타일 0.500u` 라 바닥이 배경으로 안 물러나고 **적과 같은 층위의 격자**로 읽혔다. 타일 0.5→**0.25u** · `SizeScale` 전부 **×1.5**. 🔴 타일만 줄이면 `Fill()` 비용이 8배 → 기준점 **8칸 양자화**(다시 채우는 간격을 **2.0유닛으로 고정** — 칸 크기와 무관). 🔑 **바닥 색은 손잡이를 둘로 갈랐다** — `C37` 램프는 *층 안의 균일함*과 *층 사이의 차이*를 **같은 손잡이**로 조절해 서로를 깎고 있었다. 타일 10종을 **한 점**으로(편차 0) · 층 구분은 `Tilemap.color` 로. 🔴 **`D65` 는 부호만 고쳤다** — 전단이 피벗(가운데) 기준이라 **다리가 반대로 밀렸다**. `_LeanPivotY`(밑변) 신설. 판정 — 머리 +38.5/발 **−37.5** → 머리 +88.5/발 **+12.5** · 칸 편차 0.0138→**0.0075** · dL **0.059** · 끊김 **음수 사분면 0.00 %** · **최악 위치(나머지 7,7) 테두리 2/4096**. 🔴 `DeepTint` 한 번 되돌림(층9 가 예전의 절반이 됐다). ⚠️ CONTENT 소유 5종 변경 → 요청-55 |
| CONTENT | `C47` |
| 버그(공용) | `B13` |

> 번호를 쓸 때 이 표를 **즉시** 올린다. 선점이 곧 예약이다.
> ℹ️ 과거 이력의 `I-1`~`I-61` 은 그대로 둔다. 새 번호만 접두어 방식이다.
