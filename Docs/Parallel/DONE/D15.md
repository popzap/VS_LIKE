# D15 — 엘리트 외곽선이 실루엣으로 돌아왔다 (B1)

**세션:** DEV · **날짜:** 2026-08-30 · **버그:** [`BUGS.md` §B1](../BUGS.md)
**소유 예외:** `Assets/Game/Shaders/SpriteOutline.shader` 는 사용자 결정으로 **DEV 가 직접** 고쳤다

---

## 왜 했나

사용자가 실플레이에서 **"엘리트 외곽선이 실루엣과 어긋난다"** 고 보고했다.
B1 에 이미 원인이 "옆 프레임 알파를 빨아들인다"로 적혀 있었는데,
**고치기 전에 재 보니 그건 절반이었다.**

---

## 진짜 원인은 둘이었고, 순서가 뒤바뀌어 있었다

### ① 주범 — `_OutlineTexSize` 가 `512` 로 굳어 있었다

선 굵기는 `d = _OutlineWidth / _OutlineTexSize` 라는 **uv 거리**다.
`_MainTex_TexelSize` 를 쓰면 2D SRP Batcher 가 머티리얼을 통째로 배칭에서 빼기 때문에
텍스처 크기를 **직접 넘기는** 구조인데, 그 값이 셰이더 기본값 `512` 에서 멈춰 있었다.

I-58 이 적을 **1024 시트**로 바꾸는 순간 이 값이 틀린 값이 됐다:

| | 낱장 png 시절 (512) | 시트 지금 (1024) |
|---|---|---|
| `_OutlineWidth 14` 의 실제 굵기 | **14 텍셀** | **28 텍셀** |

그런데 시트의 **한 프레임 자체가 130~230텍셀**뿐이다.

| 적 | 프레임 높이 | 28텍셀 선이 차지하는 비율 |
|---|---|---|
| Slime | 118 | **23.8 %** |
| Wolf | 133 | **21.1 %** |
| Goblin | 135 | **20.8 %** |
| Demon | 190 | 14.7 % |
| Zombie | 194 | 14.4 % |
| Ogre | 206 | 13.6 % |

낱장 시절엔 같은 설정이 **키의 2.7 %** 였다. 즉 사용자가 본 것은
선이 아니라 **캐릭터를 덮은 보라 덩어리**다. 이게 체감의 대부분이었다.

> 🔴 B1 원문은 이 항목을 *"두께 문제라 TUNING 감"* 으로 미뤄 뒀다. **그 판단이 틀렸다.**
> 512 는 취향값이 아니라 **텍스처 크기와 안 맞는 코드 결함**이다.
> 취향값은 그 다음 단계인 `_OutlineWidth`(몇 텍셀로 할지)이고, 그건 CONTENT 로 넘겼다.

### ② 옆 프레임 번짐 — 있지만 예측보다 훨씬 드물다

B1 원문의 표는 프레임이 **256 격자 칸에 꽉 찬다**고 가정하고 여백을 셌다.
실제 `Sprite.textureRect` 는 **타이트 슬라이싱**이라 칸이 아니다:

```
Wolf frame_5 textureRect = (274.08, 580.08, 223.90, 117.85)   ← 소수점. 격자가 아니다
```

내 여백 + 이웃 여백이 합쳐지므로 28텍셀을 넘는 경우가 드물다. 96프레임 전수 조사:

| 적 | 번지는 픽셀 | 판정 |
|---|---|---|
| Demon f14 | **2616 px** | 🔴 실제로 번진다 |
| Wolf | 0~수 px | 사실상 없음 |
| 그 외 4종 | 0 px | 없음 |

그래도 고쳤다. **드물다 ≠ 안 고쳐도 된다** — 앞으로 시트를 빽빽하게 채우면 바로 재발하고,
고쳐 두면 CONTENT 에 "여백 30px 이상" 같은 제약을 요구하지 않아도 된다.

---

## 무엇을 했나

| 파일 | 무엇 |
|---|---|
| `Assets/Game/Shaders/SpriteOutline.shader` | `_SpriteRect` 프로퍼티 신설(기본 `(0,0,1,1)`= 텍스처 전체 = 낱장 png 일 때 정답). `SampleAlpha` 의 `0~1` 검사를 **이 사각형 검사**로 교체. `_OutlineTexSize` 주석에 512 의 내력을 남김 |
| `Assets/Scripts/Enemy/EnemyVisual.cs` | `ApplySpriteRect(Sprite)` 신설. `Setup` 끝 + `StepFrames` 2곳에서 호출 |

```cs
r       = new Vector4(tr.xMin/tw, tr.yMin/th, tr.xMax/tw, tr.yMax/th);
texSize = Mathf.Max(tw, th);   // uv 거리는 가로세로 공통이라 기준도 하나여야 한다
```

**스프라이트가 바뀔 때만** 넘긴다 (`_rectSprite` 캐시). `SetPropertyBlock` 은 매 프레임 부를
만큼 싸지 않고, 프레임은 초당 10장 남짓만 바뀐다.

⚠️ **풀 재사용 함정을 같이 막았다.** `Setup` 에서 `_rectSprite = null` 로 캐시를 깨고 **항상**
다시 넘긴다 — 안 그러면 늑대였던 개체가 슬라임으로 재활용될 때 `_SpriteRect` 에 **남의 칸**이
남아 외곽선이 엉뚱하게 잘린다. (`_frames` 를 지우는 기존 주석과 같은 이유다.)

---

## 어떻게 확인했나

🔴 **A/B 렌더가 처음에 `diff px = 0` 을 냈다. 여기서 "고쳐졌다"고 넘어가면 안 됐다** —
값이 셰이더에 도달조차 못 한 경우와 구분이 안 되기 때문이다.

### 대조군을 먼저 세웠다

`_SpriteRect` 를 **일부러 아주 작은 상자**로 강제했더니 보라 픽셀이 **32821 → 4710** 으로
떨어졌다. → **MPB 는 `CBUFFER_START(UnityPerMaterial)` 안의 프로퍼티도 덮어쓴다**가 증명됐다.
그 다음에야 `diff = 0` 이 "이 표본엔 번질 이웃이 없다"는 뜻임을 말할 수 있었다
(표본이던 Wolf frame_5 는 28텍셀 안에 이웃이 없다. Demon f14 로 바꾸니 2616px 이 나왔다).

### 오프라인 A/B 렌더 장치 (에디트 모드)

`HideFlags.HideAndDontSave` 오브젝트를 레이어 31 에 놓고, `cullingMask = 1<<31` 인
직교 카메라로 `RenderTexture` 에 그린 뒤 `ReadPixels` → `EncodeToPNG` 로 `Temp/` 에 떨궜다.
**씬을 더럽히지 않고 프로브 MonoBehaviour 도 필요 없다.**

> ℹ️ 스프라이트 원본 텍스처는 `isReadable=false` 라 `GetPixels32` 가 안 된다.
> png 를 `File.ReadAllBytes` + `ImageConversion.LoadImage` 로 일회용 `Texture2D` 에 올려 읽었다.

### 실플레이 검증 — 살아있는 적 14마리

```
살아있는 적 14   _SpriteRect 불일치 0   _OutlineTexSize 틀림 0   외곽선 씌운 수 4
Enemy_Goblin(Clone) frame_10 tex=1024 want=(0.5147,0.3165,0.7363,0.4511) got=(동일) texSize=1024
```

서로 **다른 프레임 3종**(`frame_2`·`frame_10`·`frame_14`)이 각자 자기 사각형과 일치했다 =
스폰 때 한 번 넣고 마는 게 아니라 **프레임마다 따라간다**는 뜻이다.

### 판정

| # | 확인할 것 | 결과 |
|---|---|---|
| ① | 셰이더가 `_SpriteRect` 를 받는다 | ✅ 대조군으로 증명 (32821 → 4710) |
| ② | `EnemyVisual` 이 프레임마다 갱신한다 | ✅ 실플레이 14마리 불일치 0 · 다른 프레임 3종 확인 |
| ③ | `_OutlineTexSize` 가 실제 크기다 | ✅ 14마리 전부 `1024` |
| ④ | 옆 칸 알파를 안 빨아들인다 | ✅ Demon f14 오프라인 렌더 2616px → 0px |
| ⑤ | 낱장 png 적이 망가지지 않는다 | ✅ 기본값 `(0,0,1,1)` = 텍스처 전체 = 종전과 동일 |
| ⑥ | 게임 화면에서 선이 실루엣에 붙는다 | ✅ 게임 카메라 캡처 — 고블린 2마리 보라 선이 윤곽을 탄다 |
| ⑦ | 컴파일 에러 0 | ✅ 콘솔 8건 전부 `Log`, Warning·Error **0** |
| ⑧ | 최종 굵기가 보기 좋은가 | 🟡 **사람이 봐야 한다** → CONTENT 로 이관 |

---

## 알게 된 것

### 🔴 `IN.uv` 는 스프라이트 기준이 아니라 **텍스처 기준**이다

`if (uv.x < 0 || uv.x > 1)` 같은 검사는 **낱장 png 일 때만 우연히 맞는다.**
아틀라스·시트를 쓰는 순간 이 검사는 한 번도 걸리지 않는다.
앞으로 스프라이트 셰이더에서 **이웃 샘플링**을 하면 반드시 사각형을 같이 넘길 것.

### 🔴 `Sprite.textureRect` 는 **격자 칸이 아니라 타이트 bbox** 다

슬라이서가 알파에 딱 붙여 자르므로 소수점 좌표가 나온다.
"칸이 256 이니 여백이 몇 px" 같은 계산은 **틀린 전제**다. 실제 rect 를 읽어야 한다.

### `MaterialPropertyBlock` 은 `UnityPerMaterial` 안의 프로퍼티도 덮어쓴다

SRP Batcher 와 MPB 가 상충한다고 알려져 있어 확인이 필요했다. 실측으로 덮어써진다.
(대신 MPB 를 쓰는 렌더러는 배처에서 빠진다 — 적은 어차피 `_AnimPhase` 때문에 이미 빠져 있다.)

---

## 남은 것

- 🟡 **최종 굵기는 사람이 정한다.** 지금은 `_OutlineWidth` 엘리트 **14** · 보스 **12** 가
  *실제* 텍스처 크기 기준으로 걸린다 = 낱장 시절 체감으로 치면 **2배**다.
  낱장 시절과 똑같이 보이게 하려면 **7 / 6**. → [`REQ/CONTENT.md` 요청-9](../REQ/CONTENT.md)
- `Assets/Game/Materials/SpriteOutline.mat` 은 `_OutlineTexSize` 를 **직렬화하고 있지 않다**
  (셰이더 기본값 512 를 그대로 씀). 지금은 `EnemyVisual` 이 매번 덮어쓰므로 문제가 없지만,
  **적 이외의 것**이 이 머티리얼을 쓰면 512 를 다시 밟는다. 현재 사용처는 적뿐이다
- `B6`(무기 칸 하나 모자람)은 이번에 **고치지 않고 등재만** 했다
