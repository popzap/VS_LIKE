# C45 — `D78` 확인 · DEV 의 경제 변경을 내 문서에 반영 (CONTENT)

**날짜:** 2026-09-05 · **애셋 변경 0** · **출력:** [`REQ/DEV.md`](../REQ/DEV.md) 요청-37

---

## 1. ✅ 바닥 — 내 경보가 맞았고, 어긋난 자리는 **둘**이었다

`C44` 에서 *"`Grid.Cell Size` 가 아직 1,1 이라 바닥이 깨져 있을 수 있다"* 고 넘겼고
`D78` 이 **실제로 깨져 있었다**고 확인했다. 그런데 자리가 하나 더 있었다:

| | 무엇 |
|---|---|
| ① | 씬 `Grid.Cell Size` `1.0 → 0.5` — 칸 채움률 **25 % → 100 %** |
| ② | `GroundTiler.EnsureBuffer` 가 **월드 유닛을 칸 수로 그대로** 썼다 |

🔑 **②는 ①을 고쳐야 드러난다.** 셀이 1.0 인 동안에는 **월드 유닛과 칸 수가 우연히 같아서**
아무 증상이 없었다. ①만 고쳤으면 화면 가장자리가 검게 남았을 것이다.

⇒ **한 값이 두 뜻으로 쓰이는 자리는, 그 두 뜻이 갈라지기 전까지 안 보인다.**

### 내가 다시 쟀다 — 0.500 유닛으로 정확하다

```
maxTS  256 -> 텍스처  256px · 실효 PPU  512 · 월드 0.500 유닛
maxTS 2048 -> 텍스처 1024px · 실효 PPU 2048 · 월드 0.500 유닛
```

`D78` 이 *"`maxTextureSize` 가 PPU 를 같이 낮춘다"* 고 한 게 맞다 —
**어느 쪽으로 임포트되든 0.5 유닛**이라 내 `PPU 2048` 은 처음부터 맞았다.
🟢 **그래서 나를 지적하는 요청을 쓰다 말고 접은 것도 맞는 판단이었다.**

---

## 2. 🔴 내 문서가 낡아 있었다 — `ShopPrice` 가 **정가가 아니게 됐다**

`D69` 가 상점 가격을 **고정가 → 층·보유 레벨 스케일**로 바꿨다.

```
가격 = ShopPrice × 0.6 × (1 + 0.35 × 보유레벨) × (1 + 0.18 × 층)
```

| Sword (`ShopPrice 70`) | 미보유 | Lv4 보유 |
|---|---:|---:|
| 0층 | **42** | 101 |
| 8층 | 102 | **246** |

**`C27` 이 잡은 `40~110` 이 "정가"에서 "기준가"로 바뀌었다.**
그런데 내 `BALANCE.md` 는 그걸 모르고 여전히 정가처럼 적어 두고 있었다.

⇒ `Items.csv` 절에 **곱셈식과 층별 표**를 넣고, `§3-1` 의 옛 계산에는
*"절대값이 아니라 비율로 읽을 것"* 경고를 달았다.
`TUNING.md` §H 의 배수구 표에도 *"낡았다"* 를 붙였다 — 가격도 방문 횟수(1.2 → **2.65회**)도 바뀌었다.

---

## 3. 🔑 DEV 의 해법이 **내가 준비해 둔 답보다 나았다**

`TUNING.md` A-1 항목에 내가 이렇게 적어 뒀다:

> *"첫 상점이 빈손이면 낮추는 건 전 품목이 아니라 **패시브만** (40~60 → 30~45)"*

`D69` 는 대신 **`priceBaseMult 0.6` 으로 0층 전체를 0.6배**로 만들었다(가장 싼 칸 40 → **24**).

🔑 **초반이 비싼 건 품목 문제가 아니라 시점 문제였다.**
품목을 골라 내리면 **그 품목만** 싸지고, 시점으로 풀면 **초반 전체**가 싸진다.
⇒ 내가 적어 둔 대응을 **그 해법으로 갈아 끼웠다.**

`C43` 에서 *"체크리스트에 적어 둔 대응을 반사적으로 실행하면 안 된다"* 를 배웠는데,
이번엔 **더 나은 답이 나왔을 때 내 답을 버리는** 쪽이었다.

---

## 4. 🟡 찾은 것 — 환급에 작은 차익이 생겼다

`GetRefundAmount = GetPrice() × 0.5` 인데 `GetPrice` 가 **지금 층·보유 레벨** 기준이다.
⇒ **싸게 사서 비싸게 되팔 수 있다.**

```
Sword: 0층 구매 42 -> 8층 환급 69 = +27 G
슬롯 3개를 다 굴려도 82 G  (잉여 1168G 의 7 %)
```

🟢 **손대지 않았다.** 되파는 동안 슬롯을 차지하고, 크기도 작다.
🔴 **다만 `priceLayerStep` 을 올리면 이 차익이 같이 커진다** — 그때는 같이 봐야 한다고 적어 뒀다.

**크기를 재고 나서 "안 고친다"고 한 것**이 요점이다. 재지 않았으면 둘 중 하나였을 것이다 —
못 보고 넘어가거나, 7 %짜리를 마감 이틀 전에 고치겠다고 붙들거나.

---

## 5. 남은 것 — 프리팹 한 줄

`Pickup_Swift.prefab` 의 `m_Sprite` 가 아직 Haste guid 다. `요청-36 §2` 그대로 열려 있다.
🟢 `PLAYTEST_2` 2-I 에 DEV 가 *"지금은 색만 바꾼 것"* 이라고 경고를 달아 둬서
**사용자가 자리표시를 판정할 위험은 없다.**

`PLAYTEST_2` 에 내 항목 셋(`2-I` 이속 · `2-J` 바닥 · `2-K` Ogre 접촉 피해)이 **이미 들어 있다.**
`C42` 때는 내가 남의 파일에 직접 썼는데 이번엔 **DEV 가 먼저 넣어 줬다** — 그게 맞는 순서다.

---

## 6. 남긴 것

- 🔑 **한 값이 두 뜻으로 쓰이면, 그 두 뜻이 갈라지기 전까지 안 보인다.**
  `EnsureBuffer` 의 월드 유닛/칸 수는 셀이 1.0 인 동안 **우연히 같았다.**
  내 변경이 그 우연을 깨서 드러났다 — **고친 자리가 아니라 그 옆에서 터진다**
- 🔴 **내가 만든 값의 "뜻"이 남의 손에서 바뀔 수 있다.** `ShopPrice` 는 내가 정한 정가였는데
  `D69` 뒤로 **기준가**가 됐다. 값은 그대로인데 의미가 달라졌고, **내 문서만 옛 뜻으로 남아 있었다**
- **더 나은 답이 나오면 내 답을 버린다.** *"패시브만 내린다"* 보다 *"초반 전체를 0.6배"* 가 낫다 —
  **품목 문제가 아니라 시점 문제**였기 때문이다
- **작은 것도 재고 나서 "안 고친다"고 한다.** 환급 차익 7 % 를 재지 않았으면
  못 보고 넘어가거나 마감 이틀 전에 붙들었을 것이다. **크기를 알아야 무시할 수 있다**
