# D7 — 진화 무기 3종 아이콘 배선 (DEV)

> 2026-08-30 · 병렬 세션 DEV · [`REQ/DEV.md` 요청-5](../REQ/DEV.md) (CONTENT `C6`) 처리

## 왜 했나

진화 무기 Excalibur / Windforce / Devastator 가 **재료 무기 아이콘을 그대로 쓰고 있었다.**
진화했는데 인벤토리 그림이 안 바뀌면 "뭐가 달라졌지?"가 된다.

🔴 **그림이 없던 게 아니라 배선이 빠져 있었다.**
전용 png 3장은 **I-57(20차, `a85cc9f`)에 이미 들어와 있었다.**
빠진 것은 `Weapons.csv` 의 `Icon` 열 하나뿐이었고, 그래서
`TODO.md` §4 에는 20차 시점에 이미 "진화 무기 아이콘도 교체했다"고 적혀 있었다 — **사실이 아니었다.**

CONTENT 가 `4f1a92c`(C6) 로 CSV 를 고쳐 두었으므로 **DEV 는 Import 1회**만 하면 됐다.
CSV 는 CONTENT 소유, `WeaponData/*.asset` 은 DEV 소유라 이렇게 갈린다.

## 한 것 — `Game/Balance/Import CSV -> ScriptableObjects` 1회

Import 전에 guid 3쌍을 먼저 기록해 두고 실행했다. 그러지 않으면
"원래 그랬던 것"과 "바뀐 것"을 나중에 구분할 수 없다 (D6 의 교훈 125 와 같은 함정).

| 무기 | 전 — 재료 무기 아이콘 | 후 — 전용 png |
|---|---|---|
| Excalibur | `009d725fe54e4a1499ff3903c4bf378e` (Sword) | `c35a002a740ab5c4aa5a673ce063905a` |
| Windforce | `7cb2b59ea0ed3e44e91c54ebd05f2221` (Bow) | `077dea53464b16546941e766e8074cd3` |
| Devastator | `0cfefbeb330f45c42918c8beb48a1d2d` (Gun) | `e515f1078e686b444a21c688bbaafe9f` |

## 검증 — 판정 기준 4개

| # | 기준 | 결과 |
|---|---|---|
| ① | Excalibur 의 `Icon` 이 Sword guid 가 아니다 | ✅ `c35a002a…` |
| ② | Windforce · Devastator 도 각자 전용 png | ✅ `077dea53…` / `e515f107…` |
| ③ | 🔴 **나머지 필드가 안 바뀐다** | ✅ `git diff` 가 **파일당 `Icon` 한 줄뿐.** `Damage` 70/55/34 그대로 |
| ④ | Import 에러 0 | ✅ `[BalanceImporter] Import 완료` 1건 · Warning·Error 0 |

③ 이 중요하다. Import 는 CSV 전체를 다시 쓰므로 **의도한 열 하나만 바뀌었는지**를
diff 로 확인하지 않으면 다른 무기 수치가 조용히 되돌아가도 알 수 없다.
`Economy.csv : 43/43 적용` 도 그대로라 **전날 D6 의 `CombatFeel` 12줄도 무사하다.**

**실플레이 검증은 하지 않았다** — 요청-5 가 면제했다.
진화 아이템은 보물상자에서만 나와 화면에 띄우는 비용이 크고,
아이콘은 `WeaponData.Icon` 을 UI 가 그대로 그리는 구조라 guid 3개면 충분하다.

## 같이 닫은 것 — `TODO.md`

| 위치 | 무엇 |
|---|---|
| `:328` | `**진화 결과 3종 전용 아이콘 없음**` → ✅ 해결 (배선이 빠져 있었다는 사실을 남김) |
| `:409` | I-57 절의 "진화 무기 아이콘도 교체했다" 문장을 **사실대로** 고침 — 그때는 png 만 들어왔고 배선은 30차에 끝났다 |

`:41` `:500` 의 "17장 생성·**배선** 완료"는 20차 시점엔 틀린 문장이었으나
**이번 작업으로 실제로 참이 되었다.** 그대로 둔다.
