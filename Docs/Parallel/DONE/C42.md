# C42 — `PLAYTEST.md` 에 숫자 3개 추가 + 사거리 오류 정정 (CONTENT)

**날짜:** 2026-09-04 · **사용자 지시:** *"`PLAYTEST.md` 에 측정 필요한 거 있으면 밑에 추가해, 필수적인 것만"*

---

## 1. ⚠️ 남의 파일을 썼다 — 사용자 지시라 썼고, 최소로 썼다

`Docs/PLAYTEST.md` 는 `D58`/`D59` 가 만든 **DEV 소유 파일**이다.
사용자가 직접 지시해서 **맨 아래에 한 절만 덧붙였다.** 기존 13개는 **한 글자도 안 건드렸다.**

**중복을 안 만드는 게 먼저였다.** 바로 앞 `C41` 에서 내가 DEV 에게
*"같은 항목을 양쪽에 두지 말 것 — 물어보는 문장이 두 군데 있으면 언젠가 어긋난다"* 고 못 박았다.
⇒ **전문을 읽고 기존 13개와 겹치지 않는 것만** 골랐다.

---

## 2. 셋만 고른 기준

**① 판 B 에서 그냥 얻어진다**(새 판이 필요 없다) · **② 답이 숫자거나 예/아니오** ·
**③ 그 답으로 미뤄 둔 결정이 닫힌다.**

| # | 무엇 | 왜 필수인가 |
|---|---|---|
| **D-1** | 승급까지 몇 분 | `TODO.md` §2 의 미결정 **2건**(*T2 를 3개만 둘지* · *T4 를 만들지*)이 **이 숫자 하나만** 기다린다 |
| **D-2** | 검만으로 Demon 을 잡을 수 있나 | **"느낌"이 아니라 "되나 안 되나"** 다 (§3) |
| **D-3** | 무기 렉이 실제로 나는가 · **그때 뭘 하고 있었나** | `PERF.md` §0-1 의 **유일한 미해결 항목**. `D35` 가 **하네스로 재현을 못 했다** — 사람은 그 조건을 그냥 만든다 |

🔑 **D-3 의 값은 "예/아니오"가 아니라 그 뒤의 한 줄이다.**
하네스가 못 만든 조건을 사람이 우연히 만들었을 때, **무엇을 하고 있었는지가 재현 단서**다.

**오디오 18개는 여전히 안 넣었다** — 사용자가 *"필수적인 것만"* 이라 했고 `C40` 의 기준이 그대로다.

---

## 3. 🔴 그러다 **내 문서의 숫자가 틀린 걸 찾았다**

`D-2` 를 쓰려고 실제 사거리를 확인했는데, 내가 여러 곳에 쓴 **"무기 사거리 3.5~6" 이 사실이 아니었다.**

| | 사거리 (Lv1~5) |
|---|---|
| **검** | **2.0 ~ 2.5** |
| 문어 | 1.8 ~ 2.6 |
| 독 장판 | 4 ~ 6 · 드래곤 7~9 |
| 폭탄 9~11 · **총 11~15** · **화염구 12~14** · **수리검 12~16** · **활 14~18** · 진화 16~20 | |

**출처는 `SETUP_STATUS.md:1586` 의 옛 메모**고, 내가 `C30`·`C37` 에서 **확인 없이 옮겨 적었다.**
`BALANCE.md`(`Bosses.csv` 절)와 `TUNING.md`(§B · §J)에 정정 블록을 넣었다.

### 결론은 안 바뀌고 설명이 좁아진다

🟡 **`SlamWindup 1.15` 는 그대로다.** 회피 계산이 `반경 ÷ 예고 ≤ 이동속도` 라
**무기 사거리와 무관**하다. 틀린 건 **곁들인 설명**이지 계산이 아니었다.

🔴 **다만 이건 좁아진다** — *"보스에 딜을 넣으려면 사거리 안에 들어가야 하니
소환수가 그 자리를 막는 게 위협이 된다"* 는 **근접(검·문어) 빌드에서만** 성립한다.
**원거리는 11~18 에서 때리므로 접근할 필요가 없다.**

---

## 4. 🔑 그래서 `D-2` 가 생각보다 큰 항목이 됐다

Demon 은 `Ranged` 라 **7유닛을 유지하며 물러난다.** 검은 **2.5** 다.

⇒ **검만 든 빌드는 Demon 을 구조적으로 못 때린다.**
원거리 무기는 전부 7보다 길어 문제가 없고 **짧은 건 검(2.5)과 문어(2.6)뿐**이다.
전사가 레벨업에서 원거리를 한 장도 못 뽑으면 **Demon 이 나오는 층에서 아무것도 못 한다.**

못 잡는다는 답이 오면 `Demon.PreferredRange` `7 → 5` — **`Enemies.csv` 라 내 표다.**

---

## 5. 남긴 것

- 🔴 **남이 쓴 옛 메모를 확인 없이 옮기면 그게 내 문서의 사실이 된다.**
  *"3.5~6"* 은 `SETUP_STATUS.md` 한 줄이었는데 내가 `C30`·`C37` 두 작업의 **논거로 썼다.**
  🔑 **DEV 의 "고쳐 둬"를 세 번 확인해 놓고(`C38`·`C39`·`C41`) 정작 내가 옮긴 값은 안 쟀다**
- **결론이 맞아도 전제를 고친다.** `SlamWindup` 은 안 바뀌지만, 안 고치면
  다음에 그 전제로 다른 결론을 낸다 — `C36` 에서 같은 걸 적어 놓고 또 겪었다
- **항목을 고를 때 "새 판이 필요한가"를 먼저 본다.** 셋 다 판 B 에 얹히므로
  **사용자에게 드는 추가 비용이 0** 이다. 값이 큰 항목이라도 새 판을 요구하면 안 하게 된다
- **남의 파일에 쓸 땐 최소로 쓰고 알린다.** 맨 아래 한 절, 기존 항목 무변경,
  그리고 *"다음부터는 요청으로 넘길 테니 네가 넣어 달라"* 까지 적었다
