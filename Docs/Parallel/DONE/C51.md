# C51 — 배수구를 닫았고, 지갑 하나가 안 도는 걸 찾았다 (2026-09-06)

> **애셋 0 · 코드 0.** `D96` 이 남긴 **Dev 패널 없는 한 판**으로 `§H` 를 닫았다.

---

## 1. 먼저 판이 깨끗한지부터 쟀다

`D61` 의 세 판(`M` 89 / 1168 / 465)을 버린 이유가 **Dev 패널 층 스킵 오염**(`D63`)이었다.
그래서 숫자를 읽기 **전에** 그것부터 봤다.

| 구간 | `DevPanel` 흔적 |
|---|---:|
| **대상 판** (`Editor.log` 22397 ~ 24200) | **0줄** |
| 바로 뒤 구간 (24200 ~ 26100) | `강제 클리어` **3줄** |

🔑 **대조군이 없으면 "0줄" 은 아무 뜻도 없다** — grep 이 틀렸는지 판이 깨끗한지 못 가른다.
바로 뒤 구간에서 3줄이 나온 게 **grep 이 작동한다는 증거**다.

⚠️ 처음 로그를 훑었을 땐 `DevPanel 강제 클리어` 가 **보였다**(24646 · 26001).
그런데 정산 줄이 **24200** 이라 **그건 다음 판 것**이었다. 구간을 안 잘랐으면 멀쩡한 판을 버릴 뻔했다.

---

## 2. 🟢 배수구 — 뚫렸다

**판 조건** — Warrior→Warden · 1스테이지 10노드(전투 4 · 이벤트 4 · **상점 2**) · 보스 처치 완주.

| 런 골드 흐름 | 액수 |
|---|---:|
| 리롤 4회 (25 + 40 + 55 + 70) | −190G |
| 구매 `Sword` | −210G |
| 구매 `Damage Up` | −101G |
| 이벤트 환전 (런 87 → 메타 29) | −87G |
| **지출 합** | **−588G** |
| 제거 환급 (`Octopus` +84 · `Turret` +96) | **+180G** |
| **소멸 (`M`)** | **74G** |

⇒ 수입 **482G**, 환급까지 가용 **662G**, 그중 **588G(88.8 %)를 썼다. 소멸 11.2 %.**
`C27` 이 걱정한 *"절반 넘게 소멸"* 은 끝났다. 통과선 `M ≤ 300` 을 크게 밑돈다.

### 🔴 뚫은 것이 내가 적어 둔 처방이 아니었다

`C27` 은 세 가지를 적어 뒀다 — `weightShop` 올리기 · `shopSlotCount` 4~5 · 새 소비처 신설.
**하나도 안 쓰였다.** 실제로 뚫은 건 이것들이다:

| 뚫은 것 | 어떻게 |
|---|---|
| `D69` 층·레벨 가격 스케일 | `Sword` 기준 70 → 이 판에서 **210G**. 한 자루가 판 수입의 **44 %** |
| `D70` `shopPityLayers 4` | 상점 0회인 판이 사라졌다 |
| **`D71` 제거 환급** | +180G 를 회수해 **그 돈으로 다시 샀다** — 배수구가 **한 번 더 돈다** |
| **환전 이벤트** | `C27` 표엔 **아예 없던** 소비처. 런 87 을 메타 29 로 빼내 간다 |

🔑 **내 처방 목록이 틀린 게 아니라 목록 밖에서 풀렸다.** 배수구를 *"상점 방문 × 칸 수"* 로만
모델링했는데, 실제로는 **되파는 고리**와 **상점이 아닌 소비처**가 있었다.
⇒ **이제 `weightShop`·`shopSlotCount` 를 올리면 안 된다.** 89 %를 흡수하는데 더 넓히면 **모자란다.**

⚠️ **`M` 1168 → 74 를 "16분의 1" 로 읽지 않았다.** 1168 은 9층 도는 오염된 판이고
이건 1스테이지 10노드다 — **판 길이가 다르다.** 길이에 안 휘는 값은 **비율 11.2 %** 쪽이다.

---

## 3. 🔴 리롤 값이 틀려 있었다 — 로그 문구가 낚는다

`D96` 이 `PLAYTEST_2` 에 *"리롤 4회(**누계** 40→55→70→85G)"* 라 적었는데 **누계가 아니다.**
`ShopManager.cs:156` 이 **증가시킨 뒤** `:164` 에서 찍으므로 그 값은 **다음 리롤 가격**이다.

`base 25` · `increase 15` (`Economy.csv`:47-48 · 씬에서도 확인) ⇒ 실제로 낸 값은
**25 + 40 + 55 + 70 = 190G**. 85 도 250 도 아니다.

이 차이로 그 판의 **수입 추정이 377G → 482G (+28 %)** 로 바뀐다.
⇒ 문구를 `비용 누계` → `다음 리롤` 로 (요청-42 ②).

🔑 **값이 아니라 이름이 틀린 것**이라 동작은 멀쩡하다. 그런데 **이미 한 번 오독을 만들었다.**
로그 문구도 `C49` 의 도감 한 줄과 같다 — **읽는 사람에게 닿는 표면**이다.

---

## 4. 🔴 곁에서 더 큰 걸 찾았다 — **48판 동안 영구 강화 0개**

`§1-9 ③`(*"첫 판이 끝났을 때 하나라도 샀나"*)을 닫으려고 `save.json` 을 **읽었다**(안 건드렸다).

```json
"Currency": 1794,  "TotalRuns": 48,
"UpgradeLevels": { "Keys": [], "Values": [] }
```

| | |
|---|---:|
| 잔고 | **1,794G** |
| 가장 싼 칸 `Magnetism` | 60G — 잔고의 **1/30** |
| 7종 1단계 전부 | 650G — 잔고로 **2.7번** 산다 |
| 나무 전체 | 3,910G — 잔고가 **46 %** |

🔴 **내 처방(`60 → 45`)을 폐기했다.** 가격 가설이 죽는다 —
**가장 싼 칸의 30배를 들고 안 샀다.** 5G 를 깎아도 아무 일도 안 난다.

🔑 **같은 판에서 지갑 둘이 정반대로 굴렀다** — 런 골드 **88.8 % 소비** ↔ 메타 골드 **0 % 소비**.
⇒ 문제는 **경제가 아니라 그 화면에 닿느냐**다.

배선은 파일로 확인했다. 멀쩡하다:
`MainMenuUI.metaButton`(씬 배선됨) → `OnMetaClicked()` → `ChangeState(MetaScreen)`
→ `GameStatePanel` → `MetaScreenUI`. 저장도 `PurchaseUpgrade` → `UpgradeLevels.Set`.
⇒ 남은 갈래는 **둘뿐이고 파일로는 못 가른다**: *안 눌렀다* / *눌렀는데 빈손으로 나왔다.*

🔴 **`C49` 와 같은 계열이다** — 데이터도 배선도 멀쩡한데 **사람이 그 값에 닿지 못한다.**
`C49` 는 도감에서 **한 줄**이 빠진 거였고, 이건 **화면 통째**다.
**마감이 내일(9/7)** 이라, 지금 상태면 영구 강화는 **포트폴리오에서 한 번도 안 보인 시스템**이다.

⚠️ **답을 듣기 전에 수치를 만지지 말 것.** 갈래에 따라 고칠 곳이 정반대다 —
*안 눌렀다* 면 **메인 메뉴 버튼**, *빈손* 이면 **`MetaScreenUI` 안**이고,
**`Upgrades.csv` 는 어느 쪽도 아니다.**

---

## 5. 이번에 배운 것

🔑 **"쓸 곳이 없다" 와 "쓸 줄 모른다" 는 증상이 같다 — 잔고가 남는다.**
런 골드는 전자였고 고쳐졌다. 메타 골드는 **후자**인데 **같은 처방을 적어 뒀었다**(가격 인하).
잔고만 보고 처방을 쓰면 절반은 틀린다. **가르는 건 잔고가 아니라 잔고 ÷ 최저가**다 —
런 골드는 74 / 210 = **0.35배**(못 산다), 메타 골드는 1794 / 60 = **30배**(안 산다).
