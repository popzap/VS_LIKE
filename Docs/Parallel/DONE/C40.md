# C40 — 실플레이 검증 요청 (CONTENT) · 111개 중 17개

**날짜:** 2026-09-04 · **애셋 변경 0** · **출력:** [`REQ/DEV.md`](../REQ/DEV.md) 요청-32

---

## 1. 왜 지금인가

내가 넘긴 것들이 **전부 "플레이해 봐야 안다"로 끝났다.** 세어 보니:

| | |
|---|---|
| `TUNING.md` 미체크 항목 | **111개** |
| 그중 🔴 (최우선) | **34개** |
| §4 플레이 세션 기록 | 🔴 **0줄** |
| 마감 | **2026-09-07** |

`C27` 이 상점 가격을 **10배**로 올리고, `C29` 가 영구 강화 7종을 신설하고,
`C38` 이 9층 동시 생존을 **294**로 올리고, `C30`·`C33`·`C34`·`C37` 이 그림과 연출을 갈아 끼웠는데
**아무도 한 판도 안 했다.**

---

## 2. 🔴 111개를 다 넘기지 않았다 — **17개만**

**전부 넘기면 아무것도 안 된다.** 고른 기준은 셋이다:

1. **최근에 크게 바꿨고 한 번도 안 본 것**
2. **한 판에 여러 개가 같이 답이 나오는 것**
3. **틀렸을 때 고칠 곳이 명확한 것** (대부분 CSV 한 칸)

---

## 3. 🔑 판 수를 정한 것은 **Dev 패널에 층 점프가 없다**는 사실이다

요청을 쓰기 전에 `DevPanel.cs` 를 읽었다. 있는 것: 적 소환 · 무적 · 레벨 · 골드 · 무기 레벨 · 승급.
**없는 것: 층 이동.** ⇒ **9층에 닿으려면 완주뿐이다.**

그래서 *"항목별로 확인해 달라"* 가 아니라 **"판 두 개"** 로 묶었다.

| | 무엇 | 왜 나뉘나 |
|---|---|---|
| **런 A** | 경제 5개 · **Dev 패널 금지** | 🔴 `Run Gold +100`·`Level +1` 을 누르면 경제 판정이 **통째로 무의미**해진다 |
| **런 B** | 후반·보스 8개 · **폭발광** · Dev 패널 허용 | 9층에 닿는 게 목적이라 보조가 필요하다 |
| §3 | 관찰 4개 | 어느 판에나 얹힌다 |

🔴 **런 B 의 직업을 못 박았다** — 폭발광은 이동 **3.5** 로 **가장 느리고**,
`C30` 이 `SlamWindup 1.15` 를 계산할 때 **그 값을 기준으로** 잡았다. 다른 직업으로 하면 판정이 안 된다.

---

## 4. §0 을 따로 뺀 이유

vSync 와 `EditorLoop` 를 **맨 앞에** 뒀다. 이걸 안 하면 §2 의 프레임 숫자가 **전부 쓰레기**다:

- 품질 5(Ultra)라 vSync 가 켜져 있고, 프레임이 **정확히 6.94 ms** 에 붙으면 성능이 아니라 vSync 다
- `EditorLoop` 가 `D35` 에서 **프레임의 45 %** 였다 — 빌드에 없는 비용이라 빼고 봐야 한다

🔑 **둘 다 내가 이미 `TUNING.md` §G 에 적어 둔 함정인데, 요청 안에서 다시 0단계로 뽑았다.**
문서에 있는 것과 **지금 이 작업의 첫 줄에 있는 것**은 다르다.

---

## 5. 🔴 오디오 18개를 **일부러 뺐다**

`TUNING.md` §C 의 청취 항목이 18개다. 이유 셋을 요청에 적었다:

1. **판마다 조건이 다르다** — *"Lv5 Sword 로"*, *"마법사+소환사 조합으로"*, *"30번쯤 들은 뒤에"*.
   §1·§2 에 못 얹고 **전용 판이 여러 개** 필요하다
2. **제출물이 시각화 중심 PDF** 다. 소리는 거기 안 들어간다
3. **틀려도 고치는 비용이 낮다** — `AudioLibrary.asset` 의 볼륨 한 칸

🔑 **"다 중요하다"는 우선순위가 아니다.** 뺀 것을 **왜 뺐는지 적어야** 나중에 되살릴 수 있다.

---

## 6. ⚠️ 빈칸을 통과로 읽지 않게 했다

> *"못 한 항목은 '못 했다'고 적어 줘. 빈칸이면 내가 통과로 읽는다."*

`C36` 에서 배운 것을 그대로 옮겼다 — **"0건은 깨끗하다는 뜻이 아니라 못 봤다는 뜻일 수 있다."**
17개 중 몇 개만 해도 되지만, **어느 것을 안 했는지는 알아야 한다.**

결과는 `TUNING.md` §4 형식으로 **답신에 적어 달라고** 했다 — 그 파일은 내 소유라
내가 옮기는 게 서로 파일을 안 건드리는 방법이다.

---

## 7. 남긴 것

- 🔴 **"플레이해야 안다"가 쌓이면 그것 자체가 부채다.** 111개가 될 때까지 아무도 한 판을 안 했다.
  **판정 항목을 만드는 사람이 판정이 실제로 일어나게 만들 책임도 진다**
- **요청의 단위를 "항목"이 아니라 "판"으로 잡았다.** `DevPanel` 에 층 점프가 없다는 사실 하나가
  구조를 정했다 — **먼저 상대의 도구를 보고 나서 일을 나눈다**
- 🔑 **뺀 것을 이유와 함께 적는 게 고른 것만큼 중요하다.** 오디오 18개를 그냥 빼면
  "잊었나"가 되고, 이유를 적으면 **"마감 뒤에 할 것"** 이 된다
- **금지 사항도 요청의 일부다.** 런 A 에서 Dev 패널을 쓰면 경제 판정이 무의미해진다 —
  *"이렇게 해 줘"* 만큼 *"이건 하지 마"* 가 중요할 때가 있다
