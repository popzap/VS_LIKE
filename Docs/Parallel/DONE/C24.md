# C24 — 수치·설계 문서의 옵시디언 지도 + 미판정 체감값 체크리스트 (CONTENT)

**2026-08-31** · 짝: `D23`(DEV — 정션 + `00_INDEX` + `지도/개발 지도`)
지시 원본: `D:\obsidian_claude\UNITY_GAME\05_문서이관_CONTENT.md`

---

## 1. 원인 — 문서가 커져서 못 찾는다

`Docs/` 는 36개 · 약 1.1MB 다. 그중 **CONTENT 소유가 184KB** 다.

| 문서 | 크기 | 문제 |
|---|---|---|
| `BALANCE.md` | 73KB | CSV 12종의 열 정의가 다 여기 있는데 **어디에 뭐가 있는지 모른다** |
| `TUNING.md` | 78KB | 미판정 항목이 흩어져 있어 **"오늘 뭘 보면 되나"** 가 안 보인다 |
| `DESIGN_CLASSES.md` | 32KB | `C6` 진행 중 |

에디터에서 `Ctrl+F` 로 훑는 것 말고는 진입로가 없다.
**옵시디언을 열람 계층으로 덧씌운다** — 문서를 옮기지 않는다.

🔴 **왜 옮기지 않나** — 옮기면 git 이력이 끊기고 `CLAUDE.md` 의 `Docs/...` 경로 규칙이
전부 거짓말이 된다. 그리고 두 벌이 되는 순간 **반드시 갈라진다.**
그래서 repo `Docs/` 를 vault 에 **디렉터리 정션**으로 걸고(`D23`, DEV) 그 위에 **포인터만** 쓴다.

> 피해야 할 실패 사례가 이미 vault 에 있었다 — `00_프로젝트_현황.md` 는 값을 베껴 놓고
> **2026-08-25 에서 멈춰** 있었다. 지도는 **가리키지, 담지 않는다.**

---

## 2. 변경한 파일

### repo (`C:\Unity\VS_LIKE`)

| 파일 | 무엇 |
|---|---|
| `Docs/Parallel/BOARD.md` | §1 에 `C24` 줄 등록 → 완료 후 제거 · §3 `C24`→`C25` |
| `Docs/Parallel/DONE/C24.md` | 이 파일 (신설) |

🔴 **repo 의 `.md` 본문은 한 글자도 안 고쳤다.** frontmatter 추가도 안 했다.
`BALANCE.md` · `TUNING.md` · `DESIGN_CLASSES.md` 는 **읽기만** 했다.
**코드·애셋 변경 0.** Unity 에디터 **미접촉**.

### vault (`D:\obsidian_claude\UNITY_GAME` — git 밖)

| 파일 | 무엇 |
|---|---|
| `지도/콘텐츠 지도.md` | **신설.** CONTENT 소유 3문서의 `##`/`###` 목차 + CSV 파이프라인 카드 + `SceneWiring.csv` 함정 경고 |
| `지도/미판정 체감값.md` | **신설.** `TUNING.md` 에서 **안 닫힌 항목만** 뽑은 체크리스트 (69건) |
| `게임 설계/설계_이벤트.md` | 0바이트 → `VS_LIKE/DESIGN_EVENTS.md` 리다이렉트 (목차만, 내용 복사 없음) |
| `게임 설계/설계-진화 시스템.md` | 0바이트 → `ROADMAP` §2-3·§2-6 + `BALANCE` 두 CSV 절 리다이렉트 |

---

## 3. 지도가 담은 것 / 안 담은 것

### `지도/콘텐츠 지도.md`

| 담았다 | 안 담았다 (일부러) |
|---|---|
| 3문서의 절 목차 + 각 절로 가는 링크 | **절의 내용** |
| CSV 12개 ↔ SO 9폴더 대응표 (**파일 이름과 링크만**) | Import 절차 — `BALANCE.md` §0~§1 로 넘긴다 |
| 🔸 `Economy`·`SceneWiring`·`Events` 는 SO 가 아니라 **씬 컴포넌트에 직접 쓴다**는 구분 | 열 정의 |
| CSV 가 **아닌** 2파일(`AudioLibrary.asset` · `Pretendard SDF.asset`)은 인스펙터 편집 | |
| 🚨 `SceneWiring.csv` 함정 한 줄 — 새 아이템/웨이브/직업은 `allItems`·`normalWaves\|eliteWaves`·`classes` 에 경로를 안 넣으면 **게임에 안 나온다** | |
| `CLAUDE.md` §1 의 3분기 (동작하나 / 느낌이 맞나 / 어떻게 고치나) | |

### `지도/미판정 체감값.md`

`TUNING.md` **880줄 전체**를 훑어 **미체크 69건**을 뽑았다. 닫힌 항목(촉수 정렬 순서)은 뺐다.

항목 형식은 전부 **판정 기준 · 고칠 파일 · 현재값** 셋을 갖춘다:
`- [ ] <무엇을 보는가> — 이상하면 <파일>의 <필드>를 <현재값>에서 조절 → [링크]`

🔴 **원본에 값이 없으면 지어내지 않고 "원본에 없음" 이라고 썼다.**
1단계 `무기 Damage vs 적 MaxHp` 한 건이 그렇다 (`TUNING.md` 에 `—` 로 비어 있다).

맨 위에 **판정 자체를 막는 4건**을 먼저 세웠다. 이게 없으면 플레이해도 "판정했다"가 거짓이 된다:

| 막는 것 | 왜 |
|---|---|
| `bgm_vol=0.00` / `sfx_vol=0.00` | 저장된 사용자 설정. `PlaySfx()` 가 `SfxVolume <= 0f` 면 즉시 리턴 → **오디오 30여 건 판정 불가** |
| 엘리트·보스가 일반 웨이브에 안 나온다 | 랭크 관련 항목 판정 불가 |
| 픽업 클립이 `AudioLibrary.asset` 에 없다 | `D20` 대기 |
| `Crit` 배선이 아예 없다 | `CalculateDamage` 가 치명타 결과를 버린다 |

그리고 `TUNING.md` §3 의 **단계 순서(0→1→2→3, 한 세션에 한 단계)** 를 그대로 옮겼다.

> ℹ️ **체크박스의 원본은 여전히 `TUNING.md` 다.** vault 에서 체크하지 않는다 —
> 노트 맨 위에 그렇게 적어 뒀다.

---

## 4. 검증 로그

| 확인 | 결과 |
|---|---|
| 정션 (`D23`, DEV) | ✅ `D:\obsidian_claude\UNITY_GAME\VS_LIKE\` 에 `BALANCE.md` `TUNING.md` `DESIGN_CLASSES.md` `DESIGN_EVENTS.md` `ROADMAP.md` `SETUP_STATUS.md` `TODO.md` `Parallel/` 전부 보인다 |
| vault → repo 링크 | ✅ 내 노트 4개의 상대링크 **20개 전부 실제 파일에 도달**(`%20` 디코드 후 확인). MISS 0 |
| vault 에 repo 문서 복사본 | ✅ **없다.** 목차·파일명·링크만 있다 |
| repo `.md` 본문 변경 | ✅ `BOARD.md`(내 줄) + `DONE/C24.md` 뿐 |
| 코드·애셋 변경 | ✅ **0** |
| Unity 에디터 | ✅ 미접촉. `BOARD.md` §2 는 읽기만 했다 (`IDLE`) |

---

## 5. 발견한 것 · 넘기는 것

### ✅ `BOARD.md` §0 에 `Docs/DESIGN_CLASSES.md` 는 **이미 들어가 있다**

지시서 §5 는 없으면 `REQ/DEV.md` 에 요청을 남기라고 했는데, **확인해 보니 이미 있었다**
(`BOARD.md:13`). **중복 요청을 만들지 않고 보고만 한다.**

> ⚠️ 다만 §0 표가 아직 안 담은 CONTENT 소유 경로가 더 있다 —
> `Tools/Art/` · `Tools/Audio/` · `Docs/Parallel/DONE/C*.md` · `Docs/Parallel/REQ/DEV.md`.
> 이번 지시 범위가 아니라 **건드리지 않았다.** DEV 가 §0 을 손볼 때 같이 보면 된다.

### ⚠️ `개발_아이디어/03_CONTENT_세션_프롬프트.md` — **지우지 않았다**

지시서 §4-3 은 `Docs/Parallel/SESSION_PROMPT.md` 와 대조해 **중복이면** 아카이브하라고 했다.
대조 결과 **중복이 아니다.** 원본에 없는 내용이 있다:

| vault 쪽에만 있는 것 | 성격 |
|---|---|
| **§6 "내 파트가 특히 조심할 것"** 7개 — 가짜 투명배경(I-41) · Static 폰트 115자(I-60) · SO 직접 편집 금지 · `SceneWiring.csv` · `Economy.csv` 행 삭제 · 큰 바이너리 · UI 영문 | 🔴 **전부 실제 사고 이력.** 원본 `SESSION_PROMPT.md` 에 대응 절이 없다 |
| §3 "`Assets/` 안에 직접 넣지 않는다 — DEV 가 Refresh 할 때 반쯤 만든 파일이 딸려 간다" | 원본은 규칙만 있고 **이유가 없다** |
| §3 "`BOARD.md` §2 에 쓰지 않는다. 읽기만 한다" | 원본에 없다 |
| §4 "배선 요청은 **어느 SO 의 어느 필드**인지까지" (Sentinel 예시) | 원본에 없다 |
| §1 의 소유 경로 **명시 목록** | 원본은 `BOARD.md` 로 넘기기만 한다. ⚠️ 다만 이 목록은 **낡았다** — `DESIGN_CLASSES.md` · `Tools/` · `DONE/C*.md` 가 빠져 있다 |
| 말미 "CONTENT 의 첫 일감 후보" | 2026-08-29 시점. **낡았다** |

그래서 **아카이브하지 않고 그대로 뒀다.** 두 가지 이유가 더 있다:

1. DEV 의 `00_INDEX.md:74` 가 이 노트를 **살아 있는 vault 노트로 링크**하고 있다.
   아카이브로 옮기면 그 링크가 깨진다
2. 이 파일은 `개발_아이디어/` 안에 있는데, 지시서 §4-3 은 그 폴더를 **DEV 것**이라고 못박았다.
   §1 은 이 파일 하나만 내 것이라고 했다 — **소유가 어긋난다**

👉 **사용자 판단이 필요하다.** 셋 중 하나다:
   (a) §6 을 `SESSION_PROMPT.md` 로 올리고 vault 쪽을 아카이브
   (b) vault 쪽을 정본으로 두고 §1 목록만 갱신
   (c) 그대로 둔다

`02_DEV_세션_프롬프트.md` 와 `분업화 아이디어.md` 는 **이름이 지시서에 없어서 손대지 않았다.**

---

## 6. 남긴 것 — 다음에 이걸 쓰는 사람에게

- **지도는 가리킨다. 담지 않는다.** 값을 베끼면 그 노트는 베낀 날짜에 고정되고,
  고쳐야 할 곳이 두 곳이 되며, 결국 **어느 쪽이 맞는지 아무도 모르게 된다**
- **체감 체크리스트에서 제일 중요한 건 항목이 아니라 "막힘" 절이다.**
  소리가 0인 채로 플레이하면 오디오 30건이 전부 **가짜 판정**이 된다
- **원본에 없는 값은 지어내지 않는다.** "원본에 없음" 이 정확한 답이다
