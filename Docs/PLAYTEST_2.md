# 실플레이 체크리스트 2 — `D63`~`D78` 확인

> **이 문서는 사용자가 채우는 곳이다.** 세션은 **질문만** 넣고, **답은 사용자가 적는다.**
> `PLAYTEST.md` 와 같은 규칙이다. 답이 채워지면 DEV 가 수치를 고치고 `TUNING.md` 로 옮긴다.
>
> **적는 법:** `[ ]` 를 `[x]` 로 바꾸고, `→` 뒤에 한 줄만 쓰면 된다. 빈칸으로 둬도 된다.
>
> 🔴 **§1 은 제가 볼 수단이 아예 없는 것들이다.** `ScreenSpaceOverlay` 캔버스는 렌더 텍스처로 안 잡혀
> **UI 를 제가 확인할 방법이 없다** — `D72` 에서 맵 화면이 망가진 걸 사용자가 잡아 주셨던 이유다.
> **여기만 봐 주셔도 값이 제일 크다.**

---

## 0. 어떻게 하면 되나

**한 판이면 충분하다.** 죽어도 된다 — 중도 사망도 유효한 데이터다.

| | |
|---|---|
| 🔴 **Dev 패널(백틱 `` ` ``)을 쓰지 말 것** | `Level +1`·`Run Gold +100` 을 누르면 §2·§3 이 무의미해진다 |
| 🟢 **다만 §1-G 만은 Dev 패널로 봐야 한다** | 보스를 만나려면 9층까지 가야 해서다. **그 판은 따로 한 번** |
| 판이 끝나면 | Console 의 `[GameManager] 런 정산 —` 줄을 그대로 옮겨 적어 주시면 된다 |

---

# 1. 🔴 화면 — 제가 못 보는 것들

## 1-A. 승급 연출이 이제 "가장 큰 보상"만큼 큰가

`D75` 에서 **네 가지를 같은 순간에** 겹쳤다 — 히트스톱 0.12초 · 주변 적을 밀어내는 충격파 ·
화면 흔들림 · **파동 3연**.

🟡 **제가 못 잰 것:** 파동 2·3번째와 흔들림. `RunCommand` 왕복(1초+)이 파동 수명(0.55초)보다 길다.
히트스톱과 충격파는 숫자로 확인했다(`timeScale 0` · 반경 안 16.0 / 밖 0.0).

- [ ] 이제 크다 — `PLAYTEST.md` 의 "약하다" 가 해결됐다
- [ ] 아직 약하다
- [x] 과하다 (눈이 아프다 / 화면이 너무 흔들린다)
→ 3개의 파동이 동시에 나와 난잡해 큰 파동 하나면 될거 같아

**파동이 3연으로 보이나** (하나로 뭉쳐 보이는지)
- [x] 세 번 겹치는 게 보인다
- [ ] 하나로 뭉쳐 보인다
→ ______________________________________________

> 뭉쳐 보이면 `Economy.csv` 의 `EvolutionManager,promotePulseInterval` `0.13 → 0.18~0.22`.
> 과하면 `promoteShakeMagnitude 0.5 → 0.3` 또는 `promoteHitstop 0.12 → 0.08`.

## 1-B. 캐릭터가 이제 **가는 쪽으로** 기우나

🔴 `D65` 에서 고쳤다 — 예전에는 **반대로** 기울었다(`flipX` 보정이 틀렸고 주석도 반대였다).

- [ ] 가는 쪽으로 기운다 (자연스럽다)
- [x] 아직 반대다
- [ ] 기우는 게 안 보인다
→ warrior, mage, demolitionist, assasin, summoner, doomlord는 아직 반대로 보여
ranger, sentinal 은 제대로 움직여
나머지는 미확인

## 1-C. 버프 오라가 읽히나

`D67`·`D68` — 무적 **하늘색** · 공속 **주황** · 이속 **연두**. 남은 시간이 1.5초 아래면 **점등**한다.

- [x] 색으로 뭘 먹었는지 안다
- [ ] 오라는 보이는데 색 구분이 안 된다
- [ ] 오라가 잘 안 보인다
→ ______________________________________________

**🔴 맞을 때마다 오라가 번쩍이나** (그러면 안 된다 — 피격 무적과 타이머를 갈랐다)
- [x] 안 번쩍인다
- [ ] 맞을 때도 번쩍인다 🔴
→ ______________________________________________

> 안 보이면 `PlayerVisual` 의 `auraWidth` `9 → 12`.
> 🔴 **인스펙터가 아니라 씬 값이 정본이다** — `D68` 에서 코드 기본값만 고쳤다가 안 먹은 적이 있다.

## 1-D. 맵 화면이 이제 읽히나

`D72` 에서 셋을 고쳤다 — 세로 정렬을 **0 중심 대칭**으로 · 연결을 **단조**로(교차 판당 1.0) ·
선을 **박스 테두리까지만**.

- [x] 읽힌다
- [ ] 아직 난잡하다
→ ______________________________________________

## 1-E. 보스 오프스크린 화살표가 쓸모 있나

`D66` — 보스가 화면 밖이면 테두리에 빨간 삼각형이 뜨고 맥동한다.

- [ ] 어디 있는지 바로 알았다
- [x] 화살표를 못 봤다
- [ ] 거슬린다
→ 화살표가 안보여

## 1-F. 클리어 화면이 볼 만한가

`D66` — `Layer / Level / Kills / Time / Biggest hit / Earned` + **직업 사슬** + **먹은 아이템 전부**.

- [ ] 볼 만하다
- [x] 글자가 너무 작다 / 넘친다
- [ ] 더 있으면 좋겠다 →
→ 너무 난잡해 아이템은 아이작의 번제 같이 먹은 아이템들에 대해 아이콘 과 lv 표시로 간단하게
너무 글자를 많이 쓰지 마 글자 크기도 키우고 거리도 띄워서 눈에 잘들어오게

## 1-G. 🟢 **Dev 패널로 봐야 하는 것** — 보스 패턴 2종

백틱(`` ` ``) → **`Spawn Boss`** 를 누르면 보스만 따로 부를 수 있다 (`D64`).
쿨다운이 있으니 **10~20초쯤 지켜봐야** 두 기술이 다 나온다.

**① 쫄 소환 + 돌진** — 쫄이 나오고 → 보스가 멈춰 노려보고 → 돌진하고 → 굳는다

- [x] 피할 수 있다
- [ ] 못 피한다 (너무 빠르다 / 노려보는 시간이 짧다)
- [ ] 너무 느려서 위협이 안 된다
→ ______________________________________________

**② 예고 일직선** — 보스에서 플레이어 쪽으로 원 7개가 줄지어 깔리고 동시에 터진다

- [ ] 피할 수 있다
- [ ] 못 피한다
- [x] 일직선으로 안 보인다 (그냥 원 여러 개)
→ 원여러개를 쓰지 말고 직사각형의 범위로 새로 만들어

> 못 피하면 `Bosses.csv` 의 `ChargeWindup 0.75 → 1.0` / `LineWindup 1.1 → 1.4`.
> 🔴 `ChargeSpeed`·`SlamRadius` 를 먼저 줄이지 말 것 — 줄이면 *"피한다"* 가 *"안 맞는다"* 가 된다.

**🟢 `Dev 패널` 자체가 잘 보이나** (`D63`·`D64` 에서 **렌더링을 못 봤다**)
- [x] `Clear Node` · `Spawn Boss` · `Wave timer` 버튼이 다 보이고 눌린다
- [ ] 뭔가 깨져 있다 →
→ ______________________________________________

## 1-H. 잔손 4가지

- **레벨업에서 `1`·`2`·`3` 키로 카드가 골라지나** — [o] 된다 [ ] 안 된다
- **리롤 버튼이 이제 눌러 보이나** (`PLAYTEST.md` 의 *"회색이라 시인이 어렵다"*) — [o] 보인다 [ ] 아직 안 보인다
- **`[Z] Build` 안내가 좌하단으로 가서 안 묻히나** — [o] 읽힌다 [ ] 아직 묻힌다
- **상점에서 `KILLS/TIME/LEVEL` 이 사라진 게 나은가** — [o] 낫다 [ ] 있는 게 나았다
→ 리롤 글자 색을 바꿔 하얀색으로
Build 안내 중앙 아래로 (맨 아래 말고 중앙에서 아래 2:1 느낌으로)
상점에 아직 `KILLS/TIME/LEVEL' 쪽에 같이 있던 - 가 아직 남아있어 확인해서 지워

---

# 2. 숫자 — 한 판 돌리면 나온다

## 2-A. 🔴 한 판에 **몇 레벨**까지 갔나

`D65` 에서 XP 곡선을 고쳤다. 예전에는 **`Lv11` 이 810** 이라 벽이었다(Lv10 은 260).

| XP 예산 | 예전 | 지금 |
|---:|---:|---:|
| 2,600 | Lv13 | **Lv24** |
| 3,500 | Lv13 | **Lv28** |

- [x] 도달 레벨 **Lv 20** (몇 번째 전투까지: **4번째 = 보스** · 완주)

> 통과선 **20~28레벨**. 너무 적으면 `Economy.csv` 의 `xpThresholds` 를 더 내리고,
> 너무 많으면 `xpTailStep 12 → 16`.

**🔴 DEV 가 로그로 채웠다 (2026-09-06 · `D96`).** 사람이 아니라 `Editor.log` 가 답한 항목이다.
`State → LevelUp` 을 센 값이라 **정확하다** — 전투별로 **7 · 7 · 3 · 2 = 19회**, 시작 Lv1 이므로 **Lv 20**.

⚠️ **통과선의 아래 끝에 딱 걸렸다.** 20~28 중 **20** 이다.
이 판은 **1스테이지(10노드) 완주**였고, 그중 전투 노드가 **4개뿐**이었다
(나머지는 이벤트 4 · 상점 2). ⇒ **곡선이 낮은 게 아니라 전투 수가 적었을 가능성**이 크다.
`2-C` 에서 사용자가 적은 *"1스테이지라 그렇지 2~3스테이지 분량이면 적절"* 과 같은 이야기다.
🔑 **수치를 만지기 전에 전투 노드 비율을 먼저 볼 것** — `Economy.csv` 의 `weightEvent`(지금 이벤트가 많다).

## 2-B. 🔴 **승급까지 몇 분** 걸렸나 (`PLAYTEST.md` 에서 7분이었다)

- [x] ______ 6분 · 도달 층6/10 ______
- [ ] 승급 못 했다

> 🔴 **승급 조건은 안 건드렸다.** 이미 요구대로(무기 만렙 + 건물 Lv3)여서다 —
> 7분이 걸린 건 조건이 아니라 **거기 도달할 레벨업이 안 나와서**였고, 그건 `2-A` 로 고쳤다.
> 그래도 오래 걸리면 **그때** 조건을 `무기 Lv4 + 건물 Lv2` 로 내린다.



## 2-C. 상점을 **몇 번** 만났나

`D70` 에서 배치 규칙을 넣었다 — 400판 통계로 **판당 2.65회 · 0회인 판 0건**
(`PLAYTEST.md` 때는 9층 도는 동안 **0회**였다).

- [x] ______ 1~2번
이게 1스테이지라 그렇지 2스테이지~3스테이지 까지 분량을 늘린다고 생각하면 적절해
## 2-D. 상점에서 **살 수 있었나**

`D69` — 기준가를 0.6배로 내리고 층·레벨로 올린다 (Sword 70 → 0층 **42** · 8층 Lv4 **246**).

- [x] 초반에 살 수 있었다
- [ ] 초반에도 비쌌다
→ ______________________________________________

- [x] 후반에 골드가 남았다 (`PLAYTEST.md` 때 `M=1168`) — 다만 **74** 다
- [ ] 후반에도 빠듯했다
→ **`D69` 의 가격 스케일이 먹혔다.** `PLAYTEST.md` 의 `M=1168` 이 이번엔 **74** 다 — **16분의 1**.
  마지막 상점은 열었다가 **아무것도 안 사고 닫았다**(살 게 없었는지 비쌌는지는 로그로 못 가른다).

**판 끝 정산 줄**
```
[GameManager] 런 정산 — 메타 골드 +90 · 남은 런 골드 74 소멸
```

**🔴 DEV 가 로그로 채웠다 (2026-09-06 · `D96`).** 이 판의 상점 활동 전부:

| 상점 | 무엇 |
|---|---|
| 1번째 | 리롤 **4회**(누계 40→55→70→85G) · **구매** Sword 210G · **제거** Octopus +84G · **제거** Turret +96G · **구매** Damage Up 101G |
| (이벤트) | 환전 — 런 골드 **87 → 메타 29** (환율 3:1 · 상한 40) |
| 2번째 | **열었다가 아무것도 안 하고 닫았다** |

🔑 **`D71` 의 제거·리롤이 실제로 쓰였다.** 제거 2회로 **+180G** 를 회수해 그 돈으로 다시 샀다 —
*"살 게 없으면 판다"* 가 설계대로 돌았다는 뜻이다. 배선만 보고는 알 수 없는 것이었다.

⚠️ **남은 74G 는 "남았다"로 읽되 과하게 읽지 말 것** — Sword 한 자루가 210G 였다.
74 는 **아무것도 못 사는 액수**라, `M=1168`(남아돌던 때)과는 성격이 다르다.

## 2-E. 🔴 **아이템 칸이 2개로 줄었다** — 적은가

`D71` 이 휴식 칸을 넣으면서 상점 진열이 `Item · Item · Heal` 이 됐다.
(살 게 없으면 `Heal · Exchange · Exchange`)

- [ ] 2개로 충분하다
- [x] 3개였으면 좋겠다 → `shopSlotCount 3 → 4` (**카드 4장이 화면에 들어가는지 봐 주셔야 한다**)
→ 이거는 수정해서 heal이 살게 없어야지만 뜨게 해서 3개로 보여 해당 테스트가 수정전 버전인거 같아

**휴식 / 골드 전환 칸을 실제로 봤나**
- [ ] 봤다 → [ ] 샀다 [ ] 안 샀다
- [x] 못 봤다 (살 아이템이 계속 있었다)
→ 아이템 소지 제한이 적용되고 있는지 확인해줘 ( 캐릭터 마다 무기, 패시브, 건물 소지 개수 상한 있는거)

**✅ DEV 확인 (2026-09-06 · `D97`) — 상한은 실제로 걸린다.**
플레이 모드에서 `CanAcquire` 가 막을 때까지 전부 사들여 봤다:

```
칸 상한 — 무기 3/3 · 패시브 5/5 · 건물 5/5
칸 3개: Heal 35G · Exchange 100G · Exchange 100G
```

🟢 꽉 차면 `Heal · Exchange · Exchange` 로 바뀌는 것도 확인했다(`D71` 설계대로).
🔵 사용자 스크린샷의 콘솔에 있던 `구매 거절 — 'Bow' 을(를) 지금 받을 수 없다. 골드는 그대로다` 도
**`B12` 가드가 제 일을 한 것**이라 정상이다 — 골드를 안 깎고 거절한다.
⚠️ *"꽉 찼는데 아이템이 나왔다"* 는 **업그레이드**였을 가능성이 크다 — 이미 가진 것의 레벨 올리기는
칸을 더 먹지 않으므로 꽉 차도 살 수 있는 게 맞다(스크린샷의 `Fireball Lv.1 → 2` 가 그것이다).

## 2-F. 체력 재생이 후한가

`D74` — 기본 **0.4/s**(분당 24) + 패시브 `Regeneration`(0.4~2.6/s).

- [x] 적당하다
- [ ] 너무 후하다 (안 죽는다)
- [ ] 있는지도 몰랐다
→ ______________________________________________

## 2-G. 기습 이벤트가 "기습" 같은가

`D76` — **35초** 동안 **100마리**가 사방 테두리에서 쏟아진다 (원거리 Demon 제외).
🔴 예전에는 **그냥 노말 웨이브**였다 — 배선이 아예 없었다.

- [x] 만났다 → [o] 짧고 빽빽했다 [ ] 너무 쉬웠다 [ ] 못 버텼다
- [ ] 이벤트 노드를 안 밟았다
→ 이벤트인지 몰랐어 브금을 새로 만들어서 위급한 상황을 연출해

## 2-H. 진화 특전을 봤나

`D68` — **Excalibur = 골드 2배** · **Windforce = 필드 드랍 지속 2배** · **Devastator = 투사체 2배**

- [x] 진화했다 → 어느 것: devastator, excalibur · [o] 체감됐다 [ ] 뭐가 달라졌는지 몰랐다
- [ ] 진화를 못 했다
→ ______________________________________________

## 2-I. 이속 픽업(연두)을 먹어 봤나

`D68` 신설. 드랍률 0.008 (공속 0.01보다 낮다).
🟢 **`D80` 에서 전용 그림이 꽂혔다** — 이제 공속과 **모양이 다르다**:
공속 = **파랑 세로 번개** ↔ 이속 = **연두 가로 꺾쇠 »»»** (주축이 90도 다르다).

- [x] 먹었다 → [o] 쓸모 있었다 [ ] 별로였다
- [ ] 못 봤다
- [ ] 🔴 **공속 픽업과 헷갈렸다** → 언제? ______________________________________________
→ ______________________________________________

> 🟡 **한 가지 알아 둘 것** — 스프라이트 칸 크기는 둘 다 같은데(0.650u),
> **그려진 내용의 가로 폭이 이속만 2.45배**다 (알파 폭 Haste 352px vs Swift 864px · 세로는 둘 다 480px).
> 화면에서 **이속만 유독 넓어 보인다.** 거슬리면 CONTENT 가 그림을 좁힌다 (`요청-56`).

---

## 2-J. 🟢 **바닥을 두 번 고쳤다** — 이제 자연스러운가 (D78 · D79)

`D78` 이 칸 크기를 맞춰 틈을 없앴고, `D79` 가 두 가지를 더 했다:

| | |
|---|---|
| 타일이 **절반**이 됐다 | 0.500u(36px) → **0.250u(18px)** |
| 튀는 색을 **없앴다** | 타일 10종이 전부 **같은 한 색**이다. 층 구분은 타일맵 전체 색조로 옮겼다 |

- [x] 이제 자연스럽다
- [ ] 아직 뭔가 튄다 → 무엇이? ______________________________________________
- [ ] 이제는 **너무 밋밋하다** (무늬가 안 보인다)
→ ______________________________________________

> 🔴 **밋밋하다면 그건 튐을 없앤 대가다.** 되돌리는 손잡이는 두 개다:
> ① `GroundTiler` 의 `DeepTint` — 층이 깊어질 때의 색조. 지금 (0.62, 0.68, 1.00)
> ② `Tools/Art/retint_tiles.py` 의 `TARGET` — 모든 타일이 모이는 한 점.
> 여기에 다시 폭을 주면 무늬가 살아나지만 **튐도 같이 돌아온다.**
> (칸 크기는 `*.png.meta` 의 `spritePixelsToUnits` **와** 씬 `Grid.Cell Size` 를 **반드시 같이** — 4096↔0.25 · 2048↔0.5)

---

## 2-K. Ogre 접촉 피해가 후반에 과한가 (CONTENT `요청-36 §1`)

CONTENT 가 Ogre 를 `Normal3` 3마리 · `Elite1` 2마리로 내보냈다(행동 `Blocker` 를 보이게 하려고).
접촉 피해 25 는 **9층에서 38.5** 다. 다만 `Blocker` 라 쫓아오지 않으니 **피할 수 있어야** 맞다.

- [x] 피할 수 있었다 (의도대로)
- [ ] 못 피하고 맞았다 → 어떤 상황? ______________________________________________
- [ ] Ogre 를 후반에 못 봤다
→ ______________________________________________

> 이상하면 CONTENT 가 수를 3/2 → **2/1** 로 줄인다 (`Waves.csv` 의 `Normal3`·`Elite1` 행).

---

## 2-L. 🔴 적이 커졌다 — 히트박스도 같이 커졌다 (D79)

`SizeScale` 을 전부 **×1.5** 로 올렸다. 플레이어(72px) 대비:

```
Wolf 47px(65%) · Slime 48(68%) · Goblin 54(75%) · Zombie 57(80%) · Demon 66(93%) · Ogre 86(120%)
```

🔴 크기는 `transform.localScale` 이라 **콜라이더·접촉 피해 범위·서로 밀치는 반경이 같이 1.5배**다.
**로그로는 판정할 수 없다.**

- [ ] 크기가 맞다
- [ ] 아직 작다  /  [ ] 너무 크다
- [x] **맞기 쉬워졌다** (피할 수 있던 걸 맞는다)
- [ ] **적끼리 너무 벌어진다** (밀치는 반경이 커져 뭉치질 않는다)
→ 적와 플레이어 크기는 적절한데 화면에 너무 끼는 느낌이라
F7,F8로 화면 배율을 조절할 수 있게 해줘
> 해당 배율 조절 조작에 대해서는 메인화면에서 도움말 페이지에서 안내해줘

> 이상하면 `Assets/Game/Balance/Enemies.csv` 의 `SizeScale` 열(7번째)을 고치고
> `Game/Balance/Import CSV -> ScriptableObjects` 를 돌린다. 지금 값 —
> Slime 1.35 · Goblin 1.5 · Zombie 1.6 · Wolf 1.3 · Demon 1.85 · Ogre/Bonecaller 2.4

---

## 2-M. 🔴 좌우로 움직일 때 몸이 **가는 쪽으로** 기우나 (D79 · 세 번째 시도)

`D65` 가 부호를 고쳤고 그래도 반대로 보였다. 원인은 부호가 아니라 **기준선**이었다 —
전단이 스프라이트 한가운데를 기준으로 걸려서 **위가 오른쪽이면 다리는 왼쪽으로** 밀렸다.
이제 밑변이 기준이라 **발은 거의 고정되고 위만 기운다.**

- [ ] 이제 가는 쪽으로 기운다
- [ ] 아직도 반대로 보인다 → 어떤 상황에서? ______________________________________________
- [x] 기울기가 **너무 세다**  /  [ ] **너무 약해서 안 보인다**
→ 기울일 필요가 있을까? 좌우 이동에 대해서 애니메이션을 만들어놔서 굳이 필요할까 싶어

> 세기는 `PlayerVisual` 의 `leanAmount`(지금 0.10). 🔴 기준선을 밑변으로 옮기면서
> **머리가 움직이는 폭이 2배**가 됐다(같은 값에서). 세면 0.05~0.07 로 내린다.

---

# 3. 결정이 필요한 것 2건

## 3-A. `B13` — `Luck` 이 최종 스탯에 안 들어간다

`PlayerStats.RecalculateStats` 가 `Final` 을 **필드별로 나열**해서 만드는데 **`Luck` 이 빠져 있다.**
패시브의 `Luck` 은 뒤이어 더해지므로 **패시브만 작동**한다 — 그래서 지금까지 안 걸렸다.

🟡 **아직 안 터진다** — 메타 업그레이드에 `Luck` 이 없고 `baseStats.Luck` 행도 없다.
**둘 중 하나만 생기면 그 순간 터진다.**

🔑 이 나열은 **두 번** 사람을 속였다 — `Luck`, 그리고 `D74` 의 `HpRegen`.

- [ ] **A** — `Luck` 한 줄만 더한다 (5분 · 다음 필드에서 또 밟는다)
- [x] **B** — `StatBlock.Add(a, b)` 로 묶는다 (🔴 스탯 합산은 게임 전체가 매달린 자리라 **전 스탯을 다시 재야 한다**)
- [ ] 마감 뒤로 미룬다
→ ______________________________________________

## 3-B. 마감(2026-09-07)까지 뭘 더 할까

남은 큰 항목은 **메타 강화 화면**(`ROADMAP.md`)과 **콘텐츠 물량**(적 7/12 · 보스 1/3 · 상위 직업 4/8)이다.

- [x] 이 체크리스트의 수치 조정만 하고 마무리
- [ ] 콘텐츠를 더 넣는다 → 무엇: ______
- [x] 포트폴리오 PDF 준비를 먼저 한다
→ 내가 추가로 작성해둔 수정사항, 요청사항만 작업하고 포트폴리오 준비로

---

> ℹ️ **CONTENT 몫 3건은 여기 없다** — `Pickup_Swift` 아이콘 · `Regeneration` 아이콘 ·
> 바닥 타일 크기(`B-4`). 전부 그림이라 `Parallel/REQ/CONTENT.md` `요청-46`·`51`·`53` 에 있다.

추가 사항들
1. 상점에서 보유 골드 표시 지금 안보여
2. 캐릭터 체력바 밑에 현재 소유한 아이템들 ㅁ 박스안에 아이콘으로 보여줘
   그러면 상한까지 몇개남았는지 확인하기가 쉬워
3. 캐릭터 얼굴을 보여주는 박스가 아직 비어있는 상태 현재 class의 상반신이 들어가게
4. 상점에서 상인 캐릭터가 너무 작게 보여 새로 일러스트 뽑은거 첨부했으니 활용해

---

# 4. 📋 이번 판의 원본 기록 (2026-09-06 · `Editor.log`)

> `2-A`·`2-D` 를 채운 근거다. **사람 판정이 아니라 로그**이므로 언제든 다시 셀 수 있다.
> 사용자가 준 단서는 정산 한 줄(*"메타 골드 +90 · 남은 런 골드 74 소멸"*)뿐이었고,
> 그 줄을 `Editor.log` 에서 찾아 **거슬러 올라가 판 전체**를 복원했다.

## 판 개요 — **Warrior → Warden · 1스테이지 완주**

| 항목 | 값 | 근거 |
|---|---|---|
| 직업 | Warrior (시작 무기 Sword Lv1) → **Warden** 승급 | `직업 승급` |
| 도달 레벨 | **Lv 20** (레벨업 19회) | `State → LevelUp` × 19 |
| 노드 | **10개** — 전투 4 · 이벤트 4 · 상점 2 | 상태 전이 |
| 끝 | **보스 처치 → 완주** | `보스 처치 — 웨이브 즉시 클리어` |
| 정산 | 메타 **+90** · 남은 런 골드 **74 소멸** | `런 정산` |

## 전투별 레벨업

| 전투 | 레벨업 |
|---|---|
| 1번째 | 7 |
| 2번째 (야전 승급 발동 중) | 7 |
| 3번째 | 3 |
| 4번째 (보스) | 2 |
| **합** | **19 → Lv 20** |

## 이벤트 4개

| | 결과 |
|---|---|
| Ancient Shrine (Reward) | 받았다 |
| Field Promotion | **수락** — 야전 승급 발동, 2번째 전투 끝에 `야전 승급 종료` |
| Field Promotion (두 번째) | **거절** |
| Exchange | 런 골드 **87 → 메타 29** (환율 3:1 · 상한 40) |

🔑 **`D76` 의 이벤트 배선이 실제로 돌았다.** 같은 이벤트가 두 번 나왔고 한 번은 거절도 됐다.

## 🟡 로그가 답하지 못한 것

| 질문 | 왜 못 답하나 |
|---|---|
| `2-B` 승급까지 **몇 분** | 로그에 **시각이 안 찍힌다.** 승급은 4번째(마지막) 전투에서 났다는 것만 안다 |
| `2-D` 마지막 상점에서 **왜 안 샀나** | *"살 게 없었다"* 와 *"비쌌다"* 를 로그가 못 가른다 |
| `2-G` 기습 이벤트 | **이 판엔 안 나왔다** (`Ambush` 로그 0건). 이벤트 4개가 전부 다른 종류였다 |
| `2-I` 이속 픽업 | 픽업 획득은 로그를 안 남긴다 |
| §1 전부 | 화면이라 로그로는 원리적으로 못 본다 |

## 🔵 곁에서 하나 확인했다 — 버그 아님

```
[EvolutionManager] 직업 승급 — Warden → Warden
```

승급 전후가 같은 이름이라 이상해 보이는데, `EvolutionManager.cs:358` 이
`{evo.EvolutionName} → {evo.ResultClass.ClassName}` 을 찍는다.
**앞은 이전 직업이 아니라 레시피 이름**이고, 그 레시피 이름이 마침 `Warden` 이다.
⇒ **표기 혼동일 뿐 동작은 정상.** 고치려면 앞을 `_ps.Class.ClassName` 으로 바꾸면 된다.
